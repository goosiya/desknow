# Deferred Work

## ⚠️ 배포 전 사람-확인-필수: 모바일 시각 패리티 실기기 재검증 (Story 9.4 · correct-course 2026-06-20)

- **⚠️ iOS/Android 네이티브 실기기 시각 패리티 재검증** [apps/mobile 전 화면 · EAS dev build] — Story 9.4(웹↔모바일 시각·인터랙션 패리티 정정)의 AC1~AC6 검증은 **웹 vs Expo Web(react-native-web) 한정**이다. 실제 네이티브 빌드는 **폰트 렌더링·그림자(`elevation.fab`/`elevation.dialog` boxShadow)·터치 하이라이트·세이프에어리어·키보드 회피**에서 Expo Web보다 더 벌어질 수 있어, Expo Web "일치"가 네이티브 "일치"를 보장하지 않는다. KTH 확정(2026-06-19 correct-course): 이 재검증은 9.4 **차단 AC가 아니라 별도 사람-확인-필수** 단계로 분리(EAS dev build·시뮬레이터/실기기 필요 → 자동 빌드/푸시·Playwright로 안 잡힘). **트리거(언제):** EAS dev build로 모바일을 처음 실기기/시뮬레이터에 올리는 시점(배포 준비). **회수(무엇을):** 9.4가 정정한 항목 전수(글로벌 헤더·인증화면 하단탭·온보딩 4슬라이드 캐러셀·룸폼 12h 시간입력/룸형태 세그먼트·FAB 아이콘·로그인 정렬/라벨색·홈 제목·챗봇 시트 입력바·후기 답글 틴트·뒤로버튼)를 **실기기에서 웹과 재대조**하고, 1.6 deferred(아래 "dev-story 1.6": 모바일 boxShadow RN 렌더 검증·mobile-web 폰트)도 같은 패스로 함께 회수. **책임:** dev-story 9.4 자체는 Expo Web까지만 닫고, 본 항목은 배포 준비 스토리/체크리스트가 능동 회수(deferred-work는 자동 스케줄러 아님). [[deployment-first-time-checklist]] [[defer-human-verification-flag-explicitly]] [source: correct-course 2026-06-20]

## Deferred from: code review of 9-4-모바일-시각-패리티-정정.md (2026-06-20)

차단성 위반 0(AC1~AC6 충족·Expo Web 한정). **독립 게이트 재실행 클린**(mobile `check-types`/`lint`·web `check-types` 전부 EXIT 0) + 핵심 항목 코드 직접검증(스플래시 z순서=z1000 최상위·CORS `:3002` 잔존 0·E2E 주입 게이팅 정상·URL 화이트리스트 웹동일·to12h 산수 정확). 아래는 전부 Low — pre-existing / 9.4 범위 밖 / 네이티브 재검증 결합이라 즉시 조치 불요. 상당수는 위 "⚠️ 배포 전 사람-확인-필수: 모바일 시각 패리티 실기기 재검증"과 **같은 패스로 회수**.

- **챗봇 FAB `bottom:96` 고정(insets.bottom 미반영)** [apps/mobile/src/components/ChatbotFabSlot.tsx:60] — 9.1 스텁 위치 보존(9.4 미변경). 제스처 네비 기기는 탭바+insets가 96px를 넘겨 FAB가 탭바에 묻히거나, insets 0 기기는 과도하게 떠 보일 수 있다. Expo Web에선 정상. 회수: 네이티브 재검증 시 `insets.bottom` 기반 동적 오프셋. [source: edge]
- **provider 리스트(예약자현황·후기) 하단 패딩 < NativeTabs 탭바+insets 가능** [apps/mobile/src/features/provider/ProviderReservations.tsx:213 · ProviderReviews.tsx:196] — FlatList `contentContainerStyle.paddingBottom=Spacing[6]` 고정이 제스처 네비 탭바 높이보다 작으면 마지막 카드의 "예약 거부"/"답글 등록" 버튼이 탭바 뒤로 가릴 수 있다(룸폼은 AC3③로 Spacing[16] 확보했으나 리스트는 미반영). pre-existing. 회수: 네이티브 재검증 시 `insets.bottom`/탭바 높이 반영. [source: edge]
- **익일 마감 영업시간(예 22:00~02:00) 등록 불가** [apps/mobile/src/features/provider/RoomForm.tsx:210] — `close_time <= open_time` 문자열 비교가 자정 넘김 영업을 항상 거부("종료가 시작보다 늦어야") → 심야 운영 스터디룸 등록 불가. 검증 로직은 9.4가 명시적으로 불변(범위 밖)·웹도 동일 제약 추정. 회수: 익일 영업 허용 정책 결정 시 웹·모바일 공통 변경. [source: edge]
- **비격자 보유 시간값(예 09:15) ComboSelect 표시 폴백 미확인** [apps/mobile/src/features/provider/RoomForm.tsx:53-61] — `TIME_OPTIONS`는 30분 그리드(48개)라 그리드 밖 레거시 저장값은 옵션 매칭 실패 → ComboSelect 표시가 placeholder/원시값으로 떨어질 수 있다(저장값은 보존). 9.4의 12h 라벨화는 매칭 옵션에만 적용(기존 동작). 실데이터는 이 폼이 30분 그리드로 저장하므로 도달 드묾. 회수: ComboSelect 미매칭 value `to12h(value)` 폴백 표시. [source: edge]
- **챗봇 모바일 절대URL `new URL` polyfill 의존 + 자동스크롤 단일 setTimeout** [apps/mobile/src/features/chatbot/ChatbotPanel.tsx:312 · 83] — `toInternalPath`가 절대 URL에 `new URL` 사용 → Hermes URL 미/부분 구현이면 throw→catch→null(절대URL 룸링크 라벨 평문화). 단 정당한 내부 링크는 상대경로라 미도달·절대URL 억제는 의도(오픈리다이렉트 안전). 또 자동스크롤이 80ms 단일 setTimeout(웹은 rAF+100ms 이중)이라 긴 대화 재수화 직후 시트 펼침 전 1회 스크롤이면 하단 고정 실패 가능. 둘 다 Expo Web 무영향. 회수: 네이티브 재검증 시 RN URL polyfill 확인 + onContentSizeChange 기반 스크롤. [source: edge]
- **provider 콜드부팅 시 탭바 booker→provider FOUC** [apps/mobile/src/components/app-tabs.tsx:13 · app-tabs.web.tsx:25] — `session?.role==='provider'`가 세션 복원 전 undefined→false라, provider 콜드 부팅 시 booker 탭이 잠깐 보였다가 provider 탭으로 스왑된다(웹 AppNav도 동형). 9.4 이전 역할스왑 패턴(9.4는 순서만 정정). 회수: 세션 로딩 중 중립/직전 role 유지(웹·모바일 공통). [source: edge]

## Deferred from: code review of 9-2-모바일-예약-예약후경험.md (2026-06-19)

차단성 위반 0(8 AC·12 제약·스코프 경계 충족). 아래는 전부 Low — pre-existing / 웹 verbatim 미러 / 서버 계약 의존이라 즉시 조치 불요.

- **배너 React key가 `type:reservation_id`에만 의존(`notification.id` 미사용)** [apps/mobile/src/components/InAppBannerSlot.tsx:46] — 서버가 동일 (type, reservation_id) 통지 2건을 반환하면 React key 중복 경고 + 한 건만 렌더된다. status_change는 행 id가 있으나 key에 미사용 → 유일성을 (type, reservation_id) 가정에만 의존. 계약상 "종류당 예약별 1건"이 지켜지면 미도달. 회수: 하드닝 시 `${type}:${reservation_id}:${id ?? 'reminder'}` 복합 key. [source: blind+edge]
- **NotificationBanner 닫기 dead 버튼(비-reminder + `id=null` 조합 시 무동작)** [apps/mobile/src/features/notifications/NotificationBanner.tsx:87-95] — `handleDismiss`가 `isReminder`면 reservation_id로, 아니면 `notification.id` 있을 때만 dismiss한다. 비-reminder인데 `id=null`이면 닫기 버튼이 아무 동작도 안 한다(영구 표시). 서버 계약상 reminder만 `id=null`이라 정상경로 미도달. 회수: 미지 type/누락 id 방어 카피 또는 무력 버튼 숨김. [source: edge]
- **예약현황 빈 첫 페이지 + `hasNextPage` 시 "아직 예약이 없어요" dead-end** [apps/mobile/src/features/reservation/ReservationList.tsx:153] — 첫 페이지가 `{items:[], next_cursor:"x"}`면 `data.length===0`→빈 카피(InfoCard, onEndReached 없음)라 다음 페이지를 영영 못 불러온다. 서버가 빈 페이지+커서를 반환할 때만·웹 `ReservationList` 동형. 회수: 서버 페이징 계약 확정 또는 `hasNextPage` 시 자동 fetch. [source: edge]
- **`ReservationPanel` `today` 마운트 1회 고정 → 자정 롤오버 stale** [apps/mobile/src/features/reservation/ReservationPanel.tsx:43] — `useState(() => kstToday())`라 패널을 자정 직전에 열어 자정을 넘기면 `today`가 어제로 고정돼 어제 칸이 선택 하한으로 남고 어제 슬롯 선택을 시도할 수 있다. 웹 동형 패턴·서버 past 슬롯 차단이 최종 방어. 회수: 시간 규약 하드닝 시 포커스/타이머 기반 `today` 재계산(웹과 공통). [source: edge]
- **후기 텍스트 이모지/서로게이트 페어 카운트·`slice` 절단** [apps/mobile/src/features/reservation/ReviewForm.tsx:80,90] — `text.length`/`v.slice(0,500)`가 UTF-16 코드유닛 기준이라 4바이트 이모지는 2로 세고 250개에서 잘리며 서로게이트 페어 중간 절단으로 깨진 문자(`�`) 발생 가능. 백엔드 길이 기준이 코드포인트면 경계에서 422 가능. **웹 `ReviewForm`과 verbatim 동일** — 웹-모바일 동시 처리 대상. 회수: codePoint 기반 카운트/절단으로 웹·모바일 함께 교체. [[web-mobile-parity-on-changes]] [source: edge]
- **미니지도 `lat`/`lng` raw 보간 + `originWhitelist={['*']}` + origin 하드코딩** [apps/mobile/src/features/detail/RoomLocationMap.tsx:57,110,29] — 좌표를 HTML 템플릿에 문자열 보간(`name`은 `JSON.stringify` 이스케이프하나 lat/lng는 raw)·`originWhitelist={['*']}`로 임의 origin 스크립트 로드 허용·`KAKAO_WEBVIEW_ORIGIN='http://localhost:3000'` 하드코딩. 좌표는 number 계약이라 즉각 위험은 낮으나 NaN/Infinity·타입 우회 시 표면. **9.1 상속**(아래 9.1 WebView origin·SDK 워치독 항목과 동류). 회수: 네이티브 하드닝 시 origin env화(`EXPO_PUBLIC_KAKAO_WEBVIEW_ORIGIN`)·좌표 `Number.isFinite` 가드. [source: blind+edge]

## Deferred from: code review of 9-1-모바일-인증-탐색-검색.md (2026-06-19)

- **WebView 카카오 origin 하드코딩 `'http://localhost:3000'`** [apps/mobile/src/features/map/MapWebView.tsx:29] — `KAKAO_WEBVIEW_ORIGIN`이 env가 아닌 하드코딩 상수. 카카오 JS SDK는 이 baseUrl을 콘솔 등록 도메인과 대조하므로, 운영 네이티브 빌드에서 실제 등록 도메인과 불일치 시 지도가 AC5 graceful degrade("지도를 못 불러왔어요")로 떨어진다. **스토리 Dev Notes가 dev 단계 localhost:3000을 명시 허용**했고 Expo Web은 react-native-webview 미지원이라 9.1에선 graceful degrade 경로만 검증 가능(네이티브 미검증·Completion Notes도 동일 인정). 회수: 9.2/9.3 네이티브 검증 시 origin을 env(예: `EXPO_PUBLIC_KAKAO_WEBVIEW_ORIGIN`)로 분기 + 카카오 콘솔 도메인 등록. [source: blind+edge+auditor]
- **WebView 카카오 SDK 로드 워치독(타임아웃) 부재 → 영구 'loading' 고착 가능** [apps/mobile/src/features/map/MapWebView.tsx:148-161 / mapHtml.ts:95-109] — `<script>`가 200으로 떴으나 `window.kakao`가 비-SDK 응답(도메인 거부 키·프록시 오류 페이지)이거나 스톨이면, `init()`이 `error` post에 도달하지 못하고 `onError`/`onHttpError`도 발화하지 않아 `mapStatus`가 'loading'에 영구 고착될 수 있다(타임아웃 가드 없음). 웹 `kakao-map.ts`는 load 이벤트에서 `window.kakao?.maps` 부재를 명시 reject. 회수: 네이티브 하드닝 시 로드 타임아웃 워치독 추가(N초 내 ready/error 미수신 시 error 전이). [source: edge]
- **RoomSheet 닫힘 애니메이션 중 탭 시 직전 룸으로 동작** [apps/mobile/src/features/map/RoomSheet.tsx:64,123,219] — 닫힘 중 `roomId=''`이면 `shownRoomId=lastRoom.roomId`(직전 룸 유지)로 폴백한다. 그 좁은 윈도우에 "상세 보기"/하트를 누르면 이미 해제한 직전 룸으로 라우팅/토글된다(FavoriteButton의 `!roomId` 방어는 비어있지 않은 shownRoomId라 무력). narrow timing·경미. 회수: 하드닝 시 `!open`(닫히는 중)에 액션 비활성. [source: blind]
- **ExploreView radius 진입 `queueMicrotask(geoLocate)` 취소 부재 → 지연 위치 프롬프트** [apps/mobile/src/features/list/ExploreView.tsx:68-72] — radius 모드 진입 시 마이크로태스크로 `geoLocate()`를 예약하나 effect cleanup에서 취소하지 않는다. 검색방식(지역↔내 반경)을 틱 내 빠르게 연타하면 이미 떠난 모드인데 측정/프롬프트가 뒤늦게 뜰 수 있다. `geoLocate`는 idempotent(locatingRef)·prompt에서만 요청이라 경미. 회수: effect cleanup에서 취소 플래그. [source: blind]
- **[웹 패리티 forward note] 웹 SignupView 도 역할 토글 시 `register.reset()` 미호출** [apps/web/src/features/auth/SignupView.tsx:83,89] — 모바일 P2(역할 전환 시 `selectRole`로 register 에러 리셋)와 동일한 stale-error 잠재 이슈가 웹에 그대로 존재했다(예약자 가입 실패 후 제공자 전환 시 직전 register 에러 카피가 잠깐 남음). pre-existing·Low. [[web-mobile-parity-on-changes]] [source: edge+직접확인] ✅ **회수 완료(2026-06-19 KTH 승인):** 웹에 동일 `selectRole`(setRole+setLocalError(null)+register.reset()) 핸들러 적용 + 회귀 테스트 신설(`SignupView.test.tsx` "역할 전환 시 직전 가입 에러 카피가 사라진다"). 웹 게이트 typecheck 0·lint 0·vitest 391/391 통과.

## Deferred from: code review of 8-3-예약-임의-취소.md (2026-06-18)

- **통지 add-only(`stage_status_change_notification`) IntegrityError 비처리 — 데이터 이상 시 전이·슬롯 DELETE 롤백** [apps/api/app/notifications/service.py:76-113] — 8.3은 통지 원자화(AC2·deferred L42 회수)를 위해 `create_notification`의 `IntegrityError` 멱등 변환을 일부러 버린 add-only 프리미티브를 도입, winner 전이 commit과 단일 트랜잭션으로 묶는다. **정상 상태머신에선 안전**: 통지는 winner(`rowcount==1`)일 때만 staged되고 `confirmed→종료`는 단 한 번(종료=흡수·fast-path 재진입 차단)이라 `(user, reservation, STATUS_CHANGE)` 통지는 트랜잭션당 최대 1 INSERT → `uq_notifications_user_reservation_type` 충돌 도달 불가(코드 docstring·신규 테스트가 winner-only·멱등 no-op 통지 0을 핀). **잔여 위험(저확률·데이터 이상 전제):** 선행 잔존 status_change 행(이전 버전 비원자 통지·수동 개입 등)이 같은 키로 이미 있는데 예약이 어떤 이유로 confirmed로 잔존하면, winner 통지 INSERT가 `session.commit()`에서 IntegrityError → 전이·슬롯 DELETE까지 전부 롤백·미캐치 시 500 + 슬롯 미재활성(force-cancel/reject 실패). 의도된 트레이드오프(원자성 우선)지만 graceful degradation 부재. **회수:** E8 하드닝 또는 통합 게이트 도입 시 add-only에 방어심층(예: pre-stage 존재 검사 또는 commit IntegrityError를 안전 신호로 변환) 검토. [source: blind+edge]

- **비활성 actor의 access 토큰 ≤15분 잔존(즉시 무효화 부재)** [apps/api/app/core/security.py:180-200] — `deactivate_account`가 `users.is_active=false`로 전이하나 `get_current_principal`은 JWT 클레임만으로 principal을 구성(DB 미접근). `is_active` 재확인은 로그인(`authenticate_user`)·refresh(`rotate_token_pair:131`) 두 곳뿐이라, 이미 유효한 access 토큰(15분)을 든 비활성 booker/provider는 만료까지 모든 인증 엔드포인트를 계속 호출 가능(예약 생성/취소/거절 등). **결함 아님 — AC1이 "최종 강제 ≤15min·stateless 핫패스 불변"을 명시 수용**하고 즉시취소는 architecture가 E8로 deferred. FE 확인 카피("이 계정의 로그인이 차단됩니다")는 정확(로그인·refresh는 즉시 차단, 발급된 access 토큰만 만료까지 유효)이라 과약속 아님. Edge Hunter가 Critical 플래그했으나 스펙 대조 결과 설계대로. **회수:** E8 즉시 토큰 무효화(per-request `is_active` 조회 또는 거부목록/짧은 토큰 TTL) 결정 시. [[deployment-first-time-checklist]] [source: edge]
- **AC3 캐스케이드→즐겨찾기 '비활성' 라벨 end-to-end 회귀 테스트 부재** [apps/api/tests/admin/test_service.py:186-199] — AC3는 "캐스케이드가 즐겨찾기 라벨 경로를 올바르게 트리거함을 회귀 테스트로 실증"을 요구하나, 실제 충족은 **조합 논증**이다: (1) `test_deactivate_provider_cascades_rooms`가 캐스케이드 후 룸 `is_active=False` 단언 + (2) 기존 favorites 테스트(Story 3.7 `tests/favorites/test_service.py:222`)가 비활성 룸→`is_active=False`·`remaining_slots=0` 라벨 단언. provider 비활성→룸 캐스케이드→favorite 라벨까지 잇는 단일 통합 테스트는 없다. **dev가 "1.4 통합 대신 단위 갈음"을 의식적 선택·문서화**(Completion Notes)했고 FakeSession이 도메인 경계를 못 넘어 합리적 트레이드오프. 8.2 신규 favorites 테스트는 없음(기존 3.7 그린 의존). **회수:** 라이브/통합 게이트(TEST_DATABASE_URL) 도입 시 캐스케이드→favorites 라벨 1-shot 통합 테스트 보강. [source: auditor]
- **User.role enum DB 검증 부재 + 캐스케이드 `target.role == "provider"` exact match** [apps/api/app/admin/service.py:97] — 룸 캐스케이드는 `role`이 정확히 문자열 `"provider"`일 때만 발화한다. `User.role`은 1.7에서 가입 시 확정되는 free-text(DB CHECK/enum 강제 없음)라, 손상/예상밖 값(미래 role·대문자 `"Provider"`·룸을 소유한 비-provider)이면 사용자만 비활성되고 **활성 룸이 orphan으로 공개 노출**(map/search/regions 전부 `Room.is_active`만 게이팅)될 수 있다. **pre-existing**(role 자유문자열은 8.2가 만든 것 아님)이고 정상 경로 미도달(booker는 `uq_rooms_provider_id`·FK상 룸 미소유). **회수:** `User.role` enum DB 강제, 또는 캐스케이드를 "룸 소유 && 비-admin"로 일반화. [source: edge]
- **캐스케이드로 룸 비활성화 시 확정 미래 예약 보유 booker 미통지** [apps/api/app/admin/service.py:97-102] — provider 비활성으로 룸이 `is_active=false`가 되면 그 룸에 확정 미래 예약을 가진 예약자는 예약이 `confirmed`·슬롯 점유 그대로 보존되나(AC2 의도), 닫힌/비활성 공간에 대한 **어떤 통지도 받지 않는다**. AC2가 기존 예약 보존(reservations/slots 미터치)을 명시하고 예약 취소·슬롯 재활성·통지를 **8.3(FR-32)** 로 deferred 했으므로 의도된 스코프(코드 결함 아님·product/UX gap). **회수:** 8.3 임의취소가 동일 통지 배선을 추가할 때, 비활성 캐스케이드가 영향받는 booker에게 status_change 통지를 보낼지 함께 결정. [source: edge]

## Deferred from: code review of 8-1-관리자-웹-셸-시드-관리자-접근.md (2026-06-18)

- **CSRF — 쿠키 인증 cross-origin(`credentials:"include"`)** [apps/admin/src/lib/api-client.ts:15] — admin 웹(3001)→API(8000) cross-origin에서 httpOnly 쿠키를 `credentials:"include"`로 동봉. 클라가 CSRF 토큰을 안 보냄. **단 쿠키 SameSite=lax(1.8 desknow_access)라 cross-site POST(login/logout/미래 mutation)에 쿠키 미전송 → CSRF 상당 완화.** 1.8 인증 설계 영역이고 production CSRF·Secure 쿠키·도메인 화이트리스트는 배포 스토리 스코프. 회수: 배포 하드닝에서 서버측 CSRF/origin 검증·SameSite 정책 확정 시 함께. [source: blind]
- **`useSession` 401→null을 staleTime 5분 캐시** [apps/admin/src/features/auth/useSession.ts:31] — `authMe` 401을 `null`(미로그인)로 정규화하되 `staleTime: 5*60_000`이라 음성 인증 결과를 5분 fresh 캐시. 다른 탭/경로에서 세션이 생겨도(쿠키 리프레시 등) 이 인스턴스가 5분간 로그아웃 상태로 보일 수 있음(로그인 mutation은 invalidate하나 타 경로는 미반영). **web `useSession` 미러 패턴 상속(동형 유지가 의도).** 회수: web과 함께 음성 인증 캐시 정책 재검토 시(staleTime 단축 또는 refetchOnWindowFocus). [source: blind]
- **`seed_admin` commit `IntegrityError` 미처리(동시 실행 TOCTOU)** [apps/api/scripts/seed_admin.py:42-53] — `select(...).first()`(미존재 판정)→`add`/`commit` 사이 비원자. 동시 2회 실행 시 둘 다 None 보고 후 두 번째 commit이 `uq_users_email` 위반 → 미캐치 traceback(exit≠0). UNIQUE 제약이 중복 행은 막아 **데이터 무결성은 안전**하나 스크립트 docstring의 "멱등·중복 없음" 약속이 동시성에서 깨짐. 1회 수동 부트스트랩 스크립트라 현실성 낮음. 회수: commit `try/except IntegrityError`로 멱등 재조회 또는 upsert. [source: blind+edge]
- **NO_VCS라 `apps/web`·`apps/mobile` 무변경 독립 검증 불가** [프로세스 한계] — `baseline_commit: NO_VCS`·git 저장소 아님 → diff로 web/mobile 무변경을 증명할 수단 없음. 음성 grep(`apps/web/src`에 `8.1|admin|Story 8` 누출 0)으로 방증만 확보. Dev 기록 단언에 의존. **코드 결함 아님(검증 메커니즘 부재).** 회수: VCS 도입 시 자연 해소. [source: auditor]

## Deferred from: code review of 7-7-범위-밖-질문-거절.md (2026-06-18)

- **REJECT_SIGNALS 거짓음성 — 패러프레이즈 거절 누락** [apps/api/tests/chatbot/test_golden_set.py:60-66] — `REJECT_SIGNALS`(도와드리기 어려/힘들·답변드리기 어려·도와드릴 수 있는 주제·스터디룸 찾기나 예약)가 프롬프트의 고정 거절 카피는 잡으나, LLM이 낼 수 있는 정당한 패러프레이즈("도움을 드리기 어렵네요"≠"도와드리기"·"그건 제 역할이 아니에요"·"답하기 어려워요"≠"답변드리기")는 미스 → `_has_reject_signal`=False → **라이브 골든셋 flaky 실패**. Dev Notes의 "자구 완전 일치 강제 안 함(강건 부분문자열)" 약속 대비 시그널셋이 실제로는 협소. **7.7 신규지만 라이브 미실행(전용 `TEST_DATABASE_URL` 부재)이라 현재 무영향.** 회수: 라이브 게이트 opt-in 실행(`TEST_DATABASE_URL`+`OPENAI_API_KEY`) 시 거절 시그널 보강(UNKNOWN_SIGNALS 정밀화와 함께). [[chatbot-golden-set-destructive-teardown]]
- **UNKNOWN_SIGNALS 과광폭 "모르"/"찾지 못" 교차분기 오판** [apps/api/tests/chatbot/test_golden_set.py:40-49] — pre-existing(7.5 도입·7.7 미변경). "모르"는 너무 짧은 토막이라 answer 응답의 "모르는 분들도 쉽게…" 같은 무관 문맥이 `_has_unknown_signal`=True→answer arm false-red("근거 있는데 모름 안내"). "찾지 못"은 §예약 검색 정상 카피("그 지역은 못 찾았어요")와도 겹친다. arm들이 분기별 어휘 비중첩을 가정하나 프롬프트가 보장하지 않음. **7.7 결함 아님.** 회수: UNKNOWN_SIGNALS를 더 긴 식별 가능 구로 정밀화(answer/unknown/out_of_scope 시그널 충돌 제거).
- **recursion_limit=8 — 범위 밖에서 모델이 툴 오발화 시 루프→500, 폴백 카피 없음** [apps/api/app/chatbot/service.py:53] — pre-existing 그래프 거동(7.7 미변경). 7.7의 전제는 "모델이 out_of_scope에 어떤 툴도 안 부름"인데, 실모델이 오분류해 `search_service_docs`를 호출하면 `NO_RELEVANT_DOCS` 반환→모델 재호출 반복→`_RECURSION_LIMIT=8` 초과 시 `GraphRecursionError`→raw 500. 이 실패모드(거절 가드 무력화)에 폴백 카피도 테스트도 없고, 결정적 테스트는 툴을 못 내는 `_NoToolFakeModel`이라 이 분기에 도달 불가. **7.7이 유발한 결함은 아니나 7.7 전제가 깨질 때 표면화.** 회수: E7 하드닝에서 recursion 상한 도달 시 안전 폴백 응답 + 가드. [[chatbot-rag-tool-calling-react]]
- **answer arm 그라운딩 미강제 + 키워드 부분문자열 false-green** [apps/api/tests/chatbot/test_golden_set.py:128-137] — pre-existing(7.5 answer arm·7.7 미변경). `expect_keywords` 존재 여부만 보고 `search_service_docs` 실제 호출·근거 일치를 단언하지 않음 → 모델이 파라메트릭 기억으로 "입실 코드"를 RAG 없이 지어내도 통과(프롬프트의 "툴이 돌려준 근거 안에서만" 위반을 못 잡음). 키워드도 부분문자열이라 엉뚱한 문맥의 "6시간"이 통과 가능. **7.7 결함 아님.** 회수: 그라운딩 단언 강화(툴콜 introspection)로 a 분기도 함께.
- **_REJECT_COPY 리터럴 중복(드리프트) + 동등단언 tautology** [apps/api/tests/chatbot/test_rag_graph.py:270] — 거절 카피가 `prompts.py` 내 명명 상수가 아니라 거대 시스템 프롬프트 문자열에 임베드돼 있어 테스트가 임포트 불가 → 테스트가 동일 카피를 리터럴로 중복 선언. `_NoToolFakeModel`이 받은 문자열을 그대로 반환하므로 `assert reply==_REJECT_COPY`는 pass-through 항등식(그래프가 모델 content를 verbatim 전달함만 증명)이고 프롬프트 카피가 바뀌어도 테스트는 못 잡는다(조용한 드리프트). **저심각·배선 검증 목적상 무해하나 회귀 가드로는 약함.** 회수: 거절 카피를 `prompts.py` 명명 상수로 추출 → 프롬프트와 테스트가 단일 출처 공유.

## Deferred from: code review of 7-5-서비스-안내-문서-rag.md (2026-06-18)

- **ToolNode 툴 예외 처리 정책 미명시 — 에러 문자열이 "근거"로 모델 재투입** [apps/api/app/chatbot/graph.py:86] — `ToolNode(_TOOLS)`는 기본 `handle_tool_errors=True`라 `search_service_docs` 내부 예외(DB 단절·`get_engine` 설정 오류·임베딩 실패)를 삼켜 에러 문자열을 ToolMessage로 모델에 재투입한다 → 모델이 에러 문자열을 그라운딩 근거로 오인해 엉뚱한 답변/환각 가능. **7.5 신규지만 저확률**(운영 DB·임베딩 정상 전제). 회수: 명시 `handle_tool_errors` 정책(에러 시 `NO_RELEVANT_DOCS`류 안전 신호 반환) 또는 툴 내부 try/except로 DB 오류를 모름 신호로 변환. E7 하드닝 또는 7.6 예약툴 추가 시 함께 정리. ✅ **회수 완료(Story 8.4, 2026-06-18):** `graph.py`의 `ToolNode(_TOOLS, handle_tool_errors=_handle_tool_error)`로 명시 정책을 둠 — 툴 내부 예외를 내부 사유 미노출 + "도구 오류임을 알리는 통제된 고정 한국어 메시지"로 치환해 모델이 에러 문자열을 그라운딩 근거로 오인·환각하지 않게 한다(내부 예외는 로그로만). 동작 테스트(`test_rag_graph.py::test_tool_error_replaced_with_controlled_message`)로 치환·내부 사유 미노출 검증.
- **세션 종료 후 ORM 컬럼 직렬화 경로가 라이브에서 미검증** [apps/api/app/chatbot/tools.py:64-71] — `with Session(get_engine())` 블록 종료 *후* `chunk.source_path`/`chunk.content` 접근(`search_documents`가 ORM 객체 반환). **리뷰가 SQLAlchemy 2.0.50+SQLModel로 동일 패턴 재현해 현재 안전 확인**(commit 없어 미-expire·이미 로드된 컬럼·lazy 관계 없음 → DetachedInstanceError 미발생). Blind Hunter의 Critical 주장 기각. **단** 이 경로를 실 Postgres+Vector로 실행하는 골든셋(`test_golden_set.py`)이 기본 skip(`TEST_DATABASE_URL` 부재)이라 운영 백엔드에서 미검증·회귀 가드 부재. 회수: 라이브 골든셋 opt-in 실행(TEST_DATABASE_URL+OPENAI_API_KEY) 시 자연 커버 — 별도 픽스가 아니라 CI/로컬에서 라이브 게이트 1회 실행 권장.

## Deferred from: code review of 7-4-응답-스트리밍.md (2026-06-18)

- **백엔드 롤백이 `native_exceptions`만 커버 → 비-LLM 예외/`CancelledError` 시 고아 HumanMessage** [apps/api/app/chatbot/service.py:163-170] — `stream_message`의 `except native_exceptions`는 LLM 업스트림 실패만 좁혀 `aupdate_state(RemoveMessage)` 롤백 + 정규화한다. 그 외 예외(코드 버그·`asyncio.CancelledError`·checkpointer 오류)는 except를 건너뛰어 입력 HumanMessage 롤백 없이 전파된다. **7.4 신규 결함 아님 — 동기 `send_message`(7.3)와 동일 트리거 범위로 의도적 미러**(코드 버그를 502로 위장하지 않는 설계). 토큰 yield 중 native 실패는 롤백 동작하며 `test_failed_turn_mid_stream_rolled_back`이 `transcript==[]`로 고정(부분 영속 없음 — 노드 출력 원자 커밋). 회수: 롤백 트리거 전면 확장은 7.3과 공유하는 별도 결정(`send_message`도 함께).
- **모든 시작 전 실패(401·검증·5xx)가 단일 `STREAM_FAILED` 카피로 뭉개짐** [apps/web/src/features/chatbot/streamMessage.ts:90-98] — non-OK 응답·네트워크 단절을 전부 "스트림을 시작할 수 없습니다." 고정 카피로 강등해 401/검증/서버오류 구분 불가. **MVP 허용** — AC4는 graceful degrade(막다른 화면 금지)만 요구하고 그건 충족. device_id 422는 Pydantic 1차 검증이 막아 사실상 도달 불가. 회수: 상태코드별 진단 카피 분기(향상). [[terminology-network-disconnect-not-offline]]
- **`_content_to_str`가 list형 content를 `str()` repr로 변환** [apps/api/app/chatbot/service.py:68-70] — 멀티모달/구조화 청크 시 `"[{'type':'text',...}]"` repr이 delta로 노출될 수 있다. **7.4 신규 결함 아님 — 7.3 `send_message`/`get_transcript`와 공유하는 헬퍼.** 현 텍스트 LLM 경로는 항상 str 반환이라 무해. 회수: 멀티모달 도입 시 text 파트 추출/비문자열 skip로 일관 처리.

## Forward note for: 7-3-플로팅-챗봇-진입-세션-대화-유지 (2026-06-17)

- **모바일 챗봇(FAB 실동작·대화 패널·세션) = "모바일 dev-build 푸시" 버킷** [apps/mobile/src/components/.../ChatbotFabSlot(placeholder 유지) · apps/mobile/src/features/chatbot(미존재)] — 7.3은 **웹만** 챗봇 진입/대화를 구현했다(BE `app/chatbot/{graph,prompts,service,schemas,router}.py` 풀스택 + 웹 `features/chatbot/{deviceId,useChatbot,ChatbotPanel}` + `ChatbotFabSlot` 실동작 교체). 모바일은 **인증 스택 부재**(`useSession` 없음·`QueryClientProvider` 없음·Bearer/secure-store 미배선)이고 화면이 **Epic 4 placeholder**라 챗봇 대화 세션의 전제(로그인 + 화면 진입점)가 없다 → 본 스토리에서 동등 적용 불가(명시 라우팅). 본 스토리 `apps/mobile`·`apps/admin` 변경 0(모바일 `ChatbotFabSlot` placeholder 그대로 둠). **E7=챗봇 에픽이지 모바일 버킷이 아님**(혼동 주의 — 메모 경고). **BE는 웹·모바일 공용**(`POST/GET /api/v1/chatbot/messages`·`DELETE /api/v1/chatbot/session` 재사용 — 모바일 재구현 불요). **회수:** 모바일 인증 스택(`useSession`·`QueryClientProvider`·secure-store Bearer) + 실 화면(Epic 4 모바일) 선행 후 "모바일 dev-build 푸시" 버킷에서 RN 챗봇 UI 구현. 웹 `deviceId`(localStorage→RN secure-store/AsyncStorage 대체)·`useChatbot`(TanStack Query·동일 SDK)·`ChatbotPanel`(RN 모달 a11y — review-accessibility.md)은 모바일 재구현 참조 후보. [[web-mobile-parity-on-changes]]

## Deferred from: code review of 6-2-예약-거절.md (2026-06-17)

- **거절 commit 후 통지 생성 비원자 — 통지 실패 시 500 + 재시도 영구 손실** [apps/api/app/reservations/router.py:384-398 · notifications/service.py:57-71] — `reject_reservation_for_provider`가 `confirmed→rejected` 전이를 commit한 *뒤* 라우터가 `create_notification`(자체 commit)을 호출한다(두 트랜잭션, 비원자). `create_notification`이 raise하면(희박 동시삽입으로 `uq` 위반 후 재조회 `existing is None`→re-raise 500, 또는 무관 DB 오류) **거절·슬롯 재활성은 이미 영속인데 응답은 500**이 되고, 제공자가 재시도하면 예약이 이미 종료 상태(`rejected`)라 `was_confirmed`가 거짓 → 통지 분기를 타지 못해 **예약자 status_change 통지가 영구 미발송**(거절 상태는 정상 잔존이라 자가치유 아님 — 5.3 배너만 누락). **6.2 신규 결함 아님 — 스토리 deferred 후보가 `create_notification 동시삽입 None→500`(L278)을 이미 명시**, 다만 "재시도 시 was_confirmed 재게이팅으로 영구 손실"은 본 리뷰가 추가한 추론. 극희박(거절 commit 직후 통지 INSERT 실패 + 동시경합)이라 defer 유지. 회수: outbox 패턴 또는 통지를 거절과 동일 트랜잭션에 편입, 혹은 `_transition_to_terminal`의 실제 rowcount(=전이 수행 신호)를 라우터로 반환해 `was_confirmed` 사전캡처를 대체(재시도 안전). 8.3(임의취소)이 동일 통지 배선을 추가할 때 함께 정리 후보.

## Deferred from: code review of 6-1-예약현황-조회-제공자.md (2026-06-17)

- **제공자 예약현황 페이지네이션 부재(무한 결과셋)** [apps/api/app/reservations/service.py:344-349 · router.py:266-319] — `list_reservations_for_rooms`는 소유 룸의 예약을 LIMIT/cursor 없이 상태 무관 전량 반환한다(confirmed+cancelled+rejected 히스토리가 영구 누적). MVP는 제공자당 룸 1개(`uq_rooms_provider_id`)지만 그건 **룸** 상한이지 **룸당 예약 수** 상한이 아니라, 인기 룸은 종료 예약이 무한 증가한다. **6.1 신규 결함 아님 — 스토리가 이미 deferred 후보로 명시(`6-1...md` deferred #5)하고 favorites·4.8 `list_booker_reservations`도 동일 미페이지네이션.** 회수: 예약 多 시 페이지네이션/날짜 윈도우/cursor 도입(4.8·favorites와 일관 적용 권장).
- **`created_at desc` 동률 시 비결정 정렬(보조 정렬키 부재)** [apps/api/app/reservations/service.py:347] — `order_by(created_at desc)`가 유일 정렬 키라, `created_at`(µs UTC) 동일한 행들(배치/동일 인스턴트 삽입)은 Postgres에서 run-to-run 비결정 순서로 반환된다(보조 `id` tiebreaker 없음). **6.1 신규 결함 아님 — `list_booker_reservations`(4.8) 동일 갭 미러.** Fake 세션이 Python 안정 정렬을 써 동률 케이스가 테스트로 안 덮인다. 회수: 결정적 정렬이 필요하면 `.order_by(created_at.desc(), id)` 보조 키를 6.1+4.8에 **일관 적용**(둘 중 하나만 고치면 비대칭).

## Forward note for: 5-5-후기-작성 (2026-06-17)

- **모바일 후기(작성·노출) = "모바일 dev-build 푸시" 버킷** [apps/mobile/src/features/(reservation·detail 미존재)] — 5.5는 **웹만** 후기를 구현했다(BE reviews 도메인 풀스택 + 웹 `ReviewSection`/`StarRating`(룸 상세 노출)·`ReviewForm`/`useCreateReview`(예약현황 작성)). **모바일 예약현황·룸 상세 화면 자체가 Epic 4 미구현 placeholder**라 후기 작성/노출의 진입점(이용 완료 예약 행·룸 상세 3차 섹션)이 없다 → 본 스토리에서 동등 적용 불가(명시 라우팅). 본 스토리 `apps/mobile`·`apps/admin` 변경 0. **E7=챗봇 에픽이지 모바일 버킷이 아님**(혼동 주의). **회수:** 모바일 예약/상세 흐름(Epic 4 모바일 구현) 선행 후 "모바일 dev-build 푸시" 버킷에서 후기 UI 구현. **BE는 재구현 불요**(웹·모바일 공용 엔드포인트 `POST /reservations/{id}/reviews`·`GET /rooms/{id}/reviews` 이미 존재). 웹 `is_reservation_completed`(BE 자격 판정)·`StarRating`·a11y 별점 입력 패턴은 모바일 재구현 참조 후보. [[web-mobile-parity-on-changes]]
- **후기 공개 조회 페이지네이션·레이트리밋(배포 의존)** [apps/api/app/reviews/router.py `GET /rooms/{id}/reviews`] — 룸당 후기를 flat 반환한다(Epic 3 공개 조회 페이지네이션 deferred 흐름과 정합). 후기가 많은 룸의 응답 비대·공개 무인증 엔드포인트 남용 방지는 **배포 스토리**에서 페이지네이션 + 레이트리밋으로 회수. [[deployment-first-time-checklist]]
- **후기 수정/삭제·내 후기 모아보기·작성자 표시 이름** [reviews 도메인 · users 모델] — MVP 스코프 밖(FR-20 경계: 작성 후 불변). 작성자 표시 이름은 `users` 스키마에 표시명 컬럼 추가(회원가입 1.7·마이그레이션·SDK 동반)가 선행돼야 하므로 후속 결정 시 별도 스토리. 현재 후기는 익명(KTH 결정 1).

## Forward note for: 5-4-카카오톡-예약-공유 (2026-06-17)

- **모바일 카카오톡 예약 공유 = "모바일 dev-build 푸시" 버킷** [apps/mobile/src/features/reservation(미존재)] — 5.4는 **웹만** 카카오 공유를 구현했다(`apps/web/src/lib/kakao-share.ts` JS SDK Share 로더 + `KakaoShareButton` + `buildReservationShareText`). 모바일(RN) 공유는 `@react-native-kakao/share` 네이티브 모듈 + **EAS dev build**가 필요하고, 무엇보다 **모바일 예약 화면 자체가 Epic 4 미구현 placeholder**(`apps/mobile/src/app/reservations.tsx`)라 공유의 전제(확정 예약 표면)가 없다 → 본 스토리에서 동등 적용 불가(명시 라우팅). **E7=챗봇 에픽이지 모바일 버킷이 아님**(혼동 주의). **회수:** 모바일 예약 흐름(Epic 4 모바일 구현) 선행 후 "모바일 dev-build 푸시" 버킷에서 `@react-native-kakao/share`+EAS dev build로 구현. 웹 `buildReservationShareText`(프레임워크 무관 순수 함수)는 모바일 재구현 시 참조/승격 후보. [[web-mobile-parity-on-changes]]
- **프로드 도메인 카카오 콘솔 등록(배포 의존)** [카카오 개발자 콘솔 · apps/web] — 공유 링크(`webUrl`)·`Kakao.init`은 카카오 콘솔 [플랫폼 → Web 도메인]에 등록된 도메인에서만 동작한다. 현재 localhost:3000만 등록(1-1 AC7)이라 프로드 도메인은 미등록 → 배포 환경에서 공유 시 AC4 graceful degrade("지금은 공유를 할 수 없어요")로 떨어진다. **회수:** 배포 스토리(E7/E8/배포)가 실제 프로드 도메인을 카카오 콘솔에 등록. [[deployment-first-time-checklist]]

## Deferred from: code review of 5-4-카카오톡-예약-공유.md (2026-06-17)

- **자정 넘는 예약의 시간 범위 표기 가독성** [apps/web/src/features/reservation/share.ts:36-40] — `buildReservationShareText`가 날짜는 `slotStarts[0]`(시작일)만 라벨링하고 시간 범위는 시작/끝을 독립 KST 벽시계로 포맷 → 22:00→01:00(KST) 예약이 "6월 20일 22:00–01:00"으로 표기되어 끝 시각이 시작보다 작아 보이고 다음날 넘어감 표시가 없다. **5.4 결함 아님 — 기존 `reservations.ts` `reservationTimeRangeLabel` 패턴과 동형이며 예약현황 행 표시에도 동일 존재.** 회수: 시간 표기 UX 하드닝 시 "+1일"/넘김 마커 도입(share + reservations 공통 포맷터 승격 후보).
- **`Kakao.Share.sendDefault` 무반응 silent no-op** [apps/web/src/lib/kakao-share.ts:105] — SDK 로드·init·키 실패는 reject→AC4 graceful degrade로 처리되나, 로드/init 성공 후 `sendDefault`가 공유 시트를 못 여는 경우(팝업 차단·SDK 내부 swallow)는 throw 없이 `shareReservation`이 resolve → 사용자가 "공유" 눌렀는데 아무 일도 안 일어나고 실패 안내도 없음. v2 `sendDefault`는 void 반환이라 즉시 await 불가·"한 번에" UX 본질상 전달 보장은 스코프 밖. 회수: Kakao SDK가 결과 콜백/Promise를 제공하면 그때 실패 피드백 배선 검토.
- **maps SDK `<script>`에 SRI `integrity` 부재(share 로더는 적용)** [apps/web/src/lib/kakao-map.ts:26] — share 로더는 `integrity`+`crossOrigin`으로 변조 SDK 차단하나 maps 로더는 SRI 없이 로드 → 두 카카오 로더의 보안 자세 비대칭. **5.4 결함 아님 — 기존 maps 코드(3.2).** 회수: 보안 하드닝 시 maps SDK에도 버전 핀 + SRI 적용(단 maps는 버전 미고정 URL이라 핀 선행 필요).

## Deferred from: code review of 5-3-예약-상태변경-통지-배너.md (2026-06-17)

- **AC5③ 문구와 실제 SDK 재생성 불일치(스펙 문구 보정 권장·코드 결함 아님)** [packages/api-client/src/generated/types.gen.ts · apps/api/app/notifications/schemas.py:34-39] — AC5③/Dev Notes는 "SDK 재생성 불요(드리프트 0 확인만)"라 단언하나, 클래스 docstring이 OpenAPI `description`으로 export되어 Layer A 드리프트 발생→실제 openapi.json+types.gen.ts 재생성됨(타입·필드·엔드포인트 불변, description prose만 변경). Completion Notes·Change Log에 이미 디스클로즈·정합 처리됨. **회수:** 차기 스토리/스펙 정리 시 AC5③ 문구를 "description-only 재생성 예상"으로 보정(향후 docstring 수정도 동일 드리프트 유발 — 패턴으로 인지).

## Deferred from: code review of 5-2-예약-도래-리마인드-배너.md (2026-06-17)

- **옵티미스틱 다중 dismiss stale `previous` 클로버** [apps/web/src/features/notifications/useNotifications.ts:103-127] — 같은 `NOTIFICATIONS_KEY` 캐시를 ~100ms 내 연속 dismiss(리마인드+상태변경 동시)하면 둘째 `onMutate`의 `previous` 스냅샷이 첫째의 옵티미스틱 제거 상태를 캡처해, 첫째 `onError` 롤백이 둘째 제거를 되살릴 수 있다(`cancelQueries`는 in-flight 쿼리만 취소·형제 뮤테이션 비직렬화). `onSettled` 정확 키 invalidate로 최종 수렴(깜빡임 수준). **5.2 결함 아님 — 3.7 favorites(L39)·5.1 dismiss 동일 패턴 재발.** 회수=동일 키 뮤테이션 `scope` 직렬화 또는 함수형 롤백(설계 변경 규모).
- **✅ 회수됨(Story 5.3, 2026-06-17) — 손상 `slot_start` 문자열이 배너 슬롯 전체를 크래시** [apps/web/src/features/notifications/NotificationBanner.tsx] — `slot_start` 파싱 불가 시 `new Date(...)`=Invalid Date → `Intl.DateTimeFormat.format()` `RangeError` throw → render-time 파생이라 `InAppBannerSlot.map()`이 통째로 깨져 정상 통지까지 사라지던 함정. **5.3이 slot_start 렌더를 status_change로 확장하며 의무 회수:** 공유 헬퍼 `formatKstDateTime(slotStart)`가 포맷 전 `Number.isNaN(new Date(slotStart).getTime())` 검사 → 손상이면 `null` 반환 → reminder·status_change **양쪽** 시각 없는 generic 카피 폴백(throw 금지·`room_name` 폴백과 동일 정신). 테스트: status_change/reminder 손상 slot_start → 크래시 없이 generic(NotificationBanner.test.tsx).
- **✅ 회수됨(Story 5.3, 2026-06-17) — 손상/naive `slot_starts[0]` → GET 500** [apps/api/app/notifications/reminders.py, router.py] — `datetime.fromisoformat(reservation.slot_starts[0])`이 손상 비-ISO면 `ValueError` 전파 → GET 500(status_change 통지까지 차단)이던 함정. **5.3이 slot_start 합성을 status_change로 확장하며 의무 회수:** 공유 안전 파서 `earliest_slot_start(reservation)`가 `try/except ValueError` → 손상 시 `logger.warning` + `None` 반환. reminder 도출(`due_reminder_reservations`는 None이면 skip)·라우터 status_change 합성(None 폴백) **양쪽** 동일 가드 경유 → GET 200 유지(부분 손상이 전체 GET을 죽이지 않음). **코드리뷰 2026-06-17 보강:** naive(무-Z·tz 없음) ISO도 비-ISO와 동일 손상 클래스로 통일 — 초기엔 fail-fast(손상≠naive)였으나, naive가 윈도 `_require_aware`/직렬화 `isoformat_utc`에 닿으면 ValueError→GET 500이라 비대칭이 되던 갭을 닫음(`earliest_slot_start`가 naive면 `logger.warning`+None·FE `formatKstDateTime`에 tz 지정자 정규식 가드). 테스트: 손상/naive slot_starts skip/None 폴백·GET 200(test_reminders.py·test_router.py·NotificationBanner.test.tsx).

## Deferred from: code review of 4-9-가용성-집계에-예약-차감-연결.md (2026-06-17)

- **`confirmed_slot_starts` 계열 reader에 status 필터 없음 — DELETE-on-cancel 불변식에만 의존** [apps/api/app/reservations/service.py:309] — 취소/거절 시 `reservation_slots` 행을 DELETE하므로 status 조인 없이도 "활성 점유만" 반환된다는 설계. 미래에 DELETE 없이 종료 상태로 전이하는 경로(어드민 강제취소·만료 배치 잡·직접 status UPDATE)가 추가되면 유령 점유 행이 가용성 차감에 영구 누출되고 복구 수단이 없다(방어심층 부재). 현 앱은 DELETE-on-cancel 단일 경로라 안전. **회수:** 비-DELETE 종료 경로(E6 거절·E8 만료/비활성 캐스케이드 등) 도입 시 reader에 status 조인 또는 FK-cascade로 방어심층 추가.
- **`get_room_slots`/예약 생성에 서버측 30일 호라이즌 미강제** [apps/api/app/rooms/service.py:383] — `_next_available_date`는 전방 탐색 후보 날짜를 30일로 클램프하나, `target_date` 자체의 슬롯 도출·예약 시도에는 호라이즌 검증이 없다. 직접 API 호출자가 far-future(또는 far-past) 날짜로 선택 가능한 슬롯을 받아 호라이즌 밖 예약을 시도할 수 있다(게이팅이 클라 `isSelectableDate`에만 존재). 4.9가 만든 결함은 아님(엔드포인트 선재 동작). **회수:** 서버 측 `target_date` 호라이즌 검증을 `get_room_slots`/`create_reservation`에 추가(이중 방어).
- **30일 전부 만석 시 `next_available_date=None`이 "영업시간 없음"과 동일 막다른 표시** [apps/web/src/features/reservation/ReservationPanel.tsx:162] — 룸이 30일 호라이즌 내내 만석이면 `next_available_date`가 `None`이라 "이 날은 다 찼어요"만 뜨고 제안 버튼이 없어, "예약이 꽉 참(30일 너머 시도)" 상태와 "이 룸은 영업하지 않음" 상태가 동일 UI로 붕괴한다. 문서화된 호라이즌 한계이나 4.9 차감으로 도달 가능성 상승. **회수:** 신선도/호라이즌 UX 하드닝 시 두 상태를 구분하는 카피(예: "이 기간엔 빈자리가 없어요") 도입.

## Deferred from: code review of 4-7-예약-취소.md (2026-06-17)

- **cancel-vs-reject 교차 전이 race(다른 종료 타깃·낙관적 락 부재)** [apps/api/app/reservations/service.py:159-176] — L12 회수는 cancel-vs-**cancel** 무해성(둘째=같은 값 flip + 0행 DELETE no-op)만 증명했다. booker 취소와 provider 거절(E6/6.2)이 동시 진입하면 둘 다 stale `confirmed`를 읽고 `_transition_to_terminal`로 진입해 last-write-wins로 최종 `status`가 `cancelled`/`rejected` 비결정적이 된다(슬롯 재활성은 양쪽 동일해 데이터 손상 없음·"누가 종료시켰나" 히스토리/통지 의미만 흔들림). **현재 미도달**(거절 엔드포인트는 E6 backlog). **회수:** Story 6.2(예약 거절) 구현 시 cancel↔reject 교차 race를 `SELECT FOR UPDATE`/조건부 UPDATE(`WHERE status='confirmed'`)/버전 컬럼 중 하나로 처리하거나 last-write-wins를 명시 수용 + 테스트.
- **confirmed-but-zero-slots 방어 경로의 텔레메트리 부재 + 오해 소지 카피/코드 재사용** [apps/api/app/reservations/service.py:256-261] — confirmed인데 점유 슬롯 0건(불변식 위반·정상 경로 불가)일 때 `CANCEL_WINDOW_PASSED` + "이제 6시간이 안 남아서 취소가 어려워요." 카피로 차단한다. 스펙 Task 2ⓑ가 "방어적 차단 또는 fail-fast"를 허용해 위반은 아니나, 사용자가 데이터 이상을 시간 만료로 오인하고 FE(4.8 `detail.code` 분기)가 실 윈도 만료와 구분 불가하며 불변식 위반이 로그/알림 없이 묻힌다. **회수:** 관측성 하드닝 시 `logger.warning`(불변식 위반) 추가 + 별도 신호(500 또는 전용 코드) 검토.
- **상태머신 전이 가드가 "non-terminal"(명시적 `confirmed` 아님)** [apps/api/app/reservations/service.py:168-171] — `_transition_to_terminal`이 "종료 상태가 아니면 전이"로 동작하지 "출발 상태=confirmed"를 명시하지 않는다. 비-종료 상태가 `confirmed` 하나뿐이라 현재 안전하나, 향후 비-종료 상태(예: `pending`) 추가 시 이 분기를 갱신하지 않으면 조용히 취소·거절될 수 있다. **회수:** 상태머신에 비-종료 상태 추가 시점에 출발 상태 명시 가드 + 회귀 테스트.

## Deferred from: code review of 4-3-날짜-슬롯-가용성-표시.md (2026-06-16)

- **BE/FE 30일 상한 상수 이중 출처** [apps/api/app/rooms/service.py:66, apps/web/src/features/reservation/slots.ts:12] — `_RESERVATION_HORIZON_DAYS=30`(py)와 `RESERVATION_HORIZON_DAYS=30`(ts)가 공유 출처 없이 각자 선언돼 드리프트 가능. 크로스-언어 단일 출처는 codegen/스키마 노출 없이는 비현실적이라 현재 이중 선언 수용. **회수:** 예약 흐름(4.4~4.9)에서 윈도가 변하거나 다른 상한이 추가될 때 OpenAPI 상수 노출 또는 빌드타임 공유로 단일화.
- **단절+웜캐시 시 슬롯 상태 노화** [apps/web/src/features/reservation/ReservationPanel.tsx] — 슬롯 past/available은 서버가 조회 시점 `current`로 고정한 값이라, 오프라인 동안 캐시된 그리드의 "available"이 실제로 지나가도 갱신되지 않는다. 스토리가 이미 "자정 KST 경계 클라/서버 불일치"(deferred-work L189 계열)로 defer 명시. **회수:** 시간 규약 하드닝 후속(클라 측 past 재판정 또는 신선도 TTL 단축).
- **프론트 날짜/시간 순수함수 방어적 입력 검증 부재** [apps/web/src/features/reservation/slots.ts:14-18, :106-113] — `parseYmd`는 비-`YYYY-MM-DD` 입력에 NaN 튜플을, `formatSlotLabel`은 비-ISO 입력에 "Invalid Date" 파생 문자열을 조용히 반환(throw 없음). 입력이 서버 계약 또는 내부 생성이라 라이브 트리거 없음. **회수:** 외부/사용자 입력이 이 함수에 닿는 경로(예: URL 쿼리로 날짜 주입)가 생기면 fail-fast 가드 추가.

## Deferred from: code review of 4-1-reservations-데이터-모델-동시성-제약-상태머신.md (2026-06-16)

- **denormalized `reservation_slots.room_id` ↔ 부모 `reservations.room_id` 정합성 DB 미강제** [apps/api/app/reservations/models.py:156-163, alembic/versions/d8afe1726e81:57-66] — 슬롯의 `room_id`가 그 예약의 `room_id`와 같아야 한다는 불변식이 service 코드(`create_reservation`이 동일 값 복사)에만 있고 DB CHECK/복합 FK로 강제되지 않는다. service가 단일 writer라 현재 무해. **회수:** service를 우회하는 writer(raw SQL·다른 도메인) 도입 시 복합 FK(`reservations(id, room_id)` UNIQUE + 슬롯이 `(reservation_id, room_id)` 참조)로 강제.
- **동시 cancel/reject 낙관적 락 부재(read-then-flip race)** [apps/api/app/reservations/service.py:144-161] — 같은 confirmed 예약에 두 전이 요청이 동시 진입하면 둘 다 종료상태 검사를 통과해 각각 flip+DELETE+commit한다. 둘째 commit은 이미 비워진 슬롯에 무해한 no-op이라 데이터 손상은 없으나 `SELECT FOR UPDATE`/버전 컬럼 가드는 없다. **회수:** Story 4.6(중복 예약 방지·동시성)이 진정한 멀티스레드 동시 검증(SM-4)과 함께 소유.
- ✅ **[Story 4.9 회수 완료]** **`confirmed_slot_starts` on_or_after 경계(`>=`)·윈도잉 테스트 부재** [apps/api/app/reservations/service.py:178-204, tests/integration/test_reservations_migration.py] — zero-occupancy 경로는 통합 테스트가 커버하나(취소 후 `== set()`), `on_or_after` inclusive(`>=` vs `>`) 경계와 상한 윈도잉(무제한 미래 슬롯 반환)이 어느 테스트에서도 단언되지 않는다. **회수:** 4.9가 reader를 카운트 4곳에 `on_or_after=current`로 배선 — `test_aggregate_on_or_after_boundary_reserved_at_now_is_deducted`(now와 정확히 같은 시각 예약 슬롯이 inclusive `>=`로 차감)·`test_aggregate_past_reserved_does_not_change_count`(과거 예약 무영향)로 경계 단언. 통합 `test_availability_deduction_roundtrip`이 실 SQL on_or_after를 왕복 검증. **윈도잉(무제한 미래 슬롯 반환)은 비-correctness**(aggregate=오늘만·get_room_slots=단일 날짜·next_available=30일 상한 카운트라 set 크기만 영향) → 문서화된 on_or_after 최적화로 수용(추가 상한 불요).
- **reservation 행 하드삭제 시 `ondelete=CASCADE` 회귀 테스트 부재** [apps/api/tests/integration/test_reservations_migration.py] — `reservation_slots.reservation_id`의 CASCADE(예약 행 삭제 시 슬롯 자동 제거)가 docstring·마이그레이션에만 있고 테스트가 없다. 현 앱은 예약을 하드삭제하지 않음(취소=status flip+슬롯 DELETE). **회수:** 예약 하드삭제 경로(E8 계정 비활성 캐스케이드 등) 도입 시 `session.delete(reservation)` → 슬롯 행 0 단언 추가.
- **예약 단위 멱등성 키 부재(재시도 시 중복 `Reservation` 생성)** [apps/api/app/reservations/service.py:71-127] — `uq_reservation_slots_room_slot`은 슬롯 겹침만 막고, 같은 예약 요청의 네트워크 재시도(다른 슬롯·동일 의도)는 새 `Reservation` 행을 중복 생성할 수 있다. idempotency-key는 데이터 계층이 아닌 쓰기 엔드포인트 책임. **회수:** Story 4.5(즉시 예약, 결제 없음)가 엔드포인트 멱등성(Idempotency-Key 헤더 또는 요청 단위 dedupe)으로 소유.

## Deferred from: code review of story-3.9 (2026-06-16)

- **모바일 온보딩 오버레이 `accessibilityViewIsModal` iOS 전용 — Android TalkBack 포커스 트랩 미보장** [apps/mobile/src/components/OnboardingOverlay.tsx] — `accessibilityViewIsModal`은 iOS 전용 prop으로 Android에선 no-op이다. TalkBack 사용 시 모달 뒤 placeholder(`FindScreen` 텍스트)로 스크린리더 포커스가 새어나갈 수 있다(배경 콘텐츠에 `importantForAccessibility="no-hide-descendants"` 등 Android 보완 없음). 스토리 Dev Notes가 "RN 포커스 트랩은 web DOM 트랩과 다르다 — accessibilityViewIsModal로 충족, 키보드 트랩 개념은 RN에 약함"으로 **이 방식을 명시 수용**했고, 모바일은 Epic 3 최초 작업·실 지도 기능은 E7로 deferred 상태라 본 스토리에서 Android 트랩까지 강화하지 않는다. **pre-existing 설계 수용**(이 변경이 만든 결함 아님 — RN Modal a11y의 플랫폼 한계). **회수:** E7 모바일 하드닝에서 Android 배경 콘텐츠 격리(`importantForAccessibility`) 또는 검증된 모달 a11y 패턴 도입.

## Deferred from: code review of story-3.8 (2026-06-16)

- **ExploreView `queueMicrotask`+`viewLockedRef` setState 취약성** [apps/web/src/features/list/ExploreView.tsx:64-73] — 자동 우회(locationDenied 확정) effect가 `viewLockedRef.current=true`로 마킹한 뒤 `queueMicrotask`로 `setViewMode("list")`/`setSearchMode("region")`을 이월하는데, (1) 이미 스케줄된 microtask는 같은 틱에 사용자가 누른 토글 선택(예: radius)을 region으로 덮어쓸 수 있고, (2) 그 사이 언마운트되면 setState-after-unmount 가능. 현재 list 뷰 진입 경로가 모두 `viewLockedRef`를 먼저 세우므로 실질 발생은 방어되나 불변식이 코드 순서에 암묵 의존. **pre-existing**(3.5/3.6 도입 패턴 — 3.8의 `handleSwitchToList` 추가가 유발한 것 아님). **회수:** 동기 set-state-in-effect 함정을 피하면서도 microtask 경합을 없애는 패턴(예: 파생 상태/리듀서)으로 전환 시.

## Deferred from: code review of story-3.7 (2026-06-16)

- **동시 토글 시 stale `previous` 롤백이 진행 중 형제 뮤테이션을 덮어씀** [apps/web/src/features/favorites/useFavorites.ts:97-116] — 같은 캐시 키를 쓰는 즐겨찾기 토글을 ~100ms 내 연속 실행하면(룸 A→B, 또는 시트+목록 동시 마운트), A 실패의 `onError` `setQueryData(FAVORITES_KEY, previousA)`가 B가 적용한 옵티미스틱 상태를 덮어쓴다(스냅샷 통째 복원). `cancelQueries`는 in-flight 쿼리만 취소하고 형제 뮤테이션은 직렬화하지 않는다. `onSettled` invalidate 재조회로 최종 수렴하므로 깜빡임 수준. **회수:** 룸별 mutation `scope: { id: roomId }`로 동일 키 뮤테이션 직렬화, 또는 함수형 롤백으로 전환(설계 변경 규모라 deferred).
- **`add_favorite` IntegrityError 경합에서 `existing=None`이면 500** [apps/api/app/favorites/service.py:49-59] — unique 위반(이미 즐겨찾기) 후 rollback→재조회 사이에 다른 트랜잭션이 그 행을 삭제하면 `existing is None`로 outer `raise`(원 IntegrityError가 500으로 전파). add-while-concurrent-remove 인터리브에서만 발생하는 극히 드문 경합. 문서상 "멱등" 보장이 이 인터리브에선 깨진다. **회수:** `existing is None` 시 1회 재삽입 시도 또는 멱등 신호로 정규화.
- **`list_favorites` N+1 쿼리 + 페이지네이션 부재** [apps/api/app/favorites/service.py:128 → apps/api/app/rooms/service.py:348-355] — 즐겨찾기 N건당 `session.get(Room)` + `room_remaining_slots`(BusinessHours·HolidayException 2 select) ≈ 2N+1 쿼리, `GET /favorites`에 LIMIT/페이지네이션 없음. 즐겨찾기 다수 사용자에서 쿼리 증폭·무제한 결과. **회수:** 룸/영업시간/휴무 배치 조회(IN 절) 또는 조인 + 페이지네이션 도입(Epic 3 공개 조회 페이지네이션 deferred 흐름과 정합).
- **로그인/로그아웃 전환 시 세션·favorites 캐시 무효화 부재** [apps/web/src/features/auth/useSession.ts:22] — `useSession` `staleTime 5분`인데 로그인 성공/로그아웃 시 `["auth","me"]`·`["favorites"]`를 invalidate하는 배선이 어디에도 없다(전용 로그인 화면=별도 스토리·본 스토리 스코프 밖). 새로고침 없이 로그인하면 최대 5분간 하트·즐겨찾기 페이지가 로그아웃 상태로 남고, 로그아웃도 캐시 잔존. **회수(후속 로그인 스토리 의무):** 로그인/로그아웃 성공 핸들러에서 세션·favorites 캐시 무효화 배선.

## Deferred from: code review of story-3.2 (2026-06-15)

- **공개·무인증 `GET /rooms`·`/availability` 무제한 전체 활성 룸 스캔** [apps/api/app/rooms/service.py:217-245] — 페이지네이션/bbox/레이트리밋 없이 전체 활성 룸 좌표·이름을 무인증으로 노출(스크래핑/스캔 벡터). 스토리 Dev Notes·KTH 3.1 결정 #2로 **이미 명시 보류**: bbox=3.5·행정동 필터=3.4·레이트리밋=후속/배포. MVP는 제공자당 룸 1개라 수백 개까지 무리 없음. **회수:** 3.5(반경/bbox) + 배포 시 레이트리밋. 🟡 **부분 회수(Story 3.4, 2026-06-15):** 신규 공개 `GET /api/v1/rooms/search?region_code=`로 **행정동(법정동 시군구/동) 단위 지역 필터를 추가**해 "전체 무제한 스캔 → 지역 좁힘" 항목을 회수했다(콤보는 룸이 있는 지역만 노출 + `enabled:!!regionCode`로 미선택 시 조회 0). **잔존:** bbox/반경 필터=3.5 · 페이지네이션·레이트리밋=후속/배포(필터는 현재 활성 룸 조회 후 Python 매칭 — SQL prefix 최적화도 bbox와 동일 계열 deferred). 🟡 **부분 회수(Story 3.5, 2026-06-16):** 같은 `GET /rooms/search`에 `lat`·`lng`·`radius_km` 반경 파라미터(순수 `haversine_km` Python 필터·가까운 순 정렬·`lat`/`lng` 둘 다 있을 때만 적용)를 추가해 "전체 무제한 스캔 → 반경 좁힘"도 회수했다(현위치 기준·기본 3km). **잔존:** SQL/PostGIS 공간쿼리·공간 인덱스(데이터 증가)·페이지네이션·레이트리밋(배포). 마이그레이션 0·신규 의존성 0.
- **핀 탭으로 열린 RoomSheet의 상태 스냅샷 노출(stale badge)** [apps/web/src/features/map/MapView.tsx:27,165] — `selectedRoom`이 탭 시점의 동결 스냅샷이라 백그라운드 refetch로 가용성이 바뀌어도(예: 예약 가능→마감) 열려 있는 시트 배지가 갱신되지 않는다. 시트 신선도는 Dev Notes상 **3.3·4.3 책임**(단일 룸 신선 조회). **회수:** Story 3.3에서 시트 상태를 현재 `pins`에서 room_id로 도출하거나 단일 룸 신선 조회로 대체. ✅ **회수 완료(Story 3.3, 2026-06-15):** 신규 공개 `GET /api/v1/rooms/{room_id}`(단일 룸 신선 요약 — `derive_slots` 재사용 `remaining_slots`)를 추가하고, `RoomSheet`가 `useRoomSummary`(키 `['rooms', roomId]`·`refetchOnMount:'always'`)로 **시트 열 때마다 신선 조회**한다. 예약 가능 배지(`summaryStatus(remaining_slots)`)가 핀 스냅샷(`fallbackStatus`)이 아니라 신선값으로 결정된다(핀 스냅샷은 로딩 중 깜빡임 방지용 초기 표시로만 사용). 회귀 단언: `RoomSheet.test.tsx` "fallback=available 이어도 신선=0 → 마감".
- **`pins` useMemo 식별자 매 refetch 변경 → 전체 오버레이 재생성** [apps/web/src/features/map/useRoomPins.ts:38] — `joinAvailability`가 매번 새 배열/객체를 반환해 refetch마다 모든 CustomOverlay를 teardown/rebuild(다수 핀 시 깜빡임·DOM 재생성 비용). 증분 갱신·마커 클러스터링은 아래 3.2 forward note(1000+ 규모)·3.5와 동일 계열. **회수:** 증분 diff 갱신 또는 클러스터링 도입 시.
- **`geolocation.getCurrentPosition` timeout 옵션 부재** [apps/web/src/features/map/MapView.tsx:50-60] — 옵션 미전달로 콜백이 영영 안 오는 환경(일부 webview)에서 위치 거부 배너가 뜨지 않음. 단 서울 중심 폴백 + 핀은 표시되어 **막다른 화면은 아님**(AC5④ 충족). **회수:** `{ timeout, maximumAge }` 옵션 추가(마이너 하드닝).
- **`injectScript` 실패 `<script>` 태그 미제거** [apps/web/src/lib/kakao-map.ts:24-36] — error 리스너가 reject만 하고 `script.remove()`를 안 해 반복 재시도 시 dead `<script>`가 `<head>`에 누적(마이너 누수). **회수:** error/load 후 정리. ✅ **회수 완료(Story 5.4, 2026-06-17):** `kakao-map.ts` `injectScript` 의 load·error 콜백 양쪽에 `script.remove()` 추가(SDK 는 로드되면 전역 `window.kakao` 에 붙어 태그 제거 후에도 사용 가능 — dead `<script>` 누적 차단). 기존 Promise 캐시·중복 주입 가드·도메인 거부 reject·재시도 동작 불변. 신규 share 로더(`apps/web/src/lib/kakao-share.ts`)도 같은 `injectScript` 패턴을 미러하므로 처음부터 load·error 후 `script.remove()` 를 내장(같은 함정 재발 방지 — 재-defer 금지).

## Forward note for: Story 3.2 (첫 진입 지도 + 핀) — 2026-06-15 (KTH)

- **지도 마커 클러스터링 (수천 개 룸 대비)** [apps/web·apps/mobile 지도 features] — 3.1 가용성 집계는 **전체 활성 룸**을 반환하고 3.2가 핀을 전부 렌더한다. 백엔드/페이로드는 수천 개까지 무리 없으나(룸당 ~60바이트·`derive_slots` 무시 가능), **클라이언트 마커 렌더가 먼저 병목**이다 — 수백 개는 OK, **1,000개 이상(특히 저사양 모바일)은 버벅임** 시작. **회수(Story 3.2 create-story 시 반드시 편성):** 카카오맵 마커 클러스터링(가까운 핀 묶어 숫자 표시)을 3.2에서 구현하거나, 최소한 "수천 개 시 클러스터링 도입" 임계를 명시. **연계:** 화면 범위(bbox) 안의 룸만 서버 계산하는 최적화(3.1에서 차후로 미룸 — 3.5 반경 검색 공간쿼리 또는 데이터 증가 시점)와 함께 확장 경로를 구성한다. MVP는 제공자당 룸 1개(룸 수≈제공자 수)라 수백 개까진 "전부 조회+전부 렌더"로 충분. [Source: Story 3.1 신선도/스코프 결정 / [[availability-freshness-policy]]]

## Deferred from: code review of 2-2-공간-등록-주소-검색 (2026-06-15)

- **geocode 빈 b_code 후보가 등록 스키마(min_length=1)와 계약 불일치** [apps/api/app/rooms/service.py:208 / app/rooms/schemas.py:71] — `_parse_kakao_documents`는 `address.b_code`·`road_address.b_code`가 모두 없으면 `admin_dong_code=""`인 `GeocodeResult`를 정상 후보(200)로 반환하나, `RoomCreateRequest.admin_dong_code`는 `min_length=1`이라 그 후보를 선택해 등록하면 422가 된다. 스펙 Task1이 "코드 부재 문서는 후보 제외 또는 빈 코드 표시"를 양자 허용해 현재 동작은 스펙 위반은 아니나, 지오코딩 API와 등록 API의 후보 사용 가능성이 어긋난다. **트리거:** 제공자 등록 프론트 폼(전용 후속 프론트 스토리)이 geocode 후보를 소비할 때. **회수:** 그 스토리에서 빈 b_code 후보 필터링 또는 "코드 미확정" 표시 + 등록 정책 확정.
- **GeocodeResult 좌표 범위 검증 부재** [apps/api/app/rooms/schemas.py:111] — `GeocodeResult`는 `lat`/`lng`에 `Field` 범위 제약이 없어 카카오가 손상 좌표(`y="999"` 등)를 반환하면 범위 밖 후보를 그대로 노출한다. 등록 단계 `RoomCreateRequest.lat/lng`(±90/±180) + DB CHECK가 최종 차단하므로 데이터 무결성은 보존(이중 방어 존재)되나, 후보 단계 검증은 비대칭. 카카오 정상 응답은 범위 내라 실발생 희박. **트리거:** 지오코딩 후보 자체에 무결성 보장이 필요해지는 요구. **회수:** `GeocodeResult.lat/lng`에 `Field(ge/le)` 범위 추가.
- **amenities DB CHECK 부재(defense-in-depth 비대칭)** [apps/api/app/rooms/models.py:105] — 2.2가 `room_type`·`role`은 DB CHECK로 회수했으나 `amenities`(JSONB 배열)는 Pydantic `Literal`(`AmenityCode`)만 검증한다. API 외 쓰기 경로(시드/raw SQL)는 임의 코드 저장 가능. JSONB 배열이라 단순 CHECK 표현 비용이 커 보류. **트리거:** rooms에 API 외 쓰기 경로 도입 또는 amenities 무결성 강제 요구. **회수:** 코드 화이트리스트 CHECK(예: jsonb 원소 검증 함수/제약) 또는 별도 amenity 참조 테이블.

## Deferred from: code review of 2-1-rooms-데이터-모델-영업시간-휴무-모델 (2026-06-15)

- **비-KST tz의 DST gap/fold 미처리** [apps/api/app/rooms/service.py:84] — `derive_slots`의 `tz` 파라미터는 다중 tz 확장 여지로 오버라이드 가능하나, DST 있는 타임존에서는 두 벽시계 슬롯이 같은 UTC 인스턴트로 매핑돼 `set`에서 침묵 중복제거되거나 fold가 모호해진다(예: America/New_York 봄철 02:00·03:00 → 동일 07:00Z). **MVP=KST(DST 없음) 기본은 안전.** 1.5 code review의 "DST 있는 타임존 동작·테스트 부재" defer와 동일 계열 — **트리거:** DST 있는 룸 도입(멀티-tz 확장) 스토리. **회수:** 그 스토리에서 DST 경계 명세 + 테스트, 또는 비-KST tz 입력 가드.
- **derive_slots room_id 미필터** [apps/api/app/rooms/service.py:75] — 루프가 `weekday`만 필터하고 `room_id`는 보지 않아, 호출부가 여러 룸의 `business_hours` 행을 섞어 넘기면 윈도우가 한 룸 슬롯으로 합쳐진다(쿼리에서 `.where(room_id==...)` 누락 시 그럴듯하게 잘못된 가용성, 오류 없음). 현재 계약은 "그 룸의 행"으로 문서화됨. **트리거:** 실제 ORM 조회로 이 함수를 호출하는 소비처(3.1 가용성 집계). **회수:** 그 스토리에서 호출부 쿼리에 room 필터 보장 또는 함수에 room_id 인자/단언 추가.
- **created_at·is_active server_default 부재** [apps/api/app/rooms/models.py:99-103] — `nullable=False`이나 기본값이 ORM 레이어(`now_utc`/`default=True`)에만 있어 raw SQL/DDL 직접 insert 시 누락된다. 1.7/1.8의 User/RefreshToken과 **동일한 기존 프로젝트 패턴**(이 변경이 만든 결함 아님). **트리거:** raw SQL 적재나 DB 기본값 의존 경로 도입 시. **회수:** server_default 일괄 도입 또는 ORM-only 정책 확정.
- **price_per_hour·capacity·lat·lng 범위 CHECK 부재** [apps/api/app/rooms/models.py:88-97] — 음수 금액/인원, 위경도 범위(±90/±180) 밖 좌표가 DB 레벨에서 삽입 가능. 값 검증(친절한 422)은 **2.2 Pydantic 쓰기 경로**가 소유(defense-in-depth). **트리거:** rooms 쓰기 경로(2.2). **회수:** 2.2에서 Pydantic 범위 검증 + 필요 시 DB CHECK(P3 enum-CHECK 묶음과 함께).
- **close_time==자정/역전 행이 순수 함수 도달 시 침묵 빈 결과** [apps/api/app/rooms/service.py:80-81] — `close_time=00:00`(오버나이트 영업 의도)나 역전 행이 순수 함수에 오면 `[]`를 조용히 반환(DB CHECK `close_time > open_time`가 라이브 저장 경로는 차단). 오버나이트 영업 모델 자체가 미지원(같은 날 내 영업 가정). **트리거:** 자정 넘김 영업 요구 발생 시(제품 결정). **회수:** 그 요구가 생기면 모델·슬롯 도출을 멀티-day 윈도우로 확장.
- **UNIQUE(room_id, weekday)가 분할 영업시간 차단** [apps/api/app/rooms/models.py:137] — 룸·요일당 영업시간 1행만 허용해 점심 브레이크 등 분할 교대(09–12 + 14–18)를 모델링 불가. `derive_slots` 루프는 요일당 다중 행을 이미 결정적으로 처리(방어적, 무해). **트리거:** 분할 영업시간 제품 요구 발생 시. **회수:** UNIQUE를 (room_id, weekday, open_time) 등으로 확장하거나 영업시간 모델 재설계.

## Deferred from: code review of story-1.1 (2026-06-14)

- **ruff/mypy 등 정적 분석 도구 미설정** [apps/api/pyproject.toml] — 보안/설정 핵심 모듈인데 dev 의존성에 `pytest`만 있고 lint/type 게이트가 없음. `.gitignore`는 `.ruff_cache/`·`.mypy_cache/`를 무시하나 도구 자체는 미선언. Story 1.2 풀 백엔드 스캐폴드 시점에 lint/type 게이트로 도입 권장 (Story 1.1은 키 검증용 최소 부트스트랩 범위이므로 지금은 보류). *(→ Story 1.2에서 회수 완료)*

## Deferred from: code review of story-1.2 (2026-06-14)

- **TypeScript 메이저 스큐: mobile `~6.0.3` vs root `5.9.2`/web `^5`** [apps/mobile/package.json] — create-expo-app가 핀한 버전이라 현재 격리된 mobile에서는 동작(검증됨). `packages/api-client` 생성 타입이 web·mobile에 공유되는 Story 1.9 시점에 타입체크 의미 차이/peer 호환 위험 → 그때 버전 정렬 검토.
- **공유 패키지가 raw `.ts` export** [packages/ui/package.json, packages/api-client/package.json] — 현재 placeholder(`export {}`)라 소비처 없음(next.config에 `transpilePackages` 없음 확인). Story 1.6/1.9에서 실제 소비 시 각 Next 앱 `next.config`에 `transpilePackages` 추가 또는 빌드 산출물 export 필요.
- **CORS 하드닝(비-로컬 배포 대비)** [apps/api/app/main.py] — origin이 `localhost:3000/3001` 하드코딩 + `allow_methods/headers=["*"]`+`allow_credentials=True`(스캐폴드 단계 스펙 허용 기본). 배포 시 env 기반 origin + methods/headers 범위 축소 필요.

## Deferred from: dev-story 1.3 Phase 0 스파이크 (2026-06-15)

- **모바일은 Expo Go 워크플로 불가 → development build/EAS 필수** [apps/mobile] — 프로젝트가 Expo SDK 56 / RN 0.85(프리뷰급 최신)라 **스토어 Expo Go가 실행 거부**(`Project is incompatible with this version of Expo Go — requires a newer version`). 1.3 RN SSE 스파이크의 기기 검증이 이 때문에 보류됨. **E7(모바일 본격 개발) 착수 시 dev client(`expo run:android`/`prebuild` 또는 EAS Build)를 처음부터 구성**해야 함. 서버측(curl)·웹 fetch-stream으로 SSE 전송·인증헤더는 검증됨 + react-native-sse는 표준 JS 라이브러리라 잔여 리스크 낮음. (참고: SDK 56이 의도된 선택인지 1.2 스캐폴드 결정 재확인 가치 있음 — 안정 SDK 대비 도구체인 부담.)
- **Phase 0 ③ pgvector 실 이관 미실행** [spikes/phase0/pgvector] — docker·로컬 pgvector·Railway 모두 부재로 시드/덤프/복원/검색 스크립트·런북만 준비. Railway pgvector 프로비저닝(또는 로컬 pgvector 설치) 후 `RUNBOOK.md`대로 1회 실행 필요(배포 준비 시점).

## Deferred from: code review of 1-3-phase-0-검증-스파이크 (2026-06-15)

- **RN SSE 진단·견고성 묶음** [apps/mobile/src/app/spike-sse.tsx] — error 핸들러 `JSON.stringify(event)`가 react-native-sse 오류 이벤트에서 `{}` 산출(서버다운·잘못된 LAN IP·헤더거부를 구분 못 함), connect 타임아웃 분기 없음(잘못된 LAN IP 시 OS 소켓 타임아웃까지 무한 대기 → AC2 거짓음성 위험), 하드코딩 폴백 `http://192.168.0.2:8001`(env 미설정 시 불투명 실패). RN 기기검증이 E7로 이관됐으므로 함께 이관 — E7에서 dev client 구성 시 진단 가능한 오류표시 + 타임아웃 + base URL 검증으로 재구현.
- ✅ **SSE 서버 프레이밍 견고성 — 회수 완료(2026-06-18, Story 7.4)** [spikes/phase0/sse/server.py → apps/api/app/chatbot/router.py] — 스파이크 `_token_stream`의 공백분할 + 리터럴 `[DONE]` 결함(토큰에 `\n\n`·연속 공백·`[DONE]` 섞이면 프레임/종료 깨짐)을 **`sse-starlette` `EventSourceResponse` + JSON 인코딩**(`data: {"delta": ...}`·`ensure_ascii=False`)으로 회수했다. 각 토큰을 JSON으로 인코딩하므로 `\n`·공백·`[DONE]`이 델타에 섞여도 와이어 `data:`는 단일 라인이라 프레임 무손상이고, 종료는 명시 `event: done`·에러는 `event: error`로 신호한다(리터럴 `[DONE]` 문자열 종료 폐기). 회귀 테스트로 고정: `tests/chatbot/test_chatbot_router.py::test_stream_framing_robust_to_special_chars` + `test_chatbot_stream.py::test_deltas_with_special_chars_pass_through`. **재-defer 금지** ([[dev-workflow-policy-deferred-and-repeat-mistakes]]).
- **모바일 챗봇 응답 스트리밍(react-native-sse) = "모바일 dev-build 푸시" 버킷** [apps/mobile/src/features/chatbot(미존재)] — 7.4는 **웹 fetch-stream만** 구현했다(BE `POST /api/v1/chatbot/stream`은 웹·모바일 **공용** SSE 엔드포인트 + 웹 `features/chatbot/streamMessage.ts` fetch-stream 파서 + `useChatbot` 스트리밍 전환). 모바일 RN 스트리밍(`react-native-sse` — RN엔 EventSource 부재라 인증 헤더·POST 바디 fetch 기반 라이브러리)은 **모바일 인증 스택 부재**(`useSession`·`QueryClientProvider`·secure-store Bearer 미배선) + 화면 **Epic 4 placeholder** + **7.3 모바일 챗봇 UI 부재**(위 L5 모바일 챗봇 버킷 선행)라 전제가 없어 본 스토리에서 동등 적용 불가 → 명시 라우팅. 본 스토리 `apps/mobile` 변경 0. **BE 재구현 불요**(`react-native-sse`가 같은 `/api/v1/chatbot/stream`을 소비). **회수:** 모바일 챗봇 UI(L5 버킷) + 인증 스택 선행 후 "모바일 dev-build 푸시" 버킷에서 `react-native-sse`로 구현 — 웹 `streamMessage.ts`(SSE 프레임 파서·`data:`/`event:` 처리)가 RN 대체 시 참조 후보. ⚠️ E7=챗봇 에픽이지 모바일 버킷이 아님(혼동 주의). [[web-mobile-parity-on-changes]]
- **pgvector 데이터 충실도 검증 (행수·체크섬·알려진 NN 단언)** [spikes/phase0/pgvector/verify.sql] — 1.3 스파이크는 이관 *경로*(확장 선설치·덤프/복원·vector 컬럼·HNSW 생존·유사도 쿼리 실행성)를 증명했고 AC3는 문자 그대로 충족(2026-06-15 KTH 결정: 수용). **단 증명 안 된 것 = 실 임베딩의 비트 충실도** — `verify.sql`이 ① 복원 행수를 시드 행수(10)와 자동 대조하지 않고 ② 질의 벡터가 랜덤·정답 NN 없음이라, 임베딩이 절단/손상돼도 검색은 3행을 반환(부분 복원·데이터 손실을 조용히 통과).
  - **트리거(언제):** 날짜가 아니라 조건 — **소스 pgvector → Railway로 실제 임베딩 데이터를 처음 1회 이관하는 시점**(E7에서 임베딩이 적재된 이후 ~ 배포 준비). 그 전(예: 1.4 DB연결/Alembic)에는 옮길 실데이터가 없어 검증 대상 자체가 없음.
  - **책임 스토리(누가):** 위 실 이관을 수행하는 스토리(E7 임베딩 적재 후속 또는 배포 준비 스토리). **그 스토리 sprint-planning 시 본 항목을 반드시 해당 스토리의 AC/Task로 회수할 것** — deferred-work는 자동 스케줄러가 아니므로 능동 회수 필요.
  - **무엇을(검증 내용):** 결정적 시드(고정 벡터) 또는 실 임베딩으로 이관 후 (a) 행수=소스와 일치 단언, (b) 임베딩 체크섬/해시 일치, (c) 알려진 질의에 대한 최근접행이 기대값과 일치, 를 단언으로 확인. CI/이관 런북에 포함.

## Deferred from: code review of 1-4-데이터베이스-연결-마이그레이션-기반 (2026-06-15)

- **네이밍 규약이 PostgreSQL 63자 식별자 한계 초과 가능 (후속 스토리 리스크)** [apps/api/app/core/db.py:29-30] — `NAMING_CONVENTION`의 `ix`/`uq` 템플릿이 `column_0_N_name`으로 제약의 모든 컬럼명을 펼친다. 폭넓은 복합 제약(예: 3~4컬럼 UNIQUE)은 생성 제약명이 63자를 넘겨 PostgreSQL이 침묵 절단 → 이름 충돌·autogenerate 불일치 위험. **1.4 범위에선 미발생**(도메인 테이블 0개). **트리거(언제):** 후속 스토리(2.1 rooms·4.1 reservations 등)가 폭넓은 복합 UNIQUE/INDEX를 추가하는 시점. **회수:** 해당 스토리에서 긴 복합 제약은 명시적 단축명(`Index("idx_...", ...)` / `UniqueConstraint(name=...)`)을 부여하거나 규약에 truncation 정책 도입. 🟡 **부분 회수(Story 2.1, 2026-06-15):** rooms 도메인 복합 UNIQUE/CHECK에 명시 단축명 + `tests/rooms/test_models.py` ≤63 회귀 가드를 도입했다(트리거가 2.1에서 1차 발화). ✅ **회수 완료(Story 4.1, 2026-06-16):** reservations 도메인 복합 UNIQUE에 명시 단축명 `uq_reservation_slots_room_slot`(30자) + CHECK `ck_reservations_status`(이중접두 아님) 부여, `tests/reservations/test_models.py`로 ≤63 + 이중접두 부재 회귀 가드. FK 자동명 중 가장 긴 `fk_reservation_slots_reservation_id_reservations`(48자)도 ≤63 확인. 스토리가 명시 지목한 트리거(4.1 복합 UNIQUE)를 정면 회수 — 더 이상 후속 트리거 없음(규약 truncation 정책은 미도입, 명시 단축명 패턴으로 충분).
- **DATABASE_URL 정규화기가 host 없는 `postgresql://`를 수용** [apps/api/app/core/config.py:105-120] — 스킴만 있고 host/db가 빈 URL이 `postgresql+psycopg://`로 통과한다(엔진 연결 시 `host=(unknown)`로 fail-fast하므로 비밀 노출·보안 문제는 없음, 단 오류가 한 단계 늦게 표면화). host 존재 가드를 추가하면 더 이른 명확한 거부 가능 — 선택적 하드닝.
- **`get_engine` lru_cache가 테스트 픽스처에서 리셋되지 않음** [apps/api/app/core/db.py:46] — `get_settings`는 `cache_clear` 픽스처가 있으나 `get_engine`은 없다. 현재 테스트는 `get_engine`을 통째로 monkeypatch(test_db)하거나 별도 엔진을 직접 생성(integration)해 실 `get_engine`을 호출하지 않으므로 **현 시점 트리거 없음**. 향후 라우터 테스트가 실 `get_engine`/세션을 구동할 때 첫 엔진이 프로세스 전역에 캐시돼 `DATABASE_URL` 변경이 무시될 수 있음 → 그 테스트 도입 시 `get_engine.cache_clear()` 픽스처 추가.

## Deferred from: code review of 1-5-횡단-유틸-시간-규약-표준-에러-스키마 (2026-06-15)

- **`hours_until` 부동소수 경계 정밀도** [apps/api/app/core/time.py:69-89] — 슬롯 시각이 마이크로초 정밀도면 "정확히 6h 경계"가 1µs 차이로 `<=6`을 탈락해 경계 포함 의도가 깨질 수 있음(예: `timedelta(hours=6, microseconds=1)` → `6.0000...` > 6 → `is_within_hours(...,6)`=False). **1.5 범위에선 미발현**(라우터·실 슬롯 데이터 없음). **트리거(언제):** 소비 스토리가 실 예약 슬롯 시각으로 6h/24h 경계를 판정하는 시점(E4 슬롯/4.7 취소·5.2 리마인드). 스토리가 `<` vs `<=`·경계 정밀도 정책을 소비 스토리에 명시 위임함. **회수:** 해당 스토리에서 epsilon 허용오차 또는 초 단위 절삭 정책을 결정하고 경계 테스트 추가. 🟡 **부분 회수(Story 4.7, 2026-06-17):** 6h 취소 경계를 `(earliest - current) < _CANCEL_WINDOW`(`timedelta(hours=6)`) **정수 비교**로 확정(부동소수 `hours_until` 미사용)+경계 테스트 3종으로 회귀 고정. ✅ **회수 완료(Story 5.2, 2026-06-17):** 24h 도래 리마인드 경계를 `timedelta(0) <= (earliest - now) <= _REMINDER_WINDOW`(`timedelta(hours=24)`) **정수 비교**로 확정(`apps/api/app/notifications/reminders.py` `is_within_reminder_window` — float `hours_until`/`is_within_hours` 미사용) + 경계 테스트(`tests/notifications/test_reminders.py`: 정확히 24h 포함·24h+1초 제외·delta 0 포함·음수 제외·24h-1초 포함·naive 거부)로 회귀 고정. 정책 = **양 경계 포함 + timedelta 정수 비교**(epsilon/절삭 불요 — duration 정수 비교가 1µs 모호성을 원천 제거). 6h(4.7)·24h(5.2) 두 소비처 모두 정면 회수 — `core/time.is_within_hours` float 경계 함수는 남기되 시간경계 판정 신규 소비는 timedelta 패턴을 따른다(더 이상 후속 트리거 없음).
- **DST 있는 타임존(`tz` 파라미터 일반화) 동작·테스트 부재** [apps/api/app/core/time.py:50-66] — `to_tz`/`today_in_tz`는 다중 tz 확장 여지를 위해 `tz` 파라미터를 받으나 MVP=`Asia/Seoul`(DST 없음) 단일이라 spring-forward gap·fold(중복 시각) 경계의 명세·테스트가 0건. `astimezone`에 위임하므로 stdlib가 처리하나 fold 의도·경계 계약 미정의. **1.5 범위에선 비활성**(KST 단일). **트리거(언제):** DST 있는 타임존의 룸을 실제 도입하는 미래 스토리(멀티-tz 확장). **회수:** 그 스토리에서 DST 경계(gap/fold) 동작 명세 + 경계 테스트 추가.

## Deferred from: dev-story 1.6 디자인 토큰·UI 셸 (2026-06-15)

- **RN 토큰·셸 기기 시각 검증** [apps/mobile] — 1.6은 RN 토큰 매핑·Pretendard(정적 otf+expo-font plugin)·3탭 셸·전역 레이어(FAB/배너 슬롯)·reduced-motion 게이트의 **코드 작성 + 타입(tsc)/린트(expo lint) 그린**까지 책임을 완수했다. 그러나 **실제 기기 픽셀 검증**(Pretendard 임베드 렌더·만다린 탭 색·FAB 위치/그림자·SF/Material 탭 아이콘·reduced-motion 동작)은 **Expo SDK56↔Expo Go 비호환으로 이번 스토리에서 불가**(기존 1.3 이관 항목과 동일 원인). **트리거(언제):** E7 모바일 본격 개발에서 dev client(`expo run:*`/prebuild 또는 EAS Build)를 구성하는 시점. **회수:** 그 스토리에서 ① Pretendard 4-weight가 dev build에 임베드되어 표시되는지 ② NativeTabs `sf`/`md` 아이콘이 iOS/Android에서 렌더되는지 ③ FAB가 탭바 위 우하단에 만다린·그림자로 뜨는지 ④ OS reduced-motion 설정 시 스플래시가 생략되는지 육안 확인 + 필요 시 위치/색 미세조정.
- **모바일 `boxShadow` 문자열 스타일의 RN 렌더 검증** [apps/mobile/src/components/ChatbotFabSlot.tsx] — FAB 그림자를 `elevation.fab`(CSS box-shadow 문자열, RN 0.76+ `boxShadow` 지원)로 소비한다. 타입은 통과(ViewStyle.boxShadow: string)하나 iOS/Android 네이티브 렌더 충실도는 dev build에서만 확인 가능 → 위 RN 기기 검증과 함께 회수(필요 시 플랫폼별 shadow* props 폴백 검토).
- **mobile-web(react-native-web) 폰트** [apps/mobile/src/global.css, theme.ts] — Pretendard 정적 otf는 네이티브 임베드 전용이라 mobile-web에선 미적용(Fonts.web=시스템 폴백). mobile-web은 제품 표면이 아니므로(앱=네이티브) 보류. mobile-web을 실제 표면으로 쓸 경우에만 `@font-face` 추가 회수.

## Deferred from: code review of 1-6-디자인-토큰-ui-셸 (2026-06-15)

- **모바일 런처 아이콘 ART (브랜드 아이콘/로고 에셋 필요)** [apps/mobile/app.json, assets/images] — **⚠️ 부분 처리됨(2026-06-15, KTH 결정: 스플래시=만다린 `#FF8A1E`·배경색만 브랜드).** ✅ **완료분:** splash `backgroundColor` `#208AEF`→`#FF8A1E`·`android.image`(splash-icon.png) 제거(단색 만다린 스플래시 통일)·adaptiveIcon `backgroundColor` `#E6F4FE`→`#FF8A1E`+블루 `backgroundImage` 참조 제거·`animated-icon.tsx` 오버레이 색→`colors.primary`(토큰)·미사용 `AnimatedIcon`/`keyframe`/`logoKeyframe`/`glowKeyframe`+expo-logo 죽은 코드 제거(native·web)·고아 데모 에셋 11종 삭제(splash-icon·expo-logo·logo-glow·expo-badge×2·react-logo×3·tutorial-web·android-icon-background·animated-icon.module.css). 게이트 `turbo run lint check-types test` 8 tasks 그린. 🔶 **잔여(이관):** 실제 런처 아이콘 **아트**(`icon.png`·`ios.icon`=`./assets/expo.icon`·`android-icon-foreground.png`·`android-icon-monochrome.png`)는 아직 **Expo 기본 아트** — 브랜드 아이콘/스플래시 로고 PNG·`.icon`은 디자인 에셋이라 코드로 생성 불가. **트리거(언제):** E7 EAS dev build·브랜딩 패스. **회수:** DeskNow 브랜드 앱 아이콘(iOS `.icon`·Android adaptive foreground/monochrome)·필요 시 스플래시 로고 이미지를 디자인 에셋으로 제작해 `app.json`에 배선. *(스플래시 배경·죽은 코드·블루 잔재 색은 이미 정리됨 — 남은 건 순수 아이콘 그래픽 아트.)*
- **AC1 단일출처 보장이 사실상 색 토큰에만 적용 — 타이포/elevation/radius 드리프트 무방비** [packages/ui/src/tokens.parity.test.ts, apps/mobile/src/components/themed-text.tsx, apps/web/src/app/page.tsx] — `tokens.ts`에 `typography` 토큰이 정의돼 있으나 **어느 표면도 그 토큰을 소비하지 않음**: RN `themed-text.tsx`는 램프 값을 px로 하드코딩(display 32/lineHeight 42/letterSpacing -0.32 …), web 쇼케이스·placeholder는 arbitrary Tailwind 값(`text-[2rem]`/`leading-[1.6]`/`tracking-[-0.01em]`)으로 손수 재기입. preset `@theme`에 타이포 매핑도 없음(색·radius·shadow·font-sans만). parity 테스트는 색 hex + `--radius`/`--font-sans` 존재만 검사 → **타이포·elevation 그림자·radius 패밀리가 정본과 갈라져도 테스트 통과**(AC1 "타이포 램프 3표면 동일"의 실증 구멍). **현재 값은 DESIGN 정본과 일치 → 즉시 결함 아님.** 1.6 Task 3가 색 hex만 parity 대상으로 명시했으므로 사양 위반도 아님. **트리거(언제):** 타이포/spacing/elevation 토큰을 실제로 변경하거나, 다수 컴포넌트가 타이포를 소비하기 시작하는 후속 스토리. **회수:** ① RN이 `typography` 토큰을 소비(em→px·weight→Pretendard 패밀리 변환 헬퍼) ② web은 타이포를 Tailwind text 유틸(`@theme`)로 노출하거나 토큰 기반 컴포넌트로 ③ parity 테스트를 비색 토큰(typography lineHeight·elevation·radius 패밀리)까지 확장.
- **관리자 사이드바 3개 내비가 모두 `href="/"`** [apps/admin/src/components/shell/AdminShell.tsx:115-119] — `ADMIN_NAV`(계정 관리·예약 임의취소·챗봇 인제스트)의 `href`가 전부 `/`라 메뉴가 구분 진입되지 않음(`key={item.label}`로 React key 충돌은 회피). web `AppShell`은 `/reservations`·`/favorites` 실 placeholder 라우트를 만든 반면 admin만 미생성 → 같은 역할 셸 간 비대칭. **실 화면·라우트는 Epic 8 범위.** **회수:** E8 관리자 기능 스토리에서 실 라우트(`/accounts`·`/cancellations`·`/ingest` 등) 생성 시 `href` 연결.
- **web `AppShell`에 AC4 명시 "lg(2열, 지도+사이드 목록) 자리" 스캐폴드 부재** [apps/web/src/components/shell/AppShell.tsx] — 반응형 분기가 `md:` 단일 브레이크포인트뿐(상단 내비 ↔ sm 하단 내비바)이고 `main`은 `max-w-6xl` 단일 컬럼 → AC4가 web 셸 3단계로 든 `lg`(2열) 자리가 코드에 없음. **단 2열(지도+사이드 목록)은 E3 "스터디룸 찾기" 페이지 레이아웃 영역** — 범용 셸(모든 페이지 래핑)에 2열 그리드를 강제하면 reservations/favorites 단일 컬럼 페이지까지 잘못 제약하므로 셸이 아닌 페이지에서 구현하는 것이 적절. **회수:** E3 지도+목록 화면 구현 시 해당 페이지에서 `lg:` 2열 레이아웃 적용(필요 시 셸에 `lg` 컨테이너 폭 변형 추가 검토).
- **모바일 `InAppBannerSlot` 빈 노드 `accessibilityRole="summary"` + 위치/스타일 미정의 → web 슬롯과 a11y·배치 비대칭** [apps/mobile/src/components/InAppBannerSlot.tsx] — RN 슬롯은 `<View accessibilityRole="summary" />`(스타일·위치 없음, 빈 상태에서 상단에 자리잡지 못함). web/admin 슬롯은 `empty:hidden`+`aria-live="polite"` positioned 컨테이너로 위치·동작 명확. **현재는 빈 슬롯이라 무해.** **회수:** Epic 5 인앱 배너 구현 시 3표면 배너 컴포넌트를 일괄 정의하며 RN 슬롯의 위치(상단 고정)·a11y(`aria-live` 등가)·닫기 버튼(≥44)을 함께 확정.

## Deferred from: code review of 1-7-이메일-회원가입 (2026-06-15)

- **`IntegrityError` 포괄 캐치 과대 → 향후 무관 제약 위반이 EMAIL_TAKEN으로 오변환** [apps/api/app/auth/service.py:41] — `except IntegrityError`가 모든 무결성 위반을 `DomainError(EMAIL_TAKEN, 409)`로 변환. **현재는 무해**(테이블 제약이 `uq_users_email`·`pk_users`(uuid4, 충돌 천문학적)뿐이고 NOT NULL 컬럼은 서비스/모델이 항상 채움). **트리거(언제):** 후속 스토리가 `users`에 FK·CHECK·추가 UNIQUE를 더하거나, 같은 트랜잭션에 연관 insert가 들어오는 시점(1.8 refresh_tokens FK 등). 그때 무관한 위반이 "이미 가입된 이메일"로 잘못 보고 → 디버깅 방해. **회수:** 위반 제약명(`exc.orig`/pgcode로 `uq_users_email` 식별) 확인 후 해당 위반만 EMAIL_TAKEN으로 변환, 그 외 re-raise. (blind+edge)
- **`role` 컬럼 DB CHECK/enum 부재 — `UserRole.ADMIN` 시드전용이 DB 미강제** [apps/api/app/auth/models.py:50] — 컬럼이 자유 문자열(AutoString)이라 비-가입 쓰기 경로가 임의 role("admin" 포함)을 무방비 저장 가능. 가입 엔드포인트는 `Literal["booker","provider"]`로 차단되나 defense-in-depth 부재. **스펙이 `ck_users_role`을 선택적 하드닝으로 명시 defer(Task 3 결정)** — 사양 위반 아님. **트리거(언제):** 시드 스크립트(admin 생성)·역할 전환(E8)·추가 역할 도입 시점. **회수:** DB `CHECK(role in ...)` 또는 PG enum 도입(또는 서비스 레이어 재단언). (blind+edge)
- **이메일 소문자 정규화 불변식이 `register_user`에만 존재** [apps/api/app/auth/service.py:33] — EmailStr는 **도메인만** 소문자화하고 로컬파트 대소문자는 보존(실증: `JoSe@Example.COM`→`JoSe@example.com`). 로컬파트 대소문자 중복차단은 서비스 `.strip().lower()`가 **유일 근거**이고 `uq_users_email`은 코드포인트 정확 비교라 못 잡음. **현재는 register가 유일 쓰기경로라 무해.** **트리거(언제):** 비-register 쓰기 경로(시드·bulk import·1.8 다른 사용자 생성 서비스)가 `data.email`을 직접 저장하는 시점 → `Alice@x.com`+`alice@x.com` 두 계정 공존 가능. **회수:** `User` 모델 레벨 정규화(validator/`__init__`) 또는 `lower(email)` functional unique index로 모든 쓰기경로에 불변식 강제. (edge)
- **`verify_password`가 손상/빈 해시에 예외(≠False) → 1.8 로그인 fail-open 위험** [apps/api/app/core/security.py:38] — pwdlib `verify`는 파싱 불가/빈 해시 문자열에 예외를 던짐(False 아님). 본 스토리엔 로그인 경로가 없으나 `verify_password`는 공개 프리미티브 → 1.8 로그인이 부분 마이그레이션·손상된 `password_hash` 행을 만나면 500. **트리거(언제):** 1.8 로그인 구현. **회수:** 1.8에서 손상-해시 예외를 잡아 False 반환(fail-closed)하도록 래핑. (blind)
- **`created_at`/`is_active`에 `server_default` 부재** [apps/api/alembic/versions/191d9c7dab2d_create_users_table.py] — 두 컬럼 `NOT NULL`이나 DB 기본값이 없어 값을 생략한 non-ORM insert(raw SQL/bulk)는 실패. 현재는 ORM(`default_factory=now_utc`/`Field(default=True)`)이 유일 writer라 무해. **트리거(언제):** raw SQL·bulk insert·외부 도구가 `users`에 직접 쓰는 시점. **회수:** 마이그레이션 컬럼에 `server_default=sa.text("now()")`/`sa.text("true")` 추가(선택적 하드닝). (blind)
- **테스트 충실도: `FakeSession.exec`가 쿼리 인자 무시(자기충족 경향) + 실 쿼리·UNIQUE 왕복은 CI-skip 통합테스트만** [apps/api/tests/auth/test_service.py:39] — `exec(*args,**kwargs)`가 쿼리와 무관하게 `existing`을 반환 → 중복검사 테스트가 WHERE/정규화/컬럼을 검증하지 않음. 실제 쿼리·소문자 정규화·UNIQUE 위반 왕복은 `TEST_DATABASE_URL` 가드 통합테스트(`tests/integration/test_auth_migration.py`, CI에 라이브 DB 없어 자동 skip)에만 존재. **회수:** 경량 쿼리 단언(exec에 전달된 statement 검사) 추가 또는 CI 파이프라인에 라이브 DB 통합테스트 편입. (blind)
- **비밀번호 `max_length` 부재(선택적 하드닝)** [apps/api/app/auth/schemas.py:33] — 상한 없는 `password: str`. **Argon2 DoS는 실증 반증**(10자 39.9ms ≈ 1MB 42.1ms, 비용 길이 무관)이라 보안 동인은 없음. 비정상 대형 입력의 조기 거부(예: `max_length=128`)는 순수 입력 위생용 선택 사항. (blind+edge)
- **공개 Argon2 엔드포인트 레이트리밋 부재** [apps/api/app/auth/router.py] — 미인증 공개 `POST /api/v1/auth/register`가 호출마다 메모리-하드 Argon2 수행. 남용/플러딩 제어 부재. **게이트웨이/인프라 관심사로 스토리 범위 밖.** **회수:** 배포 준비 시 per-IP 레이트리밋/WAF 규칙. (blind)

## Deferred from: dev-story 1.8 로그인·세션·RBAC (2026-06-15)

- **⚠️ 최초 배포 시 운영용 `JWT_SECRET_KEY`를 별도 생성** [운영 환경변수] — 개발 `.env`의 키(`secrets.token_urlsafe(48)`로 로컬 생성됨)를 **배포 환경에 재사용 금지**. Railway 등 운영 호스트의 환경변수에 **새 키를 따로 생성**해 주입한다(`python -c "import secrets; print(secrets.token_urlsafe(48))"`). 키가 유출/노출되면 전체 세션 위조 가능. **키를 교체하면 그 시점의 모든 access·refresh 토큰이 즉시 무효화**(전원 재로그인)되므로 운영 중 교체는 신중히. **트리거(언제):** 최초 배포 환경 구성 시점(+ CORS 하드닝·Supabase→Railway DB 전환과 함께 묶어 처리). **회수:** 배포 준비 스토리/체크리스트에서 운영 비밀 주입 + `alembic upgrade head`(refresh_tokens 적용). (배포)
- **쿠키 보안 속성의 실 브라우저 cross-origin 검증 미수행** [apps/api/app/auth/router.py `_set_auth_cookies`] — 쿠키는 `httpOnly+Secure+SameSite=lax`로 발급되나, TestClient는 Secure/실제 cross-site 전송을 강제하지 않아 **속성 단언만** 검증됨(round-trip 아님). 웹(`:3000`)↔API(`:8000`)가 다른 origin이라 운영 https·cross-site 쿠키 동작(`SameSite` 정책, 필요 시 `none`+`Secure`)은 미검증. **트리거(언제):** 프론트 인증 UI 통합(SDK 1.9 이후) + https 배포. **회수:** 실 브라우저 e2e로 쿠키 set/전송/만료 round-trip 검증 + cross-origin `SameSite` 정책 확정. (배포/1.9)
- **만료된 `refresh_tokens` 행 정리(cleanup) 부재** [apps/api/app/auth/models.py refresh_tokens] — 로그아웃·회전은 행을 삭제하지만, **사용되지 않고 만료된**(앱 미접속으로 회전 안 된) refresh 행은 테이블에 잔류한다. 보안상 무해(만료 토큰은 decode 단계에서 401)하나 장기적으로 누적. **트리거(언제):** 사용자/세션 규모가 커지는 운영 단계. **회수:** 주기 작업(cron/배치)으로 `DELETE FROM refresh_tokens WHERE expires_at < now()` 또는 조회 시 lazy 정리. (배포/운영)

## Deferred from: code review of 1-8-로그인-로그아웃-세션-유지-rbac (2026-06-15)

- **refresh 회전 동시성 더블스펜드 (행 잠금 부재)** [apps/api/app/auth/service.py:104-121] — `rotate_token_pair`가 `SELECT ... FOR UPDATE`·조건부 삭제·rowcount 검사 없이 조회→`delete`→`issue` 한다. 동일 refresh로 동시 `/refresh` 2건이 들어오면 둘 다 행을 보고(아직 누구도 커밋 안 함) 각자 삭제+새 쌍 발급 → **한 refresh에서 두 유효 토큰 패밀리** 생성(회전의 재사용-1회용화 방어가 동시성에서 무력). 새 행은 jti/해시가 달라 UNIQUE도 못 잡음. **현재는 MVP·저동시성이라 발현 낮음.** **트리거(언제):** 보안 민감 출시·동시성 증가 시점. **회수:** 조회를 `with_for_update()`로 잠그거나 `DELETE ... WHERE token_hash=... RETURNING`로 0행=재사용 탐지(→패밀리 무효화)로 전환. (blind+edge)
- **`expires_at` 컬럼이 조회 WHERE에서 미사용 (stateful 만료 미강제)** [apps/api/app/auth/service.py:112-116; apps/api/app/auth/models.py:84] — refresh 만료는 `decode_token`의 JWT `exp` 단독으로 강제되고, DB `expires_at`은 기록만 되고 어떤 WHERE절에서도 읽히지 않는다. 현재 JWT exp와 `expires_at`이 같은 TTL이라 정합하나, TTL 변경·`now` 주입 시 디싱크되면 DB 컬럼은 무방비. "이중 검증(stateful)"의 stateful 절반은 무효화(삭제)만 보고 만료는 안 본다. **회수:** 회전 조회에 `.where(RefreshToken.expires_at > now_utc())` 추가(belt-and-suspenders). 만료행 cleanup은 위 dev-story 1.8 defer(주기 정리)와 중복 — 그쪽으로 통합. (blind+edge+auditor)
- **access 토큰 role/is_active 즉시취소 지연 (≤15분)** [apps/api/app/core/security.py:174-184] — `get_current_principal`이 클레임(role)을 신뢰하고 DB를 조회하지 않아, 관리자가 강등/비활성화해도 기발급 access 토큰은 access TTL(15분)까지 유효. `require_role` 경유 엔드포인트도 DB 재확인 없음. **스펙이 명시한 설계**("access는 단명 토큰이라 클레임 신뢰; is_active 즉시취소는 E8"). **트리거(언제):** E8 계정 비활성화/역할 관리. **회수:** E8에서 즉시취소가 필요하면 access TTL 축소 또는 보호 엔드포인트의 선택적 DB is_active 재확인. (blind)
- **refresh 재사용 탐지·토큰 패밀리 무효화 부재** [apps/api/app/auth/service.py:115] — 회전 후 삭제된(=이미 회전된) refresh를 재제시하면(도난 후 재생 신호) 단순 401만 반환하고 그 사용자의 자손 토큰 패밀리를 무효화하지 않는다. 회전은 했으나 재사용-탐지 기반 봉쇄(reuse detection)가 없어 도난 토큰 차단이 약함(14일 만료에만 의존). **트리거(언제):** 보안 강화/이상탐지 도입 시. **회수:** refresh에 family/lineage 식별자 또는 재사용 감지 시 해당 user의 전 refresh 삭제. (blind)
- **세션 수 제한·"전체 로그아웃" 부재** [apps/api/app/auth/service.py:84-101] — 매 로그인이 무제한으로 `refresh_tokens` 행을 추가하고 기존을 무효화하지 않는다. 사용자당 세션 상한 없음, "모든 기기 로그아웃" 기능 없음(로그아웃은 제시한 단일 토큰만 무효화). 위 재사용 탐지 부재와 결합 시 도난 지속성↑. **트리거(언제):** 보안 기능 요구·다기기 세션 관리 도입 시. **회수:** 사용자별 활성 refresh 조회/일괄 삭제 API + 선택적 세션 상한. (blind)
- **CSRF — 쿠키 인증에 CSRF 토큰/방어 부재** [apps/api/app/auth/router.py:45-68] — access를 httpOnly 쿠키(SameSite=lax)로도 수용. lax는 cross-site POST는 막으나, 향후 상태변경 GET 엔드포인트가 생기면 ambient 쿠키로 CSRF 노출. login/refresh는 POST라 현재 안전. **트리거(언제):** 쿠키 인증되는 변경 엔드포인트(특히 GET) 등장 = SDK 1.9 프론트 통합. **회수:** CSRF 토큰(double-submit/SameSite=strict 조합) 전략 확정. (blind)
- **SameSite=lax가 cross-site 웹 refresh 쿠키 전송 차단** [apps/api/app/auth/router.py:56,65] — 웹앱과 API가 다른 site(예: `app.desknow.com` ↔ `api.desknow.com`)로 배포되면 `SameSite=Lax` 쿠키는 XHR `POST /refresh`에 전송되지 않아 쿠키 폴백 refresh가 조용히 실패(본문 토큰에 의존 = httpOnly 무의미화). same-site 배포에서만 동작. **트리거(언제):** cross-site 배포 토폴로지 + 프론트 통합(1.9). **회수:** cross-site면 `SameSite=None`+`Secure`로 전환하거나 same-site 배포(서브패스/동일도메인) 확정. (edge)
- **쿠키 `Secure=True` 무조건 하드코딩 → 로컬 HTTP 개발 쿠키 폐기** [apps/api/app/auth/router.py:51-68] — `secure=True`가 프로덕션엔 옳으나 무조건이라, `http://localhost` 개발/비-TLS 환경에서 브라우저가 쿠키 저장을 거부 → 웹 쿠키 인증 플로우가 무에러로 비동작(본문 토큰만 동작). 1.8은 프론트가 없어 미발현. **트리거(언제):** SDK 1.9 웹 프론트가 쿠키 인증을 로컬에서 소비할 때. **회수:** `COOKIE_SECURE` 설정(기본 True) env 게이트 또는 요청 스킴 기반 결정. 기존 1.8 cross-origin 쿠키 검증 defer와 함께 회수. (blind+edge)
- **유니코드 이메일 정규화 한계 → 시각적 동일 계정 중복** [apps/api/app/auth/service.py:50,73] — 정규화가 `.strip().lower()`뿐이라 NFC/NFKC 차이·일부 스크립트의 `lower()≠casefold`·`+`-주소로 사람이 "같다"고 읽는 이메일이 별개 행으로 가입 가능(register·authenticate가 동일 변환이라 로그인은 행별 동작 → 인증 우회는 아님). UNIQUE는 코드포인트 정확비교라 못 잡음. **기존 1.7 defer(정규화 불변식)와 동일 계열.** **회수:** 모델 레벨 정규화(casefold/NFKC) 또는 `lower(email)` functional unique index. (blind)
- **`delete_cookie` 속성 불일치(secure/samesite 미전달)** [apps/api/app/auth/router.py:71-74] — `_clear_auth_cookies`가 `path`만 맞추고 set 때의 `secure=True`/`samesite=lax`/`httponly`를 전달하지 않는다. 일부 엄격 브라우저는 속성 패리티(특히 Secure/SameSite)가 없으면 만료 Set-Cookie로 원본을 덮어쓰지 못함. **서버측 행 삭제가 최종 권위라 세션은 무효화되므로 무해**(브라우저에 무용 쿠키만 잔류). **회수:** `delete_cookie`에 set과 동일한 `secure`/`samesite`/`httponly` 전달. (edge)
- **`authenticate_user` 타이밍 enumeration (Argon2 스킵)** [apps/api/app/auth/service.py:75-80] — `user is None`이면 `or` 단락으로 `verify_password`(Argon2, 수십 ms)를 건너뛰어 미존재 이메일은 빠르게, 존재·틀린 비번은 느리게 응답 → 응답 시간차로 계정 존재 추정 가능. 응답 코드/메시지는 단일화돼 *내용* enumeration은 막지만(AC2 PASS) *타이밍* enumeration은 누출. AC2의 명시 목표("계정 존재 노출 방지")를 부분 약화. **회수:** `user is None`일 때 모듈 상수 더미 Argon2 해시로 `verify_password`를 1회 수행해 시간 균등화(상수시간 비교). **(권장 — 다음 리뷰/하드닝에서 우선 검토 가치.)** (blind+edge)
- **테스트 충실도: `FakeSession.exec`가 User select 술어를 무시** [apps/api/tests/auth/test_service.py:69-74] — `exec`가 `RefreshToken` 쿼리만 whereclause로 분기하고 `User` 쿼리는 술어와 무관하게 `existing`을 반환 → `authenticate_user`의 이메일 `.strip().lower()` 정규화·조회 컬럼이 상시 스위트에서 공허 통과(정규화를 제거해도 단위 테스트는 통과). 실 술어·정규화 왕복은 `TEST_DATABASE_URL` 통합테스트(라이브DB 없으면 skip)에만 존재. **기존 1.7 동일 계열.** **회수:** FakeSession이 User도 이메일로 필터링하도록 보강하거나, 정규화를 직접 단언하는 테스트 추가. (edge)

## Deferred from: code review of story-1.9 (2026-06-15)

- **operationId `{tag}_{name}` 충돌 위험(향후 라우터)** [apps/api/app/main.py:67] — `custom_generate_unique_id`가 `route.tags[0]`+`route.name`만 사용하고 유일성을 강제하지 않는다. 현재는 충돌 0(검증)이나, 같은 첫 태그·같은 핸들러 함수명을 가진 미래 라우터(예: 두 `create` 핸들러 모두 `rooms` 태그)는 동일 operationId를 내고 → hey-api가 SDK 함수 하나로 병합/충돌. 두 드리프트 게이트(json↔SDK)는 operationId 유일성을 검사하지 않아 무검출. FastAPI 기본은 path+method를 붙여 회피. **트리거:** 멀티-라우트 도메인 라우터(E2+) 추가 시. **회수:** 생성 함수에 중복 감지·실패 가드 추가, 또는 충돌 시 path/method 보강. (blind+edge)
- **prod env 누락 시 localhost:8000 무음 폴백 + baseUrl 미검증** [apps/web/src/lib/api-client.ts, apps/admin/…, apps/mobile/…] — `process.env.X ?? "http://localhost:8000"`는 env 누락 시 프로덕션에서 localhost를 가리켜 전 호출이 런타임 실패(기동 에러 없음). 또한 빈 문자열(`X=`)은 nullish가 아니라 `??`를 우회해 `baseUrl=""` → 현재 origin에 상대 URL로 호출; trailing-slash(`...:8000/`)는 `//api/v1` 중복 가능. **명시 OUT 스코프(배포준비).** **회수:** 배포 준비 시 env fail-fast 검증(누락/빈값/형식) + baseUrl 정규화(trailing-slash strip). (blind+edge)
- **web/.env.local 잔존 Phase-0 Kakao JS 키·스파이크 변수** [apps/web/.env.local] — `NEXT_PUBLIC_KAKAO_JS_KEY=<하드코딩 키>`·`NEXT_PUBLIC_SPIKE_API_BASE` 등 Story 1.3 throwaway 스파이크 잔재가 1.9가 편집한 파일에 남아있다. `NEXT_PUBLIC_*`는 의도적 브라우저 노출 키라 비밀 누출은 아니나(도메인 화이트리스트로 보호), 죽은 설정·키 위생 차원. **1.9가 도입한 게 아니라 1.3 유래(pre-existing).** **회수:** Phase-0 스파이크 정리 스토리/E7 실이관 시 잔재 일괄 제거, 키 회전 검토. (blind)
- **configureApiClient import 부작용·`sideEffects` 미선언·직접 import 설정 우회** [packages/api-client/src/index.ts:24, apps/*/src/lib/api-client.ts] — `configureApiClient({baseUrl})`가 모듈 평가 시 부작용으로 싱글턴 `client.setConfig`를 호출한다. (a) 패키지가 `"sideEffects"`를 선언하지 않아 타입만 import하는 소비처에서 공격적 트리쉐이킹 시 설정 호출이 제거될 수 있고, (b) 앱 코드가 래퍼(`@/lib/api-client`) 대신 `@desknow/api-client`를 직접 import하면 `client`가 미설정(baseUrl 기본값) 상태로 호출 → 상대 URL. (c) Next 서버/클라이언트 모듈 인스턴스 분리로 환경별 설정. 현재 정적 baseUrl이라 무해하나 패턴이 함정. **회수:** 실인증 소비 스토리에서 lib 패키지 `"sideEffects": ["./src/index.ts"]` 선언 또는 명시 init 패턴, 직접-import 금지 lint 검토. (blind+edge)
- **check-drift.mjs가 `src/generated` 부재 시 ENOENT 크래시** [packages/api-client/scripts/check-drift.mjs:30,72] — `collectFiles(committedDir)`가 `existsSync` 가드 없이 `readdirSync` → 생성 디렉터리가 삭제/미커밋이면 의도된 "SDK 드리프트 검출" 메시지가 아니라 불투명한 ENOENT 스택트레이스로 죽는다(`finally`의 `rmSync`는 실행). 저가치 방어 항목. **회수:** committedDir 존재 검사 후 `fail(REGEN_HINT)`로 분기, 생성 결과 `fresh.size>0` 단언. (blind+edge)
- **turbo `test` 태스크가 생성기 버전을 input 미포함 → stale 캐시 드리프트 은폐** [turbo.json] — Layer B(`check:drift`)는 `node_modules/@hey-api/openapi-ts`로 재생성하는데, 패키지 내부 파일을 안 건드리고 생성기 버전만 범프(package.json/lock)하면 `test` 태스크 기본 해시가 이를 포함하지 않아 캐시된 PASS가 제공될 수 있다(`build`는 `.env*`를 input에 명시했으나 `test`는 무처치). 정확 버전 핀(0.98.2)으로 실무상 완화됨. **회수:** `test` 태스크 `inputs`에 생성기/락 의존 명시 또는 `cache: false`. (edge)
- **env 산출물 gitignore 추적 불가 + 신규 Python 코드 mypy 스코프 밖** [.gitignore, apps/api/pyproject.toml] — File List가 산출물로 든 `apps/web/.env.local`·`apps/admin/.env.local`·`apps/mobile/.env`은 모두 루트 `.gitignore`(`**/.env.local`·`*.env`) 대상이라 git 도입 시 추적되지 않는다(현재 git 미초기화라 무영향). 또한 `pyproject.toml` mypy `files=["app"]`이라 신규 `scripts/export_openapi.py`·`tests/test_openapi_export.py`는 타입체크 밖(현재 무해). **회수:** git 초기화(배포준비) 시 `.env.example`만 추적 확인 + mypy `files`에 `scripts`/`tests` 포함 검토. (edge+auditor)

## 회수 라우팅 결정: Epic 2 즉시회수 deferred(P1·P2·P3) (2026-06-15, Story 2.1 create-story)

Epic 1 회고(§6)가 P1·P2·P3를 "2.1에 의무 편입"으로 적었으나, **Story 2.1을 ACs로 분해하니 데이터 모델 + 마이그레이션 + 순수 슬롯 도출 함수까지이고 HTTP 엔드포인트·쓰기 서비스·권한 게이트가 없다**(공간 등록=provider 인증 쓰기는 Story 2.2). 트리거가 실제 발생하는 위치 기준으로 KTH가 라우팅 확정(2026-06-15, "P1만 2.1, P2·P3는 2.2 명시 편성" 선택). **미래 에픽 punt가 아니라 트리거 실발생 스토리(2.2, 둘 다 Epic 2 내)로의 명시 편성 = 정책의 재-defer 금지 위반 아님.**

- **P1 (네이밍 63자 → 복합 제약 명시 단축명, 출처 1.4)** → **Story 2.1에서 회수 완료(이 스토리 AC4/Task)**. `business_hours`·`holiday_exceptions`의 복합 UNIQUE를 명시 단축명으로 부여 + 각 제약명 ≤63자 검증 + 정책 확립(향후 3~4컬럼 복합 제약은 명시 단축명 의무).
- **P2 (`IntegrityError` 과대캐치 → 제약명 식별 후 선별 변환, 출처 1.7 auth/service.py:41)** → **Story 2.2로 명시 편성**. 트리거 = rooms insert(provider가 공간 등록) 쓰기 경로 = 2.2. 2.1엔 쓰기 서비스가 없어 트리거 표면 자체가 없음. 2.2 create-story가 이 항목을 해당 스토리의 AC/Task로 회수할 것(rooms 쓰기 서비스의 IntegrityError 처리는 제약명 식별 후 선별 변환, 동시에 1.7 auth register_user도 동일 패턴으로 정합화 검토).
- **P3 (`role` DB CHECK/enum 부재 → defense-in-depth, 출처 1.7 auth/models.py:50)** → **Story 2.2로 명시 편성**. 트리거 = provider 권한으로 rooms를 *쓸* 때 role 신뢰 = 2.2. 2.1엔 권한 게이트가 없음. 또한 2.1이 신규 도입하는 `rooms.room_type`(open/private)도 같은 enum-CHECK 계열이므로 **2.2에서 role·room_type 두 enum 컬럼의 DB CHECK 하드닝을 한데 묶어 회수**(2.1은 둘 다 자유 문자열로 두고 검증은 2.2 Pydantic Literal 스키마 = 1.7 role 패턴과 동일). 2.2 create-story가 회수할 것.

## Deferred from: code review of story-2.3 (2026-06-15)

- **좌표 3종(`lat`/`lng`/`admin_dong_code`) 부분 수정 시 정합성 깨짐** [apps/api/app/rooms/schemas.py:127-129] — `RoomUpdateRequest`의 세 필드가 독립 Optional이라 `{"lat": 37.5}`만 PATCH하면 lng·법정동코드는 옛 위치로 남아 좌표↔행정동이 서로 다른 위치를 가리키는 불일치 레코드가 생길 수 있다. 스펙은 좌표를 client-trusted로 두는 설계(2.2 dismiss "client-trusted 좌표 by-design")라 즉시 차단 대상은 아니며, 프론트 폼(전용 후속 프론트 스토리로 이관)이 geocode 결과의 (lat,lng,b_code) 삼중을 묶어 전송한다. **트리거(언제):** 제공자 수정 프론트 폼 스토리 또는 좌표 서버검증 도입 시. **회수:** 좌표 삼중 원자 검증(셋 중 하나라도 오면 셋 다 요구) 또는 서버측 지오코딩 재검증. (blind)
- **`address`가 스키마에 노출되나 항상 silent-drop** [apps/api/app/rooms/service.py:194, apps/api/app/rooms/schemas.py:132] — `_NON_ROOM_COLUMN_FIELDS`에 `address`가 포함되고 Room에 컬럼이 없어, `{"address": "새 주소"}` PATCH는 200을 반환하지만 미저장·미반영이고 RoomPublic에도 미포함이라 클라이언트에 변경 실패 피드백이 전혀 없다. `RoomCreateRequest`/`create_room`(2.2)도 동일하게 address를 받고 버리는 대칭 설계라 본 스토리 신규 결함은 아니나, 스키마가 받는 필드를 조용히 폐기하는 footgun이다. **트리거(언제):** address를 실제 저장/표시해야 하는 요구가 생길 때(룸 상세 E3) 또는 스키마 위생 정리. **회수:** Room에 `address` 컬럼 추가(마이그레이션) 후 저장하거나, 스키마에서 `address`를 제거/명시 거부. create·update 양쪽 동시 정합화. (blind+edge)

## Deferred from: code review of story-3.1 (2026-06-15)

- **공개·무인증 가용성 엔드포인트의 무제한 전체 활성 룸 스캔(페이지네이션/bbox/레이트리밋 부재)** [apps/api/app/rooms/service.py:170-213, apps/api/app/rooms/router.py:102-112] — `GET /rooms/availability`는 인증 없이 `select(Room).where(is_active)`로 **전체 활성 룸**을 limit/offset/bbox 없이 집계해 단일 바레 리스트로 반환한다. N+1은 회피(벌크 IN 조회)했으나 응답 크기·계산량 상한이 없고, `active_room_ids`가 매우 크면 일부 드라이버의 바인드 파라미터 한도에도 근접할 수 있다. 인증이 없어 레이트리밋만이 방어선인데 그조차 후속. **스펙상 의도된 한계**(Dev Notes 결정 #2: "초기 지도 로드 범위(bbox) 안의 룸만 서버 계산하는 최적화는 3.5 반경 검색/데이터 증가 시점으로 미룬다", KTH 확정 2026-06-15) — MVP 룸 수가 작아 실질 위험 낮음. **트리거(언제):** Story 3.5 반경/공간 쿼리 도입 시 또는 데이터 증가 시점·배포 전 레이트리밋 도입. **회수:** 화면 bounds/bbox 기반 서버측 필터(공간 인덱스) + 공개 엔드포인트 레이트리밋. 🟡 **부분 회수(Story 3.5, 2026-06-16):** `GET /rooms/search`에 반경(Haversine Python) 필터를 추가해 **목록 도구의** 무제한 스캔을 반경으로 좁혔다. **잔존:** `GET /availability`(핀 색 집계)는 여전히 전체 활성 룸 스캔 — bbox/공간 인덱스·페이지네이션·레이트리밋은 데이터 증가/배포 시점(반경은 Python 거리 계산이라 활성 룸 전체를 조회한 뒤 필터하므로 SQL/PostGIS 공간쿼리 최적화는 동일 계열로 잔존). (blind+edge)
- **KST↔UTC 자정 요일 전환 경계의 집계 레벨 회귀 락 부재** [apps/api/app/rooms/service.py:166-167] — `today_in_tz(ROOM_TZ, now)`로 "오늘"을 KST 기준 계산하는 코드는 정확하나(UTC 15:00 이후 = KST 익일), **UTC 날짜와 KST 날짜가 갈리는 자정 경계 직전/직후에서 target_date의 요일이 바뀌어 영업/비영업이 뒤집히는 케이스**가 집계 경로 테스트로 고정돼 있지 않다. 단위 테스트(`test_morning_hours_map_to_previous_utc_date`)는 slot_start의 UTC 날짜 역전만 검증하고, 집계 테스트는 모두 이미 KST 월요일인 UTC 값만 쓴다. NFR-1 결정성의 핵심 경계라 회귀 락 가치가 있다. **트리거(언제):** 시간 규약 회귀 우려 시 또는 멀티-tz 도입(1.5). **회수:** `now=2026-06-14 14:59 UTC`(KST 일) vs `15:00 UTC`(KST 월)에서 요일 전환에 따른 remaining_slots 변화를 단언하는 집계 회귀 테스트 추가. (edge)
- **aggregate_availability의 `now` naive self-guard 부재** [apps/api/app/rooms/service.py:166] — `derive_slots`는 입력(naive reserved_starts·datetime holiday/target)을 fail-fast로 거부하나, `aggregate_availability`는 `now`에 대한 자체 가드가 없다. 라우터는 `now`를 주입하지 않아(`now_utc()` aware 보장) **현재 HTTP 경로로는 미도달**이며, naive `now`가 들어와도 `today_in_tz`→`_require_aware`가 ValueError를 던져 조용한 오작동은 아니다. 다만 그 ValueError는 전역 핸들러(DomainError·RequestValidationError만 등록)에 안 잡혀, 향후 HTTP 컨텍스트에서 호출되면 500이 될 수 있다. **트리거(언제):** Story 4.9가 예약 차감을 연결하며 `now`를 주입하는 경로가 생길 때. **회수:** `aggregate_availability` 진입부에 `_require_aware(current)` 또는 core/time 일관 가드 추가(derive_slots fail-fast 철학과 정합). (blind+edge)
- **derive_slots `wall.replace(tzinfo=tz)`의 비-KST tz DST 전환일 오프셋 위험** [apps/api/app/rooms/service.py:131] — `replace(tzinfo=tz)`는 fold/오프셋 정규화를 하지 않아 DST가 있는 타임존에서 전환일 벽시계→UTC 변환이 잘못된 오프셋을 줄 수 있다. ROOM_TZ=Asia/Seoul은 DST가 없어 **현재 무해**하나, `tz` 일반 파라미터로 비-KST tz를 주입하면 발현. **2.1 pre-existing**(derive_slots는 Story 2.1 자산)이며 기존 1.5 멀티-tz DST gap/fold defer와 동일 계열. **트리거(언제):** 멀티 타임존(룸 소재지별 tz) 도입 시. **회수:** DST tz 도입 시 `ZoneInfo` 정규화(`datetime(..., tzinfo=tz)` 직접 생성 또는 fold 명시) 전략 확정·경계 테스트. (blind)

## Deferred from: code review of story-3.3 (2026-06-15)

## Deferred from: code review of 3-5-반경-검색 (2026-06-16)

- **RadiusControl `role="radiogroup"` 화살표 키보드 내비게이션 미구현** [apps/web/src/features/list/RadiusControl.tsx:24] — 반경 프리셋 세그먼트가 `role="radiogroup"`+`role="radio"`+`aria-checked`+접근 이름(`반경 N km`)+`tap-target`(≥44px)으로 **기본 접근성은 충족**하나, native radiogroup 관례인 roving tabindex(단일 탭 스톱) + Arrow 키 이동은 없다(각 옵션이 개별 `<button>`이라 Tab으로는 순회 가능). 스펙 범위 결정 #6 "접근성 있는 radiogroup 세그먼트 버튼"은 기본 수준 충족이라 사양 위반 아님. **트리거(언제):** a11y 하드닝/키보드 UX 패스(접근성 정밀화 요구 시). **회수:** roving tabindex + ArrowLeft/Right 핸들러로 WAI-ARIA radiogroup 키보드 패턴 완성(검색방식 토글·지도/목록 토글에도 동일 적용 검토). (auditor)
- **region 검색 경로 결과 정렬 키 부재(비결정적 표시 순서)** [apps/api/app/rooms/service.py:443-450] — `search_rooms`의 반경 경로는 거리 오름차순 정렬(AC1③)하나 **행정동(region) 경로·전체 경로는 `active_rooms`의 DB 반환 순서를 그대로 사용**(정렬 키 없음)이라 목록 표시 순서가 비결정적일 수 있다. **pre-existing 3.4 동작**(3.5가 만든 결함 아님)이고 거리 표시·정렬은 범위 결정 #4로 반경에만 적용. **트리거(언제):** 목록 순서 일관성(이름/등록순 등) 요구 또는 페이지네이션 도입 시. **회수:** region/전체 경로에 안정 정렬 키(예: 이름 또는 created_at) 부여 — 페이지네이션 deferred와 함께 회수. (blind)
- **`useGeolocation` getCurrentPosition 타임아웃/정확도 옵션 부재** [apps/web/src/features/list/useGeolocation.ts:49] — 3번째 인자 `PositionOptions`(`{timeout, maximumAge, enableHighAccuracy}`) 미전달로, 콜백이 영영 안 오는 환경(일부 webview)에서 `status`가 `prompting`에 고정되어 ExploreView가 "현위치를 확인하고 있어요…"를 무한 표시할 수 있다(막다른 화면은 아니나 진행 불가). **스펙 Dev Notes에 명시 deferred**(`getCurrentPosition` 타임아웃/정확도·watchPosition → 위치 UX 하드닝)이며 **기존 MapView L50-60 동일 계열 defer(이 파일 위 story-3.2 항목)**. **트리거(언제):** 위치 UX 하드닝 스토리. **회수:** `{ timeout, maximumAge }` 옵션 추가 + 타임아웃 시 denied 폴백(행정동 유도) — MapView와 공유 훅으로 통합 검토. (blind+edge)

> 참고(재기록 아님): 공개 검색 무제한 전체 활성 룸 스캔 + O(N) Python haversine 성능 항목은 이미 본 파일 **story-3.1 L136**·**story-3.2 L5**에 3.5 부분 회수 주석과 함께 추적 중(SQL/PostGIS 공간쿼리·공간 인덱스·페이지네이션·레이트리밋 잔존) — 중복 신규 엔트리 생략.

## Deferred from: code review of story-3.3 (2026-06-15)

- **`["rooms"]` 키 프리픽스 광역 무효화 풋건** [apps/web/src/features/map/useRoomSummary.ts:16] — 3.3이 추가한 단일 룸 요약 훅 키 `["rooms", roomId]`가 3.2의 `["rooms","map"]`·`["rooms","availability"]`와 프리픽스를 공유한다. 향후 누군가 `invalidateQueries({ queryKey: ["rooms"] })`(프리픽스 매칭)를 호출하면 의도와 달리 **전 룸 요약 + 지도 + 가용성 캐시가 한꺼번에** 무효화된다(불필요한 대량 refetch). 현재 그런 광역 무효화 호출은 코드에 없어 무해(잠재적). 키 컨벤션 자체는 3.2 유래. **트리거(언제):** 즐겨찾기 배선(3.7)·예약 후 가용성 갱신(4.9) 등 캐시 무효화를 도입하는 스토리. **회수:** 무효화 도입 시 키를 더 구체화(`["room-summary", roomId]` 등 별 네임스페이스)하거나 무효화 호출을 정확한 키로 한정. (blind)

## 회수: Story 3.6 위치 권한 거부 우회 (dev-story, 2026-06-16)

- ✅ **"권한 거부 시 행정동 콤보 우회 = 3.6" 회수 완료** (3.2 sprint-status·3.5 deferred forward) — `ExploreView`가 `useGeolocation`을 마운트 즉시 단일 소유해, 위치 `denied`/`unsupported` 확정 시 진입을 **지도 대신 행정동 목록으로 자동 우회**(`viewMode=list`·`searchMode=region`) + "현재 위치를 못 받았어요. 동네를 골라서 찾아볼게요." 안내한다(세션당 1회·`useRef` 비파괴 가드 — 수동 지도 토글을 덮어쓰지 않음). Epic 3 탐색 "막다른 화면 금지"의 위치 거부 셀(EXPERIENCE L141)을 닫음.
- ✅ **"MapView 위치 로직 공유화는 선택적 후속" 회수 완료** (3.5 Dev Notes / 본 파일 story-3.2 L8 "MapView.tsx:50-60 getCurrentPosition" 항목) — `MapView`의 인라인 `navigator.geolocation.getCurrentPosition` + 자체 `locationDenied` state를 **제거**하고 `coords`/`locationDenied` **prop 주입**으로 전환했다. 위치 신호 단일 출처가 `useGeolocation`(ExploreView 소유) 하나로 통합 — **위치 요청 2곳→1곳**(브라우저 권한 프롬프트도 1회). 따라서 본 파일 **story-3.2 L8**(MapView getCurrentPosition timeout)·**story-3.5 L147**(useGeolocation timeout) 두 항목은 이제 **단일 지점(`useGeolocation`)으로 합쳐졌다** — 아래 잔존 항목으로 통합.
- 🔶 **잔존(위치 UX 하드닝 — 단일 지점):** `useGeolocation`의 `getCurrentPosition` 3번째 인자 `PositionOptions`(`{timeout, maximumAge, enableHighAccuracy}`)·`watchPosition` 미구현. 콜백이 영영 안 오는 환경(일부 webview)에서 `prompting` 고정 → ExploreView가 지도 유지(서울 폴백 + 핀 표시이므로 **막다른 화면은 아님**, 자동 우회만 미발화). **트리거:** 위치 UX 하드닝 스토리. **회수:** `{ timeout, maximumAge }` 추가 + 타임아웃 시 `denied` 폴백(→자동 우회 발화). (3.2/3.5 동일 계열 — 이제 한 곳)
- 🔶 **잔존(E7):** 모바일(RN) 위치 권한 우회(OS 권한 다이얼로그·EXPERIENCE L38)는 본 스토리 범위 밖(웹만). E7 모바일 본격 개발에서 RN geolocation + OS 권한 거부 시 동일 행정동 우회 패턴 적용.
- 🔶 **잔존(선택적 리팩터):** 안내 카피 "현재 위치를 못 받았어요. 동네를 골라서 찾아볼게요."가 3표면(MapView 배너·반경 거부 카드·자동 우회 안내)에 중복. 표시면이 달라 무해하나 상수화 가능.

## Deferred from: code review of 4-2-상세-정보-화면 (2026-06-16)

- **`isRoomNotFound`가 hey-api throw 본문 형상에 결합 — HTTP 상태 폴백 없음** [apps/web/src/features/detail/RoomDetail.tsx:43-53] — 404 분기는 throw된 에러가 객체이고 최상위 `detail.code === "ROOM_NOT_FOUND"`일 때만 성립한다. 현재 생성 SDK(hey-api `throwOnError`)가 파싱된 에러 본문 `{detail:{code,message}}`을 그대로 throw하는 실측 동작에 맞춰져 있고 테스트(`RoomDetail.test.tsx:230`)도 그 형상을 mock해 **현재 정확**하다(스펙 프리플라이트 #4②가 명시 수용). 다만 HTTP 상태(404)가 아닌 본문 코드에만 의존해, 클라이언트 라이브러리 업그레이드나 에러 계약 변경 시 진짜 404가 "정보를 못 불러왔어요"(일반 실패)로 오분류되고 재시도가 404를 반복하는 소프트 루프가 될 수 있다. **트리거(언제):** hey-api 버전 변경·에러 응답 계약(1.5 ErrorResponse) 변경·404 UX 회귀 우려 시. **회수:** `error.status === 404` 또는 `error.body?.detail?.code` 같은 방어 폴백을 추가해 본문 형상 단일 의존 제거(실측 후). (blind+edge+auditor)
- **자정 KST 경계에서 서버 remaining_slots와 클라 todayBusinessHours "오늘" 불일치 가능** [apps/web/src/features/map/roomSummary.ts:43-54, apps/web/src/features/detail/RoomDetail.tsx:80-81] — 예약 가능 배지는 서버가 요청 시점 KST "오늘"로 계산한 `remaining_slots`(`summaryStatus`)를 쓰고, 영업시간 줄은 클라가 렌더 시점에 `Intl.DateTimeFormat("Asia/Seoul")`로 "오늘 요일 행"을 재선택(`todayBusinessHours`)한다. 자정 직전 응답이 자정 직후 렌더되거나 캐시가 자정을 넘겨 표시되면 둘이 다른 요일을 가리켜 "오늘 영업 HH:MM"과 배지가 어긋날 수 있다(예: "오늘 휴무" + 비-마감 배지). **3.3 RoomSheet에서 동일 패턴을 상속**(본 스토리 신규 결함 아님), 발현 창이 ~1분/자정 횡단 캐시로 좁다. **트리거(언제):** 시간 경계 회귀 우려·캐시 staleTime 정책 손볼 때. **회수:** 영업시간 줄도 서버 신호(예: 서버가 todayWeekday/오늘 영업행을 응답에 포함)로 단일화하거나, 자정 횡단 시 재조회 무효화. (edge)
- **RoomLocationMap effect deps에 `name` 포함 → name-only 변경 시 지도 전체 재생성** [apps/web/src/features/detail/RoomLocationMap.tsx:71] — useEffect deps가 `[lat, lng, name]`인데 `name`은 핀의 aria-label(`createPinContent`)에만 쓰인다. `name`만 바뀌면 cleanup(`innerHTML=""`·overlay `setMap(null)`)이 돌고 `new kakao.maps.Map(...)`을 다시 만든다(카카오 Map은 `destroy()`가 없어 innerHTML 비우기로만 정리 — MapView 선례). 상세는 단일 룸이라 name이 안정적이라 사실상 1회지만, 백그라운드 refetch가 룸명 변경을 반환하면 라벨-only 변경에 지도 전체가 깜빡 재생성된다. **트리거(언제):** 지도 재생성 비용/깜빡임이 문제될 때. **회수:** deps를 `[lat, lng]`로 좁히고 핀 라벨은 별도 effect/overlay 갱신으로 in-place 업데이트. (blind+edge)
- **미니 지도 로드 실패 시 재시도 어포던스 없음(loadKakaoMaps는 재시도 지원)** [apps/web/src/features/detail/RoomLocationMap.tsx:74-83] — SDK 로드 실패 시 지도 영역이 "지도를 못 불러왔어요." 정적 자리로 degrade되나 재시도 버튼이 없다. `loadKakaoMaps`는 실패 시 `loadPromise=null`로 비워 재시도를 의도적으로 지원(`kakao-map.ts:55,68`)하고 페이지 레벨 일반 실패에는 "다시 시도"가 있으나, 미니 지도 영역의 에러 상태는 deps(`[lat,lng,name]`) 안정으로 effect가 다시 안 돌아 마운트 수명 동안 일시 실패가 영구 degrade로 남는다. **AC3는 "지도 영역만 graceful degrade(작은 자리 표시)"만 요구**해 사양 위반은 아님(선택적 개선). **트리거(언제):** 지도 로드 신뢰성 UX 하드닝. **회수:** 에러 자리에 재시도 버튼(상태 리셋 + loadKakaoMaps 재호출) 추가. (edge)
- **온라인 재조회 일시 실패 + 캐시 존재 시 에러-우선 전체화면이 캐시-우선보다 우선(앱 전역 정책)** [apps/web/src/features/detail/RoomDetail.tsx:76,88,121, apps/web/src/features/map/RoomSheet.tsx:121] — `showError = isOnline && isError`가 `data ?` 분기보다 먼저라, TanStack이 보존한 직전 성공 `data`가 있어도 마운트 시 `refetchOnMount:'always'` 재조회가 transient 실패하면 "정보를 못 불러왔어요" 전체화면으로 전환돼 사용 가능한 캐시를 버린다. RoomDetail·RoomSheet 양쪽이 공유하는 **앱 전역 에러-우선 패턴**이고 AC5는 캐시 유무를 구분하지 않아 사양 위반은 아니다. **KTH 결정 2026-06-16:** 현 동작 유지(앱 일관)·RoomDetail만 고치지 않음 — 캐시-우선 표시는 한 표면만 바꾸면 불일치가 생기므로 cross-cutting 정책으로 분리. **트리거(언제):** 캐시-우선 UX 정책을 앱 전역에 도입하기로 할 때(에러 표면 일괄 점검). **회수:** RoomDetail+RoomSheet(및 동형 표면) 동시에 "data 존재 시 상세 유지 + 상단 '최신화 실패·다시 시도' 배너"로 통일하거나, 에러-우선을 명시 정책으로 문서화. (review decision)

## Deferred from: code review of 4-4-연속-슬롯-선택 (2026-06-16)

- **자정 KST 경계 끝/시작 슬롯 라벨 표기** [apps/web/src/features/reservation/slots.ts:178(formatHourKorean)·188-191(endInstantUtc)·200-211(selectionLabels)] — 23시 슬롯을 끝으로 하는 선택은 끝 라벨이 +1h = "00:00"/SR "0시"(익일 표식 없음)로, 00:00 시작 슬롯은 "0시"로 표기된다. `rangeLabel "23:00–00:00"`·`announcement "...부터 0시까지"`가 익일 구분 없이 역순처럼 읽힐 수 있다. **사양 위반 아님** — 4.3 상속 defer(deferred-work L189 계열)이며 영업은 같은 날 0~24시(architecture L194)라 끝 슬롯이 자정을 넘기는 실발현이 없다. 테스트(slots.test.ts:145-152)는 포맷이 throw 없이 안정 산출됨만 단언(deferred로 명시). **트리거(언제):** 영업시간이 자정 횡단을 허용하게 바뀌거나(정책 변경) 끝-라벨 익일 표기 UX 하드닝 시. **회수:** 끝 인스턴트가 시작과 다른 KST 날짜면 "익일 00:00" 또는 "24:00" 표기 분기 추가. (blind+edge)
- **드래그 확장의 `setPointerCapture`+`onPointerEnter` 의존 — 실기기 터치 검증 필요** [apps/web/src/features/reservation/SlotGrid.tsx:107-122] — `handlePointerDown`이 원점 버튼에 포인터를 캡처한 뒤 형제 버튼의 `onPointerEnter`로 연속 구간을 확장한다. W3C Pointer Events 상 포인터 캡처가 활성이면 캡처 대상 외 요소의 pointerenter/leave가 억제될 수 있어, 실 터치 브라우저에서 **드래그 확장이 조용히 깨질 위험**이 있다(첫 슬롯만 선택되고 끌어도 안 늘어남). jsdom은 `setPointerCapture` no-op 폴리필이라 vitest(182 통과)가 이 footgun을 못 잡는다 — 실기기 미검증. **KTH 결정 2026-06-16(code review):** 자동 헤드리스 실행에선 실기기 검증이 불가하므로 현행 코드를 동결하고 후속으로 명시 기록. **탭·키보드는 1급 접근성 경로로 정상 보장**되어 드래그가 깨져도 막다른 화면은 아님. **트리거(언제):** 터치 기기(모바일 사파리/크롬·태블릿) 또는 E7 모바일 개발 시 드래그 실동작 확인. **회수:** 드래그를 `pointermove`+`document.elementFromPoint`로 현재 슬롯을 좌표 산출하도록 재작성(캡처 유지) + 비버튼 위 `pointerup`/그리드 이탈 시 `draggingRef` 확실히 종료(window pointerup 폴백). (blind+edge)

## Deferred from: code review of 4-5-즉시-예약-결제-없음 (2026-06-16)

- 서버측 슬롯 연속성/정렬 미강제 — 가용 슬롯이면 비연속 조합도 POST /rooms/{room_id}/reservations가 수용 [apps/api/app/reservations/router.py:89]. 연속성은 4.4 클라가 강제하고 서버는 멤버십만 검증(범위결정 #2 의도). 무결성 위반은 아님(결제無·실제 가용 슬롯 점유). 서버측 연속성 강제가 필요하면 후속 하드닝으로 검토.
- ✅ **[Story 4.9 회수 완료]** 백그라운드 refetch로 슬롯 배열 길이는 유지된 채 내용만 바뀌면 stale selection이 제출될 수 있음 [apps/web/src/features/reservation/ReservationPanel.tsx:107]. 서버 신선검증/UNIQUE가 409로 최종 차단(→ generic 실패 배너). safeSelection은 길이 축소 시 IndexError만 방어. 실제 차감 반영·선택 무효화는 Story 4.9 seam. **회수:** 4.9가 `isSelectionStillAvailable`(slots.ts)로 `safeSelection`을 content-aware 확장 — 선택 구간 중 하나라도 비-available이면 선택을 비우고 중립 안내("선택한 시간 중 일부가 방금 예약됐어요. 다시 선택해 주세요.") 표시(render-time 파생·set-state-in-effect 회피). stale 선택이 제출 전에 무효화된다.
- 단절 중 제출 실패 후 재연결 시 stale isError가 generic 에러 배너로 뒤늦게 표출되고 버튼 라벨이 "다시 시도"로 점프 [apps/web/src/features/reservation/ReservationPanel.tsx:103]. 경미 UX 전이. 재연결(online 전이) 시 mutation.reset() 트리거 고려. 테스트 미커버(현 단절 테스트는 offline 고정만).
- 교차일/날짜 도출 검증이 단일 ROOM_TZ(Asia/Seoul·DST 없음) 전제에 묶임 [apps/api/app/reservations/schemas.py:64, router.py:83 target_date=slot_starts[0]]. 현재 KST·DST 부재로 안전. 멀티-tz/DST 룸 도입 시 target_date 단일 도출·교차일 판정 재점검 필요(rooms/service.py ROOM_TZ defer와 연동).
- 신선검증 set 비교의 오프셋 표현(Z vs +09:00)·마이크로초 회귀 가드 부재 [apps/api/app/reservations/router.py:89]. 현재 aware-datetime instant 동치로 정상 동작(derive_slots=정시 UTC, 요청=서버 UTC ISO). UTC 정규화(.astimezone(UTC)) 또는 비-Z 오프셋 회귀 테스트는 하드닝 항목.

## Deferred from: code review of 4-6-중복-예약-방지-동시성 (2026-06-17)

- ✅ **[Story 4.9 회수 완료 — 빈날 트리거 부분]** **충돌 직후 재조회 실패/빈날 전이 시 SLOT_CONFLICT 특화 카피 미표시** (LOW) — `apps/web/src/features/reservation/ReservationPanel.tsx:147-202`. 충돌 카피는 정상 슬롯 그리드 분기 안에만 렌더. 재조회 실패 분기(showError)나 빈날 전이 시 카피가 마운트되지 않음. 막다른 화면은 아님(재조회 실패=다시 시도 제공, 빈날=4.9 차감 미배선이라 현재 트리거 불가). **회수:** 4.9 차감으로 만석일이 실제 트리거(availableCount===0 → isEmptyDay) 됨을 vitest로 확인 — empty-day는 "이 날은 다 찼어요" + `next_available_date` 제안으로 처리(막다른 화면 아님). empty-day 분기에 SLOT_CONFLICT 특화 카피를 띄우지 않는 것은 **설계대로 수용**(empty-day 메시지가 더 명확 — Dev Notes "만석일 경로"). 재조회 실패(showError) 분기의 특화 카피 부재는 비차단 잔존(트리거: 충돌+재조회 동시 실패 — 후속 하드닝).
- **제출 in-flight 중 날짜 변경 시 stale selectedDate invalidate + 타 날짜 그리드에 충돌 배너** (LOW) — `apps/web/src/features/reservation/useCreateReservation.ts:41-57` / `ReservationPanel.tsx:116-130`. 제출 진행 중 달력 날짜를 바꾸면 캡처된 이전 날짜 키로 invalidate되고, 충돌 시 현재 날짜 그리드 아래 배너가 노출. 드문 상호작용·비크래시·self-heal. 필요 시 in-flight 중 날짜 변경 가드 또는 mutation abort 검토.
- **겹침 통합 테스트 공유 세션 점유 검증** (LOW, test-strength) — `apps/api/tests/integration/test_reservations_concurrency.py:194-209`. 커밋 후 동일 세션에서 `confirmed_slot_starts`를 읽음. `test_true_concurrent`(L150)처럼 독립 fresh Session 읽기로 바꾸면 세션 캐시 마스킹에 더 강인. skipif(TEST_DATABASE_URL) 한정 실행이라 우선순위 낮음.
- ✅ **[Story 4.9 회수 완료]** **selection 인덱스 드리프트(재조회로 slots 배열 내용 변경 시)** (LOW, acknowledged) — `apps/web/src/features/reservation/ReservationPanel.tsx:116-118` / `slots.ts`. bounds 가드는 크래시만 차단, 동일 길이·다른 내용 배열이면 의도 외 슬롯 POST 가능. 4.5/4.9에서 이미 명시 deferred(주석 L71-74). **회수:** 4.9가 `isSelectionStillAvailable` content 검사를 `safeSelection`에 추가 — "동일 길이·다른 내용"(예약 슬롯은 인덱스가 안 밀리고 status만 변경)에서도 구간 내 비-available을 잡아 선택을 무효화한다. 의도 외 슬롯 POST 경로가 닫힌다(slots.test.ts·ReservationPanel.test.tsx 커버).

## Deferred from: code review of 4-8-예약현황-히스토리-예약자 (2026-06-17)

- **render-time `now` 고정 — 긴 세션 중 취소 버튼 비활성 상태가 stale** (LOW) — `apps/web/src/features/reservation/ReservationList.tsx:114`(`const now = new Date()`) → `ReservationRow`/`reservations.ts:54 isCancellable`. `now`는 렌더 시 1회 캡처되고 타이머가 없어, 6h 경계를 세션 중 넘겨도 재렌더 트리거 전까지 취소 버튼이 활성으로 남는다. **설계대로**(스토리가 set-state-in-effect 회피 위해 render-time 파생 채택·반복함정 #7) — 클릭 시 서버가 409 `CANCEL_WINDOW_PASSED`로 최종 강제하고 FE가 graceful 처리(AC2)하므로 정합성은 보존, UI만 stale. **트리거(언제):** 예약현황 화면 실시간성 하드닝 또는 사용자 장기 체류 피드백 시. **회수:** `setInterval`(분 단위)로 `now`를 재산출하거나 react-query `refetchInterval`로 주기 재조회(set-state-in-effect 회피 위해 외부 store/구독 패턴). (blind+edge)
- **자정 KST 횡단 예약의 시간 범위 라벨 모호** (LOW) — `apps/web/src/features/reservation/reservations.ts:62-86`(`reservationDateLabel`=slot_starts[0] KST 날짜 / `reservationTimeRangeLabel`=min..max+1h, 시각만). 23:00 KST 시작 슬롯을 끝으로 하면 "6월 20일 23:00–01:00"처럼 익일 표식 없이 역순으로 읽힌다. 4-4 동일 계열 defer(deferred-work 4-4 "자정 KST 경계 라벨")와 동근. 영업이 같은 날 0~24시(architecture L194)라 실발현 없음. **트리거(언제):** 영업시간 자정 횡단 허용 또는 끝-라벨 익일 표기 UX 하드닝 시. **회수:** 끝 인스턴트 KST 날짜가 시작과 다르면 익일 표식 분기. (edge)
- **비연속 슬롯을 min..max+1h로 요약 — 실제 점유 시간 오표현** (LOW, acknowledged) — `apps/web/src/features/reservation/reservations.ts:80-86`. 스냅샷 `["09:00Z","12:00Z"]`(10–11시 갭)이 "09:00–13:00"으로 4h 연속처럼 표시. 4.4 UI가 신규 예약은 연속 강제하나 `create_reservation`(service.py:90 "연속/비연속 무관")·E7 챗봇은 비연속 허용. 스토리 deferred 후보로 이미 명시("비연속 슬롯 정밀 표시"). **트리거(언제):** E7 챗봇 예약 또는 비연속 예약 경로 도입 시. **회수:** 스냅샷 전체 리스트로 구간 분할 표시. (blind+edge)
- **`list_booker_reservations` created_at desc tie-breaker 부재 — 지난 섹션 순서 비결정** (LOW) — `apps/api/app/reservations/service.py:301-306`(`order_by created_at desc`만). 동일 `created_at` 인스턴트 2건(배치/픽스처·sub-second 충돌)이면 DB 반환 순서가 요청마다 흔들릴 수 있다. 화장식 무해. **트리거(언제):** 정렬 안정성 회귀 또는 페이지네이션 도입 시. **회수:** 2차 정렬키(`id` 등) 추가. (edge)
- **클럭 스큐 409 안내가 잔존 행과 공존** (LOW) — `apps/web/src/features/reservation/ReservationRow.tsx:125`(`cancel.isError` gated) / `useCancelReservation.ts:45-50`. 409는 서버에서 예약을 confirmed로 남기므로 재조회 후에도 행이 다가오는/confirmed로 잔존하고, `cancel.isError`가 리셋되지 않아 "방금 취소 가능 시간이 지났어요" 안내가 살아있는 행과 동시 표출. 비해로움·이중 신호 경미. **트리거(언제):** 취소 실패 UX 하드닝 시. **회수:** 재조회 settle 후 `mutation.reset()` 또는 행 이동 시 메시지 소거. (edge)
- **마이그레이션 백필이 status가 아닌 점유행 존재로 키잉** (LOW) — `apps/api/alembic/versions/f1a2b3c4d5e6_...py:65-83`(`SELECT FROM reservation_slots` 그룹, status 필터 없음). 취소/거절인데 점유행이 남은 불변식 위반 행이 있으면 종료 상태 예약에 비어있지 않은 스냅샷이 백필된다(docstring은 "[] 유지" 주장). `_release_slots` DELETE 불변식이 깨졌을 때만 발현 — 저확률. **트리거(언제):** 점유행 DELETE 불변식 위반 의심 시. **회수:** 백필 SELECT에 `JOIN reservations ... WHERE status='confirmed'` 추가. (edge)
- **손상된 스냅샷 문자열 1건이 목록 전체를 500으로** (LOW, defensive) — `apps/api/app/reservations/router.py:223-241`(행 루프에 per-row try 없음). 스냅샷에 비-ISO 문자열이 있으면 `ReservationListItem(...)` 구성이 `ValidationError`를 던져 `GET /reservations` 전체가 500(타 예약까지 숨김). `isoformat_utc`만 이 컬럼을 쓰므로 정상경로 도달 불가(DB 변조 한정). `room is None` 폴백은 방어됐으나 스냅샷 파싱은 미방어. **트리거(언제):** 스냅샷 쓰기 경로 추가(E7 등) 또는 방어 하드닝 시. **회수:** 행 루프 per-row try로 손상 행 스킵/폴백. (blind+edge)
- **마이그레이션 백필 분기가 시드 데이터로 미검증** (LOW, test-gap) — `apps/api/tests/.../test_reservations_migration` + migration f1a2b3c4d5e6:64-83. 테스트는 빈 DB에 `upgrade head` 후 시드하므로 백필 `UPDATE`(기존 confirmed→reservation_slots 스냅샷)가 0행에 실행되어 그룹핑/정렬 로직이 회귀로 안 잡힌다(라이브도 예약 0건). 런타임 스냅샷 경로는 test_service.py가 커버. **트리거(언제):** 백필 로직 변경 또는 백필 대상 존재하는 환경 마이그레이션 시. **회수:** 마이그레이션 *전* confirmed 예약을 시드한 뒤 upgrade해 스냅샷 백필을 단언하는 테스트 추가. (auditor)

## Deferred from: code review of 5-1-notifications-모델-인앱-배너-인프라 (2026-06-17)

- **다중 배너 동시 dismiss 옵티미스틱 previous 스냅샷 클로버** (MED) — `apps/web/src/features/notifications/useNotifications.ts:57-70`. 배너 N1·N2를 연속으로 빠르게 닫으면 각 dismiss의 `onMutate`가 그 시점 캐시 스냅샷(`previous`)을 잡는다. N1 호출이 실패하면 N1의 `onError`가 자기 시점 previous(N1·N2 둘 다 포함)로 `setQueryData`해 이미 제거된 N2까지 되살린다. `cancelQueries`는 in-flight refetch만 취소할 뿐 동시 mutation 간 스냅샷 경합은 막지 못함. `useToggleFavorite`(3.7) 동일 계열 deferred. `onSettled` invalidate로 결국 서버 진실에 수렴(자가치유)하나 그 사이 깜빡임/유령 배너 윈도우 존재. **트리거(언제):** 배너 다건 동시 표시가 흔해지거나 옵티미스틱 깜빡임 피드백 시. **회수:** 롤백을 id 단위 merge(실패 항목만 복원)로 전환하거나 dismiss를 항목별 격리. (blind+edge)
- **create_notification 진짜 동시삽입 경합 시 재조회 None → raw IntegrityError 500** (LOW) — `apps/api/app/notifications/service.py:59-71`. `uq_notifications_user_reservation_type` 위반 rollback 후 `select(...).first()`로 기존 행을 재조회하는데, 위반을 일으킨 행을 다른 동시 트랜잭션이 아직 커밋 안 했거나(READ COMMITTED) 그 사이 사라졌으면 `existing is None` → `raise`로 raw `IntegrityError` 전파(전역 핸들러 미포착 → 500). 라우터 docstring의 "클라 4xx→500화 금지(#5)" 정신과 충돌하는 도달 가능 경로지만 매우 희박하고, `create_notification`은 5.1 표면에 비노출(호출처=5.2/6.2/8.3 후속)이며 favorites `add_favorite` 동일 선례. 테스트 미커버(Fake는 고정 리스트라 None 경로 미발생). **트리거(언제):** 5.2/6.2/8.3이 create를 배선하고 동시 생성 부하가 생길 때. **회수:** None 경로를 DomainError(또는 멱등 성공)로 변환하거나 재시도, race 통합 테스트 추가. (blind+edge)
- **GET /notifications 실패 시 배너 무음 소실(에러 미surface)** (MED) — `apps/web/src/features/notifications/useNotifications.ts:22-32` + `apps/web/src/components/shell/InAppBannerSlot.tsx:14-30`. 로그인 상태에서 GET이 5xx/네트워크 단절로 실패하면 `useQuery`가 `error`/`isError`를 노출하지만 InAppBannerSlot은 `data`만 구조분해하고 `error`를 읽지 않는다. 거절/취소 통지(FR-24/FR-32)가 있어도 조회 실패 시 사용자는 빈 컨테이너만 보고 "통지 없음"과 구분 불가. 인프라 스토리라 의도적 deferral이나 GET 에러 경로가 코드·테스트 어디에도 없음. **트리거(언제):** 5.3 상태변경 통지 표시(가시성 중요) 또는 네트워크 단절 surface 하드닝 시([[terminology-network-disconnect-not-offline]]). **회수:** isError 시 NetworkNotice 미러("네트워크 연결이 끊겼어요" 류) 또는 재시도 표면. (edge)
- **FakeSession이 where/order_by 미모사 → list_pending 필터·정렬 단위테스트 미검증** (MED, test-strength) — `apps/api/tests/notifications/test_service.py` `FakeNotificationSession.exec`. exec가 where/order_by를 무시하고 보유 리스트를 그대로 반환하므로, `list_pending`의 `dismissed_at IS NULL` 필터·`created_at DESC, id DESC` 정렬이 실제로는 검증되지 않는다(거짓 통과). 멱등 create의 "진짜 race(existing None)" 경로와 CheckConstraint 위반 경로도 Fake 한계로 미커버. favorites/reservations 공유 FakeSession 선례와 동일 한계. **트리거(언제):** list_pending 쿼리 변경 또는 통합 테스트 인프라 도입 시. **회수:** SQLite/PG 통합 테스트로 dismissed 제외·정렬을 실 SQL로 단언. (blind)
- **reason 자유 문자열 미검증(BE CHECK/길이 0 + FE 매직스트링) — type와 방어 비대칭** (LOW) — `apps/api/app/notifications/models.py:98`(`reason: str|None`, CHECK 없음·AutoString 무제한) · `service.py`(`reason: str|None` 미검증) · `apps/web/src/features/notifications/NotificationBanner.tsx:26-29`(`reason === "rejected"/"cancelled"` 하드코딩). `type`은 DB CHECK+Literal 이중 방어인데 `reason`은 무방어 sink — 후속(6.2/8.3)이 오타('canceled')·임의 문자열을 넘기면 그대로 영속되고 FE는 조용히 generic 카피로 폴백(거짓 정상). 막다른 화면은 아님(generic 폴백 graceful). **트리거(언제):** 6.2/8.3이 create_notification을 배선할 때. **회수:** 호출처를 `reason: Literal['rejected','cancelled']`로 강제 + FE/BE가 reason 상수 공유(또는 reason CHECK 추가). (blind+edge)
- **GET 통지별 reservation+room 2-홉 N+1** (LOW, acknowledged) — `apps/api/app/notifications/router.py:66-69`. 통지 N건마다 `session.get(Reservation)` + `session.get(Room)`을 개별 호출. 스토리 Dev Notes에서 이미 인프라 수용 명시(목록 작음·favorites N+1 deferred 계열). **트리거(언제):** 통지 다수 누적 또는 목록 성능 회귀 시. **회수:** reservation_id/room_id IN 절 배치 조회 후 메모리 합성. (blind+auditor)

## Deferred from: code review of 5-5-후기-작성 (2026-06-17)

- **손상 슬롯 1건이 후기 영구-불가(409 루프) — FE/BE 손상 관용 비대칭** (LOW, defensive) — `apps/api/app/reviews/service.py:53-58`(`_latest_slot_end`가 첫 손상 항목에서 early-return `None` → 이미 찾은 유효 슬롯을 버리고 전체를 미완료로 판정) + `apps/web/src/features/reservation/reservations.ts:28-32`(`endMs`는 `new Date(garbage).getTime()`=NaN을 관용 → `NaN > now`=false → FE는 "이용 완료"로 표시하고 폼 노출). 결과: 슬롯 1건이 손상되면 FE는 작성 폼을 띄우나 BE는 409 `RESERVATION_NOT_COMPLETED`로 막고 `onError`가 `["reservations"]` 재조회 → 동일 데이터 → **친절-에러 무한 루프**(성공 경로 없음). **정상경로 도달 불가** — `Reservation.slot_starts`는 `isoformat_utc`만 쓰므로 항상 유효 `...Z`. 반복함정 #4 방어 목표(손상이 500을 내지 않게)는 충족됨(루프는 graceful 409). 4.8 "손상된 스냅샷 문자열 1건이 목록 전체를 500으로" 동근. **트리거(언제):** 외부/마이그레이션 슬롯 쓰기 경로 추가(E7 챗봇 등) 또는 방어 하드닝 시. **회수:** `_latest_slot_end`를 손상 항목 **skip**(전체 abort 아님)으로 + FE `endMs`에 `Number.isNaN` 가드를 둬 FE/BE 손상 관용을 정합화. (blind+edge)
- **`formatReviewDate` malformed `created_at` 미가드 — StarRating 클램프 방어와 비대칭** (LOW, defensive) — `apps/web/src/features/detail/ReviewSection.tsx:19-26`. `new Intl.DateTimeFormat(...).format(new Date(createdAtUtc))`에 가드가 없어, `created_at`이 비-ISO/빈 문자열이면 `new Date(bad)`=Invalid Date → `format()`이 `RangeError: Invalid time value`로 **ReviewSection 목록 렌더 전체를 크래시**(에러 바운더리 없음)시키거나 "Invalid Date"를 표기. 같은 도메인의 `StarRating`은 `Math.max/min/round`로 깨진 rating을 방어하나 날짜는 미방어(비대칭). 서버 `isoformat_utc` non-null 불변식상 **정상경로 도달 불가**. **트리거(언제):** `created_at` 쓰기 경로 변경 또는 방어 하드닝 시. **회수:** `const d = new Date(utc); if (Number.isNaN(d.getTime())) return "";`(또는 폴백 표기) 가드 추가. (blind+edge)

## Deferred from: code review of 7-1-멀티-llm-어댑터-레이어 (2026-06-17)

- **`LLMConfigurationError`에 DomainError 정규화/핸들러 부재 → 배선 시 raw 500** (LOW) — `apps/api/app/chatbot/llm/errors.py:24-30`, `base.py:122`. `create_chat_model`/`get_provider_spec`는 미등록 프로바이더·선택 키 미설정 시 plain `Exception`(LLMConfigurationError)을 던진다. `normalize_llm_error`는 업스트림 502만 만들고 설정 오류용 변환/핸들러는 없다(전역 핸들러는 DomainError만 포착). 요청 경로에서 호출되면 미포착 → 500. 현재 호출처 없음(어댑터는 인프라). **트리거(언제):** 7.3(그래프)/7.4(SSE)가 `create_chat_model`을 요청 경로에 배선할 때. **회수:** 호출처가 LLMConfigurationError를 502 아닌 적절 코드(설정 오류=500/503 또는 기동 검증)로 변환하거나 어댑터가 도메인 매퍼 제공. (blind)
- **`adapters/__init__.py`가 3개 SDK 전부 import — 하나 실패 시 기준(OpenAI) 동반 실패** (LOW, 현재 비발생) — `apps/api/app/chatbot/llm/adapters/__init__.py:8`. `_ensure_providers_loaded`가 패키지를 1회 import할 때 `anthropic`/`google.genai`/`openai`를 한꺼번에 import한다. 선택 프로바이더 SDK 하나라도 import 실패하면 패키지 import가 통째로 raise되어 기준 OpenAI 등록까지 막힌다(AC4 "기준은 정상" 정신과 충돌). 단 pyproject가 langchain-openai/anthropic/google-genai를 **하드 의존성으로 핀**하므로 SDK 누락 시나리오는 현 구성에서 발현 불가. **트리거(언제):** 선택 프로바이더 SDK를 optional extra로 분리하거나 견고성 하드닝 시. **회수:** adapter별 try/except로 등록 실패를 격리(기준은 항상 등록). (blind+edge)
- **`ProviderSpec.required` 미사용(데드 필드)** (LOW) — `apps/api/app/chatbot/llm/base.py:55`. openai=True/others=False로 선언되고 `test_provider_required_flags`가 값만 단언하나, 어떤 로직도 이 플래그를 읽지 않는다(기동 시점 필수 프로바이더 키 검증 등 미구현). **트리거(언제):** 기동 시점 프로바이더 헬스/키 검증 도입 시. **회수:** required=True 프로바이더의 키 부재를 기동 fail-fast 또는 헬스체크로 활용하거나, 미사용이면 제거. (blind)
- **`_ensure_providers_loaded` 비스레드세이프 + 등록-조회 경합** (LOW) — `apps/api/app/chatbot/llm/base.py:72-84`. `_PROVIDERS_LOADED` check-then-set에 락 없음. 동시 최초 호출 시 두 스레드가 `adapters`를 동시 import할 수 있고(파이썬 import 락으로 대체로 무해), `register_provider`가 `_REGISTRY`를 변경하는 도중 `list_providers`의 `dict(_REGISTRY)` 사본이 겹치면 이론상 "dictionary changed size during iteration" 가능. 실무 위험 낮음. **트리거(언제):** 동시 최초 사용 부하 또는 스레드세이프 하드닝 시. **회수:** 모듈 로드 시 1회 eager 등록 또는 lock 가드. (blind+edge)
- **`normalize_llm_error`가 `provider`를 사용자 메시지에 그대로 반영 + 잘못된 이름 api_key override 불투명 에러** (LOW) — `apps/api/app/chatbot/llm/errors.py:44-47`, `base.py:129-130`. `normalize_llm_error`는 `provider: str`를 사용자 502 메시지에 무필터 interpolate한다(공개 export — 신뢰 입력=레지스트리 키 가정). 또한 `create_chat_model(provider="google", api_key=...)`처럼 프로바이더와 다른 이름의 키를 override로 주면 `google_api_key`(주입) + `api_key`(override)가 둘 다 init_chat_model에 도달해 명확한 설정 오류가 아니라 불투명한 생성 에러를 낸다. **트리거(언제):** provider가 비신뢰 입력으로 흐르거나 override API를 공개 노출할 때. **회수:** provider를 레지스트리 키로 검증/고정, override의 잘못된 api_key 이름을 가드. (edge)

## Deferred from: code review of 7-2-문서-인제스트-파이프라인-pgvector (2026-06-17)

- **대용량 단일 문서 단일 배치 임베딩 → OpenAI 배치/토큰 한도 초과 시 문서 전체 적재 불가** (LOW, MVP 범위) — `apps/api/app/chatbot/ingest/service.py:85`. `embeddings.embed_documents(chunk_contents)`가 한 문서의 **전체 청크를 단일 배치**로 호출한다(배치 분할 없음). 수 MB 텍스트(chunk_size=1000 → 수천 청크)는 OpenAI 임베딩 단일 요청의 입력 개수/토큰 한도를 초과해 문서 전체가 실패하고, 청킹했음에도 임베딩 단계에서 통째로 실패해 영영 적재 불가. 문서 단위 부분 실패로 흡수되긴 함(배치는 안 깨짐). **트리거(언제):** 실 운영 문서(8.4/배포)에 대형 문서가 들어오거나 견고성 하드닝 시. **회수:** `embed_documents`를 N개 단위 서브배치로 나눠 호출(예: 100청크씩) 후 합성. (edge) ⏸️ **defer 유지(Story 8.4, 2026-06-18):** 현 MVP corpus(README+샘플)는 소형이라 미발현 — 8.4는 회수 대상에서 명시 제외(present 코퍼스 위험 0). 배포 시 실 운영 문서로 교체되면 재평가.
- **corpus에서 삭제/리네임된 문서의 stale 청크 GC 경로 부재(orphan 영구 잔존)** (LOW, 범위 밖) — `apps/api/app/chatbot/ingest/service.py:72`(`iter_corpus_files`는 현존 파일만 순회) · `store.py`. 멱등 대조·삭제는 `source_path` 기준이라, 파일을 리네임(내용 동일·경로 변경)하거나 corpus에서 삭제하면 옛 경로의 청크 행을 청소하는 경로가 없어 **orphan 행이 DB에 영구 잔존**한다. 7.2는 인제스트(적재 멱등)가 책임이고 코퍼스 동기화/GC는 범위 밖. **트리거(언제):** 8.4 인제스트 관리(FR-33) 또는 코퍼스 동기화 기능 도입 시. **회수:** 인제스트 시 현 코퍼스에 없는 source_path 청크를 정리(reconcile)하거나 8.4 관리 표면이 명시적 삭제/재동기화 제공. (edge) ✅ **회수 완료(Story 8.4, 2026-06-18):** `ingest_corpus`에 `reconcile=True` 단계 추가 — 적재 후 `store.delete_orphans(present_paths)`로 DB distinct `source_path` − 현 corpus 파일집합 = orphan을 DELETE하고 `IngestReport.removed`로 보고한다. ★footgun 가드: `present_paths` 공집합(corpus 부재/빈 디렉터리)이면 reconcile를 스킵해 전체 wipe를 방지하고, reconcile 실패는 best-effort(per-doc 적재 보존). CLI·admin 엔드포인트 동일 동작. 테스트(`test_ingest.py`: orphan 정리·빈 corpus 가드·reconcile=False·실패 best-effort).

## Deferred from: code review of 7-3-플로팅-챗봇-진입-세션-대화-유지 (2026-06-17)

- **MemorySaver 무한 메모리 증가 + 멀티워커/재시작 비일관성** (지속, MVP 전제) — `apps/api/app/chatbot/graph.py`. `MemorySaver()` 싱글톤이 모든 thread의 대화를 프로세스 메모리에 TTL/eviction 없이 영구 누적 → 사용자·디바이스 증가 시 단조 증가(OOM). 멀티워커/멀티인스턴스에서는 워커별 별도 메모리라 `get_transcript`/`reset_session`이 라우팅 워커에 따라 비일관(같은 thread가 워커 A엔 있고 B엔 없음). **트리거(언제):** 멀티워커/스케일 배포 또는 재시작 내성 요구 시. **회수:** 영속 PostgresSaver 채택(Alembic 단독소유 규약 충돌 정책 결정 후행 — Completion Notes·함정 #2에 이미 명시). (blind+edge)
- **전 대화 이력 매 턴 재전송·트리밍 없음 → 긴 대화 토큰/비용 폭증** (LOW) — `apps/api/app/chatbot/graph.py:52-55`. `call_model`이 매 턴 `state["messages"]` 전체를 LLM에 재전송. 트리밍/요약 부재로 대화가 길어지면 토큰 한도 초과·비용 증가. **트리거(언제):** 장시간 대화 세션 또는 비용 하드닝 시. **회수:** 메시지 윈도잉(최근 N개)·요약 노드 추가. (blind)
- **InMemorySaver 락 부재 → 동일 thread_id 동시 요청/리셋 경합** (LOW, MVP) — `apps/api/app/chatbot/service.py:128`. sync 핸들러가 threadpool 병렬 실행 + InMemorySaver는 defaultdict 무락. 동일 thread 동시 전송 인터리브 시 메시지 유실/순서뒤섞임, 전송중 reset(로그아웃)과 `delete_thread` 경합 시 부분삭제 또는 "폐기 후 부활"(AC5 위반 가능). 단일워커·프론트 isSending 직렬화로 영향 제한. **트리거(언제):** 멀티탭/멀티디바이스 동시성 또는 동시성 하드닝 시. **회수:** thread 단위 락 또는 영속 saver의 트랜잭션 의미론. (edge)
- **transient retry(with_transient_retry) 미배선 → 단발 일시장애도 즉시 502** (LOW) — `apps/api/app/chatbot/graph.py:54`. 그래프가 raw `chat_model.invoke`만 호출(7.1 retry 훅 미사용). 레이트리밋/타임아웃 1회 실패가 바로 502로 강등. **트리거(언제):** 회복력 하드닝 시. **회수:** `call_model`에 7.1 transient retry 래퍼 적용. (edge)
- **비-DomainError 예외(LLMConfigurationError 등) → raw 500 (표준 ErrorResponse 스키마 없음)** (LOW, 전역 패턴) — `apps/api/app/chatbot/service.py:82` / `app/main.py`. `register_exception_handlers`는 DomainError+RequestValidationError만 등록 → LLM_PROVIDER 미설정 등 config 오류가 표준 스키마 없는 raw 500으로 노출(router는 502만 선언). 본 스토리 신규 아닌 전역 핸들러 갭. **트리거(언제):** 전역 에러 핸들링 하드닝 시. **회수:** generic Exception 핸들러 추가 또는 config 검증을 기동 fail-fast로. (edge)
- **useSession 네트워크 에러 상태에선 로그아웃 초기화 미발화 (null 전이에서만)** (LOW) — `apps/web/src/features/chatbot/useChatbot.ts:126`. refresh 무효화가 5xx/네트워크 단절로 표출되면 `user`가 undefined(에러)로 남아 AC5 초기화 트리거 안 됨(null 전이에서만 발화). 단일 디바이스 가정·복구 시 401 발화로 영향 제한. **트리거(언제):** 네트워크 단절 UX 하드닝 시. **회수:** 에러 상태도 세션종료 후보로 분기하거나 명시 로그아웃 핸들러 정렬. [[terminology-network-disconnect-not-offline]] (edge)

## Deferred from: code review of 8-4-챗봇-문서-인제스트-관리 (2026-06-18)

- **orphan-GC 안전 플로어 부재 — 부분 동기화된 corpus면 잔존 청크 대량 삭제 가능** (LOW, MVP 미발현) — `apps/api/app/chatbot/ingest/service.py:144`. reconcile footgun 가드는 `present_paths` **공집합(파일 0)** 만 차단한다. corpus 디렉터리가 절반만 동기화돼 파일 1개라도 존재하면 `present_paths`가 비지 않아 `delete_orphans`가 실행되고, 그 1개를 제외한 DB의 모든 `source_path` 청크가 orphan으로 간주돼 DELETE된다(비율/절대 floor 같은 "구현 가능성 낮은 손실분" 가드 없음). 현 구성에선 `docs_corpus`가 repo에 커밋돼 항상 README+샘플이 상존하고 라우터가 `corpus_dir`를 `DEFAULT_CORPUS_DIR`로 하드코딩하므로 미발현. **트리거(언제):** 배포 시 corpus를 외부 디렉터리/부분 동기화 소스로 전환하거나 견고성 하드닝 시. **회수:** reconcile에 안전장치(삭제 비율 상한·절대 floor·dry-run 카운트 확인) 도입 — 메커니즘은 설계 결정 필요. (blind)
- **파괴적·과금 인제스트 엔드포인트에 레이트리밋·감사 로그·멱등키 부재** (LOW, 범위 밖) — `apps/api/app/admin/router.py:131`. `POST /admin/ingest`는 실 OpenAI 임베딩 비용을 유발하고 reconcile로 청크를 DELETE하는 파괴적 액션이나 `require_role("admin")` 외에 레이트리밋·멱등키·감사 로그가 없다(누가 트리거했는지·무엇이 removed됐는지 영속 기록 없음 — `removed`는 응답 본문으로만 반환). admin-only 시드 운영자 전제라 MVP 위험은 제한적. **트리거(언제):** 다중 운영자·배포 보안 하드닝 또는 비용 통제 요구 시. **회수:** 인제스트 트리거에 감사 로그(actor·removed 목록 영속)+레이트리밋/멱등키 도입. (blind)

## Deferred from: code review of 9-3-모바일-provider-챗봇-e2e (2026-06-19)

- **provider 원자 가입 비원자성 — register 성공 후 save 실패 시 떠도는 계정 + 재시도 409 막힘** (MEDIUM, 웹 패리티) — `apps/mobile/src/features/provider/RoomForm.tsx:222-239`. pendingSignup 모드는 `register.mutate(...{onSuccess: () => save.mutate(...)})` **순차**다(원자 트랜잭션 아님). register 성공(계정+자동로그인) 후 save가 실패(네트워크/422)하면 룸 없는 provider 계정이 남고, `clearPendingSignup()`은 save 성공 시에만 불리므로 pendingSignup이 잔존한다. 사용자가 재제출하면 여전히 pending 모드라 register를 **다시** 호출 → 이미 가입된 이메일 409(`EMAIL_TAKEN`)로 막혀 룸 생성을 완결할 수 없는 데드엔드. 동반: `ProviderGuard`가 pendingSignup 잔존 시 세션/역할 검증 없이 통과(단 백엔드가 provider 전용 엔드포인트로 역할 강제→데이터 누출 없음·방어심층 유지). **웹 RoomForm(L231-249)이 동일 패턴**이라 9.3 도입 결함 아님(verbatim 패리티). **트리거(언제):** provider 가입 신뢰성 하드닝 또는 실 운영 가입 실패 보고 시. **회수:** 백엔드 원자 register+room 엔드포인트, 또는 클라가 재시도 시 이미 인증됐으면 register를 스킵하고 save만 재시도 — **web+mobile 동시**([[web-mobile-parity-on-changes]]). (blind+edge)
- **네이티브 `NativeTabs.Trigger hidden`으로 pendingSignup→/provider/room push 도달성 미검증** (LOW, 검증 게이트) — `apps/mobile/src/components/app-tabs.tsx:49`. 로그아웃 상태의 provider 신규 가입자는 `isProvider=false`라 `provider/room` 트리거가 `hidden={true}`인데, SignupView가 `router.push('/provider/room')`으로 이 라우트에 가야 한다. 웹 변형(`app-tabs.web.tsx`)은 `display:none`으로 라우트 등록 유지가 **Playwright로 확인됨**. 그러나 네이티브 `unstable-native-tabs`의 `hidden` 트리거가 라우트 등록을 유지하는지는 EAS dev-build 없이 검증 불가 — 미유지 시 provider 가입 플로우 전체가 차단될 수 있다. **트리거(언제):** 첫 EAS dev-build/네이티브 검증 시(필수). **회수:** 네이티브 빌드에서 가입→룸폼 push 실동작 확인, 미도달이면 provider/room을 hidden 트리거 밖 Stack 라우트로 분리. (edge)
- **ComboSelect 그리드 밖 영업시간값 placeholder 표시·재선택 시 무음 덮어쓰기** (LOW, 교차편집 엣지) — `apps/mobile/src/features/provider/RoomForm.tsx:466-485`. 영업시간을 30분 그리드 ComboSelect로 입력하는데(RN `<input type=time>` 부재), 웹 `type=time`으로 생성된 비-그리드 값(예: 09:15)을 가진 기존 룸을 모바일에서 수정 진입하면 ComboSelect가 매칭 옵션을 못 찾아 placeholder("시작"/"종료")로 비어 보인다. 저장값(`d.open`)은 보존되나, 사용자가 비어 보이는 콤보를 건드리면 09:15가 그리드값으로 덮인다(무음 데이터 변경). 스토리에 트레이드오프로 문서화됨·저빈도(시드/대부분 정시각). **트리거(언제):** 임의 분(分) 영업시간 지원 또는 web↔mobile 교차편집 UX 하드닝 시. **회수:** 비-그리드 값을 동적 옵션으로 주입하거나 시간 입력을 자유 입력+검증으로 전환. (edge)
- **챗봇 링크 화이트리스트 `INTERNAL_HREF_RE`가 빈 파라미터 딥링크 미링크** (LOW, fail-closed·웹 패리티) — `apps/mobile/src/features/chatbot/ChatbotPanel.tsx:38-39`. 정규식의 탐색 딥링크 분기 `\/(?:\?view=list(?:&(?:sigungu|dong)=\d{1,10})*)?`는 `\d{1,10}`(1자리 이상)을 요구하므로, 주석 예시 `/?view=list&sigungu=&dong=`처럼 **빈 값**(`=` 뒤 숫자 없음)은 매칭 실패→평문 렌더(미링크). `INTERNAL_HREF_RE`는 웹 verbatim 복사라 9.3 도입 아님. 보안상 fail-closed(미링크=안전)라 회귀 아님·기능상 일부 유효 딥링크가 탭 불가일 뿐. **트리거(언제):** LLM이 빈-파라미터 딥링크를 실제로 생성하거나 탐색 딥링크 라우팅 강화 시. **회수:** 빈 값을 허용하도록 정규식 조정(`=\d{0,10}`) — **web+mobile 동시**(verbatim 동기화 유지). (blind)
- **⚠️ [배포 전 사람-확인-필수·자동 안 잡힘] 챗봇 SSE 프록시 버퍼링/컷오프 root-cause 하드닝** (MEDIUM, 배포 게이트) — `apps/api`(SSE 응답 헤더/keepalive) + 배포 프록시 config. **증상:** 중간 프록시(Railway/CDN 등)가 SSE 응답을 버퍼링하다 `event: done` 없이 깨끗이 끊으면 react-native-sse가 종료 이벤트를 안 줘 챗봇 입력창이 영구 잠김. **현 상태:** 모바일 클라에 **idle 워치독(60초 무활동 graceful 강등)** 을 code-review에서 적용해 **안전망은 확보**(`streamMessage.ts`)했으나, 이는 60초 지연 후 회복일 뿐 **근본 원인은 프록시**다. **★이 항목은 빌드+푸시(기계적 배포)로 자동 검증되지 않는다 — 반드시 사람이 첫 프로덕션 배포 시 직접 확인해야 한다(passive 메모만으론 누락됨).** **트리거(언제):** 첫 프로덕션 배포([[deployment-first-time-checklist]])의 **의무 체크리스트 라인**으로 등록. **회수(사람이 직접):** ① 실프록시 뒤에서 챗봇 스트리밍 스모크 테스트(메시지 전송→입력창이 매번 다시 풀리는지 = 스트림 정상 종료 확인) ② 백엔드 `EventSourceResponse`에 `X-Accel-Buffering: no` 헤더 + sse-starlette `ping`(keepalive) 간격 설정 ③ 프록시 read/idle 타임아웃을 최대 스트림 길이보다 길게. 부족하면 워치독 값 실측 재조정. (code-review·blind+edge) — **현 단계(localhost 직결)에선 프록시 부재로 발생 불가**, 배포로 프록시가 끼는 순간 비로소 발현.
