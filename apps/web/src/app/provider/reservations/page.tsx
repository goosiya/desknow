// provider 예약자 현황 페이지 (idea.md L38). 셸/네비는 layout, 상태/거부는 ProviderReservations.
import { ProviderReservations } from "@/features/provider/ProviderReservations";

export default function ProviderReservationsPage() {
  return <ProviderReservations />;
}
