// provider 후기 페이지 (idea.md L39). 셸/네비는 layout, 목록/답글은 ProviderReviews.
import { ProviderReviews } from "@/features/provider/ProviderReviews";

export default function ProviderReviewsPage() {
  return <ProviderReviews />;
}
