// This file is auto-generated by @hey-api/openapi-ts

import type { Client, ClientMeta, Options as Options2, RequestResult, TDataShape } from './client';
import { client } from './client.gen';
import type { AdminCancelReservationData, AdminCancelReservationErrors, AdminCancelReservationResponses, AdminDeactivateAccountData, AdminDeactivateAccountErrors, AdminDeactivateAccountResponses, AdminListAccountsData, AdminListAccountsErrors, AdminListAccountsResponses, AdminListIngestDocumentsData, AdminListIngestDocumentsErrors, AdminListIngestDocumentsResponses, AdminListReservationsData, AdminListReservationsErrors, AdminListReservationsResponses, AdminTriggerIngestData, AdminTriggerIngestErrors, AdminTriggerIngestResponses, AuthLoginData, AuthLoginErrors, AuthLoginResponses, AuthLogoutData, AuthLogoutErrors, AuthLogoutResponses, AuthMeData, AuthMeErrors, AuthMeResponses, AuthRefreshData, AuthRefreshErrors, AuthRefreshResponses, AuthRegisterData, AuthRegisterErrors, AuthRegisterResponses, ChatbotGetTranscriptData, ChatbotGetTranscriptErrors, ChatbotGetTranscriptResponses, ChatbotResetSessionData, ChatbotResetSessionErrors, ChatbotResetSessionResponses, ChatbotSendMessageData, ChatbotSendMessageErrors, ChatbotSendMessageResponses, ChatbotStreamMessageData, ChatbotStreamMessageErrors, ChatbotStreamMessageResponses, FavoritesAddFavoriteData, FavoritesAddFavoriteErrors, FavoritesAddFavoriteResponses, FavoritesListFavoritesData, FavoritesListFavoritesErrors, FavoritesListFavoritesResponses, FavoritesRemoveFavoriteData, FavoritesRemoveFavoriteErrors, FavoritesRemoveFavoriteResponses, HealthHealthData, HealthHealthResponses, NotificationsDismissNotificationData, NotificationsDismissNotificationErrors, NotificationsDismissNotificationResponses, NotificationsDismissReminderData, NotificationsDismissReminderErrors, NotificationsDismissReminderResponses, NotificationsListNotificationsData, NotificationsListNotificationsErrors, NotificationsListNotificationsResponses, ReservationsCancelReservationData, ReservationsCancelReservationErrors, ReservationsCancelReservationResponses, ReservationsCreateReservationData, ReservationsCreateReservationErrors, ReservationsCreateReservationResponses, ReservationsListProviderReservationsData, ReservationsListProviderReservationsErrors, ReservationsListProviderReservationsResponses, ReservationsListReservationsData, ReservationsListReservationsErrors, ReservationsListReservationsResponses, ReservationsRejectReservationData, ReservationsRejectReservationErrors, ReservationsRejectReservationResponses, ReviewsCreateReplyData, ReviewsCreateReplyErrors, ReviewsCreateReplyResponses, ReviewsCreateReviewData, ReviewsCreateReviewErrors, ReviewsCreateReviewResponses, ReviewsListRoomReviewsData, ReviewsListRoomReviewsErrors, ReviewsListRoomReviewsResponses, RoomsAggregateAvailabilityData, RoomsAggregateAvailabilityResponses, RoomsCreateRoomData, RoomsCreateRoomErrors, RoomsCreateRoomResponses, RoomsGeocodeAddressData, RoomsGeocodeAddressErrors, RoomsGeocodeAddressResponses, RoomsGetMyRoomData, RoomsGetMyRoomErrors, RoomsGetMyRoomResponses, RoomsGetRoomData, RoomsGetRoomErrors, RoomsGetRoomResponses, RoomsGetRoomSlotsData, RoomsGetRoomSlotsErrors, RoomsGetRoomSlotsResponses, RoomsListRegionsData, RoomsListRegionsResponses, RoomsListRoomsData, RoomsListRoomsResponses, RoomsSearchRoomsData, RoomsSearchRoomsErrors, RoomsSearchRoomsResponses, RoomsUpdateRoomData, RoomsUpdateRoomErrors, RoomsUpdateRoomResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: keyof ClientMeta extends never ? Record<string, unknown> : ClientMeta;
};

/**
 * List Accounts
 *
 * booker·provider 계정 목록을 조회한다 → 200(admin 가드, 읽기 전용·페이지네이션, AC4).
 *
 * 로그인 → RBAC 403 게이트 → 실데이터 조회를 end-to-end로 실증한다(AC2·AC4).
 */
export const adminListAccounts = <ThrowOnError extends boolean = false>(options?: Options<AdminListAccountsData, ThrowOnError>): RequestResult<AdminListAccountsResponses, AdminListAccountsErrors, ThrowOnError> => (options?.client ?? client).get<AdminListAccountsResponses, AdminListAccountsErrors, ThrowOnError>({ url: '/api/v1/admin/accounts', ...options });

/**
 * Deactivate Account
 *
 * 계정을 비활성한다(provider면 룸 캐스케이드) → 200(admin 가드, 멱등, Story 8.2).
 *
 * 행 삭제가 아닌 ``is_active`` flip이라 액션 동사 ``POST .../deactivate``를 쓴다(취소/거절
 * 4.7/6.2 선례). 멱등(이미 비활성)은 200이라 별도 status 없음. admin 대상/미존재는 404
 * ``ACCOUNT_NOT_FOUND``(존재 누설 방지). 캐스케이드·원자성·멱등은 전부 service가 보장한다.
 */
export const adminDeactivateAccount = <ThrowOnError extends boolean = false>(options: Options<AdminDeactivateAccountData, ThrowOnError>): RequestResult<AdminDeactivateAccountResponses, AdminDeactivateAccountErrors, ThrowOnError> => (options.client ?? client).post<AdminDeactivateAccountResponses, AdminDeactivateAccountErrors, ThrowOnError>({ url: '/api/v1/admin/accounts/{account_id}/deactivate', ...options });

/**
 * Trigger Ingest
 *
 * ``docs_corpus/`` 문서 인제스트를 트리거하고 처리 리포트를 반환한다(Story 8.4, AC1·3·5).
 *
 * **★동기 ``def`` 핸들러(``async def`` 절대 금지):** ``ingest_corpus``·``build_embedder``(OpenAI
 * HTTP)·``SqlDocumentChunkStore``(동기 psycopg3 commit)는 전부 블로킹이다. FastAPI는 **동기
 * ``def`` 라우트를 스레드풀에서 실행**해 이벤트 루프를 막지 않는다 — ``async def``로 만들면 블로킹
 * 호출이 루프를 직접 막아 전 서버가 정지한다(반복 함정). 처리 후 ``IngestReport``를 200으로 즉시
 * 반환한다(잡 큐·폴링 없음). 멱등·부분실패·stale 청크 reconcile은 전부 service/코어가 보장한다.
 *
 * **에러 계약:** 무토큰 401 ``UNAUTHENTICATED``·비-admin 403 ``FORBIDDEN_ROLE``만. per-doc 실패는
 * 리포트 본문(``failed``)이라 엔드포인트 자체는 200(404/409 없음).
 */
export const adminTriggerIngest = <ThrowOnError extends boolean = false>(options?: Options<AdminTriggerIngestData, ThrowOnError>): RequestResult<AdminTriggerIngestResponses, AdminTriggerIngestErrors, ThrowOnError> => (options?.client ?? client).post<AdminTriggerIngestResponses, AdminTriggerIngestErrors, ThrowOnError>({ url: '/api/v1/admin/ingest', ...options });

/**
 * List Ingest Documents
 *
 * 인제스트 지식베이스 문서 목록을 상태와 함께 조회한다 → 200(admin 가드, 읽기 전용).
 *
 * 운영자가 인제스트 메뉴 진입 시 현재 적재 현황(어떤 문서가 ingested/pending/orphan인지)을
 * 본다. **읽기 전용**(OpenAI·DB 쓰기 0) — ``POST /ingest``의 파괴적 실행과 분리돼, 메뉴를
 * 여는 것만으로 인제스트가 돌지 않는다. 정적 경로 ``/ingest/documents``가 ``POST /ingest``와
 * 충돌하지 않도록 별도 서브패스로 둔다(메서드+경로 모두 구분).
 */
export const adminListIngestDocuments = <ThrowOnError extends boolean = false>(options?: Options<AdminListIngestDocumentsData, ThrowOnError>): RequestResult<AdminListIngestDocumentsResponses, AdminListIngestDocumentsErrors, ThrowOnError> => (options?.client ?? client).get<AdminListIngestDocumentsResponses, AdminListIngestDocumentsErrors, ThrowOnError>({ url: '/api/v1/admin/ingest/documents', ...options });

/**
 * List Reservations
 *
 * 확정 예약 목록을 조회한다 → 200(admin·읽기 전용·confirmed-only·페이지네이션, Story 8.3).
 *
 * 운영자가 임의 취소 대상(확정 예약)을 식별하는 목록이다. 예약자는 실 이메일로 노출한다
 * (익명 라벨 아님 — accounts 정합). 정렬·페이지네이션·N+1 합성은 전부 service가 보장한다.
 */
export const adminListReservations = <ThrowOnError extends boolean = false>(options?: Options<AdminListReservationsData, ThrowOnError>): RequestResult<AdminListReservationsResponses, AdminListReservationsErrors, ThrowOnError> => (options?.client ?? client).get<AdminListReservationsResponses, AdminListReservationsErrors, ThrowOnError>({ url: '/api/v1/admin/reservations', ...options });

/**
 * Cancel Reservation
 *
 * 확정 예약을 임의 취소한다(슬롯 재활성 + 예약자 통지) → 200(admin 가드, 멱등, Story 8.3).
 *
 * 상태 전이(``cancelled``) + 점유 슬롯 DELETE(재활성) + 예약자 ``status_change``/``cancelled``
 * 통지가 **단일 트랜잭션으로 원자**(deferred L42 회수). 행 삭제가 아닌 status flip이라 액션 동사
 * ``POST .../cancel``을 쓴다(4.7/6.2 선례). 멱등(이미 종료)은 200(별도 409 없음). 미존재는 404
 * ``RESERVATION_NOT_FOUND``(누설 방지). **시간 게이트 없음**(admin 권한 — service가 프리미티브
 * 직접 호출). 슬롯 재활성·통지 원자·멱등은 전부 service가 보장한다.
 */
export const adminCancelReservation = <ThrowOnError extends boolean = false>(options: Options<AdminCancelReservationData, ThrowOnError>): RequestResult<AdminCancelReservationResponses, AdminCancelReservationErrors, ThrowOnError> => (options.client ?? client).post<AdminCancelReservationResponses, AdminCancelReservationErrors, ThrowOnError>({ url: '/api/v1/admin/reservations/{reservation_id}/cancel', ...options });

/**
 * Login
 *
 * 이메일·비밀번호로 로그인한다 → 200 + 토큰 쌍(본문) + 인증 쿠키 set(AC1·AC2).
 *
 * 잘못된 자격(미존재·틀린 비번·비활성)은 서비스가 401 ``UNAUTHENTICATED``로 단일화한다.
 */
export const authLogin = <ThrowOnError extends boolean = false>(options: Options<AuthLoginData, ThrowOnError>): RequestResult<AuthLoginResponses, AuthLoginErrors, ThrowOnError> => (options.client ?? client).post<AuthLoginResponses, AuthLoginErrors, ThrowOnError>({
    url: '/api/v1/auth/login',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Logout
 *
 * 로그아웃한다 → 204(AC4 — 멱등). refresh 해시 행을 삭제하고 인증 쿠키를 제거한다.
 *
 * 토큰이 본문/쿠키 모두 없거나 이미 무효여도 204로 정상 종료한다(멱등).
 */
export const authLogout = <ThrowOnError extends boolean = false>(options: Options<AuthLogoutData, ThrowOnError>): RequestResult<AuthLogoutResponses, AuthLogoutErrors, ThrowOnError> => (options.client ?? client).post<AuthLogoutResponses, AuthLogoutErrors, ThrowOnError>({
    url: '/api/v1/auth/logout',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Me
 *
 * 현재 인증된 사용자를 반환한다 → 200 + UserPublic(인증 필요, AC1·AC5 실증).
 *
 * 프론트 세션 복원에 사용 + ``get_current_principal``(헤더/쿠키 추출)을 실증한다.
 */
export const authMe = <ThrowOnError extends boolean = false>(options?: Options<AuthMeData, ThrowOnError>): RequestResult<AuthMeResponses, AuthMeErrors, ThrowOnError> => (options?.client ?? client).get<AuthMeResponses, AuthMeErrors, ThrowOnError>({ url: '/api/v1/auth/me', ...options });

/**
 * Refresh
 *
 * refresh 토큰으로 새 토큰 쌍을 회전 발급한다 → 200(AC3).
 *
 * 토큰은 본문(RN) 또는 쿠키(웹)에서 추출한다. 만료/위조/회전·로그아웃됨은 401.
 */
export const authRefresh = <ThrowOnError extends boolean = false>(options: Options<AuthRefreshData, ThrowOnError>): RequestResult<AuthRefreshResponses, AuthRefreshErrors, ThrowOnError> => (options.client ?? client).post<AuthRefreshResponses, AuthRefreshErrors, ThrowOnError>({
    url: '/api/v1/auth/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Register
 *
 * 이메일·비밀번호·역할로 가입한다 → 201 + UserPublic(해시 비노출).
 */
export const authRegister = <ThrowOnError extends boolean = false>(options: Options<AuthRegisterData, ThrowOnError>): RequestResult<AuthRegisterResponses, AuthRegisterErrors, ThrowOnError> => (options.client ?? client).post<AuthRegisterResponses, AuthRegisterErrors, ThrowOnError>({
    url: '/api/v1/auth/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get Transcript
 *
 * 현재 thread의 메시지 이력을 반환한다 → 200(재수화용, AC4). 빈 thread=[]. 미인증 401.
 */
export const chatbotGetTranscript = <ThrowOnError extends boolean = false>(options: Options<ChatbotGetTranscriptData, ThrowOnError>): RequestResult<ChatbotGetTranscriptResponses, ChatbotGetTranscriptErrors, ThrowOnError> => (options.client ?? client).get<ChatbotGetTranscriptResponses, ChatbotGetTranscriptErrors, ThrowOnError>({ url: '/api/v1/chatbot/messages', ...options });

/**
 * Send Message
 *
 * 메시지를 보내고 봇의 전체 응답을 반환한다 → 200(로그인 필요, AC3).
 *
 * 같은 ``thread_id``(=``user_id:device_id``)로 연속 전송 시 이전 맥락이 유지된다(checkpointer
 * 자동 누적·멀티턴). LLM 업스트림 실패는 502 ``LLM_PROVIDER_UNAVAILABLE``. 미인증은 401.
 */
export const chatbotSendMessage = <ThrowOnError extends boolean = false>(options: Options<ChatbotSendMessageData, ThrowOnError>): RequestResult<ChatbotSendMessageResponses, ChatbotSendMessageErrors, ThrowOnError> => (options.client ?? client).post<ChatbotSendMessageResponses, ChatbotSendMessageErrors, ThrowOnError>({
    url: '/api/v1/chatbot/messages',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reset Session
 *
 * 해당 thread의 대화 상태를 폐기한다 → 204(로그아웃 초기화·멱등, AC5). 미인증 401.
 */
export const chatbotResetSession = <ThrowOnError extends boolean = false>(options: Options<ChatbotResetSessionData, ThrowOnError>): RequestResult<ChatbotResetSessionResponses, ChatbotResetSessionErrors, ThrowOnError> => (options.client ?? client).delete<ChatbotResetSessionResponses, ChatbotResetSessionErrors, ThrowOnError>({ url: '/api/v1/chatbot/session', ...options });

/**
 * Stream Message
 *
 * 메시지를 보내고 봇 응답을 **SSE 토큰 스트리밍**한다 → 200 ``text/event-stream``(AC1·AC2).
 *
 * 미인증 401은 스트림 시작 **전** dependency(``get_current_principal``)라 정상 HTTP 401이다.
 * 스트림이 시작되면 200 헤더가 이미 송신되므로(함정 #2), LLM 업스트림 실패를 ``raise``로 전파하지
 * 않고 ``event: error`` 프레임으로 **인밴드** 전달한다 → 클라가 graceful degrade(AC4).
 * 각 토큰은 ``data: {"delta": "..."}``(JSON — ``\n``·공백·``[DONE]`` 섞여도 프레임 무손상,
 * deferred L129 회수), 정상 종료는 ``event: done``. 같은 ``thread_id``로 재스트림 시 7.3
 * checkpointer가 맥락을 누적한다.
 */
export const chatbotStreamMessage = <ThrowOnError extends boolean = false>(options: Options<ChatbotStreamMessageData, ThrowOnError>): RequestResult<ChatbotStreamMessageResponses, ChatbotStreamMessageErrors, ThrowOnError> => (options.client ?? client).post<ChatbotStreamMessageResponses, ChatbotStreamMessageErrors, ThrowOnError>({
    url: '/api/v1/chatbot/stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Favorites
 *
 * 현재 사용자의 즐겨찾기를 최근순 **한 페이지**로 반환한다 → 200(로그인 필요, AC2·AC3·AC5 + F).
 *
 * 비활성 룸도 포함하며(AC3 — 행에서 '비활성' 라벨), 각 행에 신선 ``remaining_slots``를 싣는다.
 * ``created_at`` keyset 페이징(``CursorPage`` 봉투). ``limit``(기본 20·최대 100)·``cursor``는 쿼리,
 * 손상 커서는 422. 미인증은 401. 즐겨찾기가 없으면 ``items=[]``·``next_cursor=None``(정상 200).
 */
export const favoritesListFavorites = <ThrowOnError extends boolean = false>(options?: Options<FavoritesListFavoritesData, ThrowOnError>): RequestResult<FavoritesListFavoritesResponses, FavoritesListFavoritesErrors, ThrowOnError> => (options?.client ?? client).get<FavoritesListFavoritesResponses, FavoritesListFavoritesErrors, ThrowOnError>({ url: '/api/v1/favorites', ...options });

/**
 * Add Favorite
 *
 * 즐겨찾기에 룸을 추가한다 → 201 + FavoriteRoomItem(로그인 필요, AC1·AC4·AC5).
 *
 * 멱등 — 이미 즐겨찾기된 룸이면 기존 행 기준으로 201을 돌려준다(토글 견고성). 미존재 룸은
 * 404 ``ROOM_NOT_FOUND``, 미인증은 401 ``UNAUTHENTICATED``.
 */
export const favoritesAddFavorite = <ThrowOnError extends boolean = false>(options: Options<FavoritesAddFavoriteData, ThrowOnError>): RequestResult<FavoritesAddFavoriteResponses, FavoritesAddFavoriteErrors, ThrowOnError> => (options.client ?? client).post<FavoritesAddFavoriteResponses, FavoritesAddFavoriteErrors, ThrowOnError>({
    url: '/api/v1/favorites',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove Favorite
 *
 * 즐겨찾기에서 룸을 제거한다 → 204(로그인 필요·멱등, AC1·AC5).
 *
 * 없는 행을 제거해도 204(멱등 — 토글 견고성). 미인증은 401.
 */
export const favoritesRemoveFavorite = <ThrowOnError extends boolean = false>(options: Options<FavoritesRemoveFavoriteData, ThrowOnError>): RequestResult<FavoritesRemoveFavoriteResponses, FavoritesRemoveFavoriteErrors, ThrowOnError> => (options.client ?? client).delete<FavoritesRemoveFavoriteResponses, FavoritesRemoveFavoriteErrors, ThrowOnError>({ url: '/api/v1/favorites/{room_id}', ...options });

/**
 * Health
 *
 * liveness 헬스체크. DB·외부 의존성에 접근하지 않는다(DB readiness는 lifespan에서만 검증).
 */
export const healthHealth = <ThrowOnError extends boolean = false>(options?: Options<HealthHealthData, ThrowOnError>): RequestResult<HealthHealthResponses, unknown, ThrowOnError> => (options?.client ?? client).get<HealthHealthResponses, unknown, ThrowOnError>({ url: '/api/v1/health', ...options });

/**
 * List Notifications
 *
 * 현재 사용자의 통지를 반환한다 → 200(로그인 필요·읽기전용, AC1·AC3·AC4).
 *
 * **두 종류를 머지**한다: ① 도래 리마인드(``reminders.due_reminder_reservations`` — 24h 이내
 * 확정 예약을 **행 없이 도출**, 억제건 제외) ② status_change pending(``service.list_pending``).
 * 각 항목의 ``room_name``은 라우터가 PK 조회로 합성한다(service rooms import 금지·순환 회피).
 * 리마인드(``slot_start`` asc·임박순) 먼저, status_change(created_at desc) 다음의 **결정적 순서**.
 * ``now``는 1회 캡처해 도출 전체에 일관 주입한다. DB 쓰기 0(억제건 조회만). 미인증은 401.
 */
export const notificationsListNotifications = <ThrowOnError extends boolean = false>(options?: Options<NotificationsListNotificationsData, ThrowOnError>): RequestResult<NotificationsListNotificationsResponses, NotificationsListNotificationsErrors, ThrowOnError> => (options?.client ?? client).get<NotificationsListNotificationsResponses, NotificationsListNotificationsErrors, ThrowOnError>({ url: '/api/v1/notifications', ...options });

/**
 * Dismiss Reminder
 *
 * 도래 리마인드를 '다시 보지 않기'로 영속 억제한다 → 204(로그인 필요·멱등, AC2).
 *
 * **소유권 가드 먼저(반복함정 #5·FK 500 회피):** ``session.get(Reservation, id)`` → 미존재이거나
 * ``booker_id``가 요청자가 아니면 404 ``NOTIFICATION_NOT_FOUND``(타인/미존재 예약 누설 금지 —
 * 403 아님). 이 가드 없이 ``suppress_reminder``를 바로 부르면 미존재 ``reservation_id``가 FK를
 * 위반해 raw 500이 된다. 통과 시 ``service.suppress_reminder``(born-dismissed 억제행·멱등)에
 * 위임한다. 경로 세그먼트 3개라 ``/{notification_id}/dismiss``(2개)와 충돌 없음. 미인증=401.
 */
export const notificationsDismissReminder = <ThrowOnError extends boolean = false>(options: Options<NotificationsDismissReminderData, ThrowOnError>): RequestResult<NotificationsDismissReminderResponses, NotificationsDismissReminderErrors, ThrowOnError> => (options.client ?? client).post<NotificationsDismissReminderResponses, NotificationsDismissReminderErrors, ThrowOnError>({ url: '/api/v1/notifications/reminders/{reservation_id}/dismiss', ...options });

/**
 * Dismiss Notification
 *
 * status_change 통지 한 건을 소멸 처리한다 → 204(로그인 필요·멱등, 5.1 AC5).
 *
 * '확인'(5.3)이 이 generic 엔드포인트를 소비한다. 소유권 검증(타인/미존재=404
 * ``NOTIFICATION_NOT_FOUND``·누설 금지) + ``dismissed_at`` 설정(멱등 — 이미 소멸이면 no-op)은
 * ``service.dismiss_notification``에 위임한다. 도래 리마인드 '다시 보지 않기'는 행 id가 없어
 * 별도 ``/reminders/{reservation_id}/dismiss``를 쓴다(KTH 확정 — 동일 dismiss 메커니즘·다른 키).
 * 미인증은 401.
 */
export const notificationsDismissNotification = <ThrowOnError extends boolean = false>(options: Options<NotificationsDismissNotificationData, ThrowOnError>): RequestResult<NotificationsDismissNotificationResponses, NotificationsDismissNotificationErrors, ThrowOnError> => (options.client ?? client).post<NotificationsDismissNotificationResponses, NotificationsDismissNotificationErrors, ThrowOnError>({ url: '/api/v1/notifications/{notification_id}/dismiss', ...options });

/**
 * List Provider Reservations
 *
 * 제공자 소유 룸의 예약을 최근순 **한 페이지**로 반환한다 → 200(provider, AC1·AC2·AC3 + F).
 *
 * ``list_reservations``(예약자 본인 목록, 4.8)의 **거울상** — 소유권 축이 ``booker_id``(본인) →
 * ``Room.provider_id``(소유 룸)이고, 예약자 식별정보를 **익명 라벨**로 가린다(FR-23 Privacy).
 * 본인 소유 룸이 없거나 예약이 없으면 빈 리스트(정상 200). 미인증=401,
 * booker/admin=403(``_require_provider``).
 *
 * **합성(도메인 경계 — architecture.md L354·4.9 순환 회피):**
 *
 * ① ``rooms_service.list_provider_rooms``로 소유 룸을 가져온다(``Room.provider_id`` 필터 — 타
 * 제공자 룸의 예약은 구조적으로 노출 불가, AC3). 그 결과로 **이름 맵**(``{room.id: room.name}``)을
 * 만든다(룸 0개면 이후 빈 목록). ② ``service.list_reservations_for_rooms``로 그 룸들의 예약을
 * 상태 무관·``created_at`` desc로 조회한다(reservations.service는 ``room_ids``만 받고 rooms를
 * import하지 않는다). ③ 각 예약을 ``ProviderReservationItem``으로 매핑 — ``room_name``은 이름
 * 맵에서(행마다 ``session.get(Room)`` 재조회 금지 = N+1 회피, 소유 룸은 이미 메모리에),
 * ``booker_label``은 ``booker_display_label(reservation.booker_id)``로 해시 파생한다.
 * **``booker_id``는 응답에 싣지 않는다**(AC2 — 이메일·raw UUID 비노출).
 *
 * 슬롯 시간은 ``Reservation.slot_starts`` **스냅샷**(취소/거절 후에도 잔존)을 싣는다.
 * 다가오는/지난 분류·정렬은 소비처가 ``slot_starts``로 계산한다(서버 flat — 4.8 범위 결정 #3, 소비
 * UI는 provider 웹 표면 후속). **읽기 전용**(상태 전이·슬롯 변경 0 — cancel↔reject race는 6.2).
 */
export const reservationsListProviderReservations = <ThrowOnError extends boolean = false>(options?: Options<ReservationsListProviderReservationsData, ThrowOnError>): RequestResult<ReservationsListProviderReservationsResponses, ReservationsListProviderReservationsErrors, ThrowOnError> => (options?.client ?? client).get<ReservationsListProviderReservationsResponses, ReservationsListProviderReservationsErrors, ThrowOnError>({ url: '/api/v1/provider/reservations', ...options });

/**
 * Reject Reservation
 *
 * 제공자 본인 소유 룸의 확정 예약을 시작 전까지 거절한다 → 200 + ReservationPublic(AC1~AC5).
 *
 * 6.1 읽기 위의 첫 쓰기(거절) 슬라이스. 분기 순서(중요):
 *
 * ① **소유권 가드(AC5 — 404로 누설 금지):** ``session.get(Reservation, id)`` + 그 예약의 룸을
 * ``session.get(Room, reservation.room_id)``로 조회해 **본인 소유 룸(`Room.provider_id ==
 * principal.user_id`)** 인지 확인한다. 미존재 예약·**타 제공자 룸의 예약**·룸 부재는 **동일 404
 * ``RESERVATION_NOT_FOUND``**(403 아님 — 타인 예약 존재 누설 금지, 4.7 cancel 미러). 룸 소유권은
 * 라우터가 판정한다(reservations.service는 rooms import 금지 — 4.9 순환 회피, 라우터=조합 계층).
 *
 * ② **게이트 + 거절(+ 통지 원자):** ``service.reject_reservation_for_provider``가 종료 상태면 즉시
 * 멱등 반환, 아니면 시작 전 게이트(``earliest <= now`` → 409 ``REJECT_WINDOW_PASSED``)를 강제한 뒤
 * ``reject_reservation``(status flip + 슬롯 DELETE + **예약자 ``status_change``/reason=
 * ``'rejected'`` 통지가 동일 트랜잭션**)을 호출한다. ``now`` 미주입=실시간(``now_utc``). 게이트는
 * service에 위임한다.
 *
 * **★통지 원자화(Story 8.3 — deferred L42 회수):** 거절 통지 생성은 이제 **service가 winner
 * 판정 시 전이 commit과 한 트랜잭션**에서 한다(이전엔 라우터가 거절 *후* 별도 트랜잭션으로
 * ``create_notification``을 호출해, 통지 실패 시 거절은 영속인데 재시도가 "이미 종료"로 통지를
 * 건너뛰어 영구 손실 위험이 있었다). 라우터의 ``was_confirmed`` 사전 캡처·통지 호출은 제거됐다 —
 * 통지 결정은 service의 ``rowcount`` winner 판정으로 일원화(race 정확성·retry-safe 둘 다 충족).
 * 거절 통지가 **생성되는 사실은 6.2와 불변**(배선만 router→service 이동·원자화). 예약자 web 배너
 * 표시는 5.3이 이미 구축(web 신규 0).
 *
 * 성공·멱등 모두 **200**(생성만 201). 거절 후 점유 슬롯은 비워졌으므로 ``slot_starts=[]``로
 * 직렬화한다(이미 종료 상태인 멱등 경로도 슬롯 0건이라 동일 — cancel 엔드포인트 미러). 에러코드·
 * 숫자 상태는 화면에 노출하지 않는다(고정 한국어 ``message``만 — UX-DR10, FE 분기는 detail.code).
 */
export const reservationsRejectReservation = <ThrowOnError extends boolean = false>(options: Options<ReservationsRejectReservationData, ThrowOnError>): RequestResult<ReservationsRejectReservationResponses, ReservationsRejectReservationErrors, ThrowOnError> => (options.client ?? client).post<ReservationsRejectReservationResponses, ReservationsRejectReservationErrors, ThrowOnError>({ url: '/api/v1/provider/reservations/{reservation_id}/reject', ...options });

/**
 * List Reservations
 *
 * 예약자 본인의 예약을 최근순 **한 페이지**로 반환한다 → 200(booker, AC1·AC2·AC4 + F 페이징).
 *
 * 본인(``booker_id == principal.user_id``) 예약만 상태 무관(confirmed/cancelled/rejected)으로
 * ``created_at`` 내림차순 keyset 페이징한다(``CursorPage`` 봉투 — ``items`` + ``next_cursor``;
 * 다가오는/지난 분류·6h 취소 가능 판정은 FE가 ``slot_starts`` 스냅샷으로 계산 — 4.7 범위 결정 #3).
 * ``limit``(기본 20·최대 100)·``cursor``(이전 페이지 토큰)는 쿼리. 손상 커서는 422. 미인증=401,
 * provider/admin=403(``_require_booker``). 예약이 없으면 ``items=[]``·``next_cursor=None``(정상 200).
 *
 * **룸 이름·is_active 합성은 라우터에서**(``session.get(Room, …)``): reservations.service가
 * rooms를 import하면 4.9 ``rooms.service → reservations.service``(차감) 역방향과 **순환 import**가
 * 되므로, service는 ``reservations`` 테이블만 만지고 룸 메타 합성은 조합 계층인 라우터가
 * 한다(``rooms.models`` import는 router 한정 = service↔service 순환 아님 — favorites
 * ``build_favorite_item`` 선례). 슬롯 시간은 ``Reservation.slot_starts`` **스냅샷**(취소/거절
 * 후에도 잔존 — 범위 결정 #1)을 싣는다.
 *
 * **``has_review`` 합성(Story 5.5 — 후기 작성 게이팅):** ``reviews.service``의
 * ``reservation_ids_with_review``를 **단일 호출**(N+1 금지)해 본인 후기 존재 예약 집합을 구하고
 * 각 항목에 ``has_review = reservation.id in ids``를 싣는다. reservations.router →
 * reviews.service **단방향**(라우터=조합 계층, room_name 합성과 동형 — service↔service 순환 아님:
 * reviews.service는 reservations를 import하지 않는다).
 */
export const reservationsListReservations = <ThrowOnError extends boolean = false>(options?: Options<ReservationsListReservationsData, ThrowOnError>): RequestResult<ReservationsListReservationsResponses, ReservationsListReservationsErrors, ThrowOnError> => (options?.client ?? client).get<ReservationsListReservationsResponses, ReservationsListReservationsErrors, ThrowOnError>({ url: '/api/v1/reservations', ...options });

/**
 * Create Review
 *
 * 이용 완료한 본인 예약에 별점+텍스트 후기를 작성한다 → 201 + ReviewPublic(booker, AC1~AC3).
 *
 * ① **소유권 가드(AC3):** ``session.get(Reservation, id)`` → 미존재이거나 ``booker_id``가
 * 요청자가 아니면 **404 ``RESERVATION_NOT_FOUND``**. 타인 예약을 403이 아니라 404로 막아 **존재
 * 여부를 누설하지 않는다**(reservations cancel 패턴 미러). 미인증=401·provider/admin=403은
 * ``_require_booker``. ② **자격·중복은 service:** 이용 완료 아님(취소/거절/미완료) → 409
 * ``RESERVATION_NOT_COMPLETED``, 이미 작성 → 409 ``REVIEW_ALREADY_EXISTS``. 입력 형식(별점 범위·
 * 텍스트 길이)은 ``ReviewCreateRequest``가 선차단(422). room_id는 예약에서 도출한다(경로 불요).
 */
export const reviewsCreateReview = <ThrowOnError extends boolean = false>(options: Options<ReviewsCreateReviewData, ThrowOnError>): RequestResult<ReviewsCreateReviewResponses, ReviewsCreateReviewErrors, ThrowOnError> => (options.client ?? client).post<ReviewsCreateReviewResponses, ReviewsCreateReviewErrors, ThrowOnError>({
    url: '/api/v1/reservations/{reservation_id}/reviews',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Reply
 *
 * 제공자가 자기 룸 후기에 답글을 단다 → 201 + ReviewReplyPublic(provider, 5.6 AC1·AC2·AC3·AC4).
 *
 * ① **후기 존재(AC2):** ``session.get(Review, id)`` → 미존재면 **404 ``REVIEW_NOT_FOUND``**.
 * ② **소유권(AC2):** 후기의 룸(``session.get(Room, review.room_id)``)이 요청자 소유가 아니면
 * **403 ``REVIEW_REPLY_FORBIDDEN``** — 후기·룸은 공개 데이터라 존재 누설이 무관하고 epic AC가
 * 명시 403을 요구한다(booker 도메인의 404 비노설과 의도적 분기). 라우터=조합 계층이 ``Room``을
 * 조회한다(reviews.service는 ``Room``을 만지지 않음 — 순환 금지, 반복 함정 #3). ③ **중복은
 * service:** 이미 답글이 달린 후기 → 409 ``REVIEW_REPLY_ALREADY_EXISTS``. 미인증=401·
 * booker/admin=403 ``FORBIDDEN_ROLE``은 ``_require_provider``. 텍스트 형식은
 * ``ReviewReplyCreateRequest`` 선차단(422). room_id는 후기에서 도출(경로 불요). 비활성
 * 룸이라도 본인 룸이면 허용(자기 데이터).
 */
export const reviewsCreateReply = <ThrowOnError extends boolean = false>(options: Options<ReviewsCreateReplyData, ThrowOnError>): RequestResult<ReviewsCreateReplyResponses, ReviewsCreateReplyErrors, ThrowOnError> => (options.client ?? client).post<ReviewsCreateReplyResponses, ReviewsCreateReplyErrors, ThrowOnError>({
    url: '/api/v1/reviews/{review_id}/reply',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Rooms
 *
 * 활성 룸의 핀 메타(``{room_id, name, lat, lng}``)를 반환한다 → 200(공개, AC1·AC2).
 *
 * 첫 진입 지도 핀 좌표 공급(FR-4). **인증 없음**(탐색 첫 화면 핀은 비로그인 — PRD §FR-2,
 * ``/availability``와 동일 근거). 가용성(핀 색)은 ``/availability``, 룸 상세는 바텀시트
 * (Story 3.3)가 책임진다 — 이 응답은 핀 좌표·이름만 싣는다(과조회 방지). 컬렉션 루트
 * ``GET ""``는 ``POST ""``(등록·메서드 다름)·``GET /{room_id}``(동적·경로 세그먼트 다름)와
 * 충돌하지 않으나, 일관성을 위해 정적 그룹 근처·``PATCH /{room_id}`` 앞에 선언한다.
 * operationId = ``rooms_list_rooms``(1.9 규약, 도메인 내 유일).
 */
export const roomsListRooms = <ThrowOnError extends boolean = false>(options?: Options<RoomsListRoomsData, ThrowOnError>): RequestResult<RoomsListRoomsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RoomsListRoomsResponses, unknown, ThrowOnError>({ url: '/api/v1/rooms', ...options });

/**
 * Create Room
 *
 * 공간을 등록한다 → 201 + RoomPublic(provider 전용, AC1·AC5).
 *
 * 인증된 provider의 ``user_id``를 ``provider_id``로 ``create_room`` 호출. 제공자당 1개
 * 초과는 409, 자정 넘김 영업시간 등 검증 실패는 422(Pydantic→1.5 핸들러).
 */
export const roomsCreateRoom = <ThrowOnError extends boolean = false>(options: Options<RoomsCreateRoomData, ThrowOnError>): RequestResult<RoomsCreateRoomResponses, RoomsCreateRoomErrors, ThrowOnError> => (options.client ?? client).post<RoomsCreateRoomResponses, RoomsCreateRoomErrors, ThrowOnError>({
    url: '/api/v1/rooms',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Aggregate Availability
 *
 * 활성 룸별 오늘 잔여 빈 슬롯 수를 반환한다 → 200(공개, AC1·AC3).
 *
 * 핀 색 결정용 집계(FR-5·NFR-2) — 서버가 1회 계산해 ``list[RoomAvailability]``(각
 * ``{room_id, remaining_slots}``)를 돌려준다. **인증 없음**(탐색 첫 화면 핀은 비로그인 —
 * PRD §FR-2). ``now`` 미주입 → ``now_utc()`` 기준. 정적 경로라 ``/{room_id}``보다 먼저 선언한다.
 */
export const roomsAggregateAvailability = <ThrowOnError extends boolean = false>(options?: Options<RoomsAggregateAvailabilityData, ThrowOnError>): RequestResult<RoomsAggregateAvailabilityResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RoomsAggregateAvailabilityResponses, unknown, ThrowOnError>({ url: '/api/v1/rooms/availability', ...options });

/**
 * Geocode Address
 *
 * 주소를 검색해 좌표·지역 코드 후보를 반환한다 → 200(provider 전용, AC2).
 *
 * 카카오 REST 키는 백엔드 전용(NFR-6)이라 이 프록시를 경유한다. 업스트림 실패는 502.
 * 결과 0건은 정상(빈 리스트). 정적 경로라 향후 ``/{room_id}``보다 먼저 선언한다.
 */
export const roomsGeocodeAddress = <ThrowOnError extends boolean = false>(options: Options<RoomsGeocodeAddressData, ThrowOnError>): RequestResult<RoomsGeocodeAddressResponses, RoomsGeocodeAddressErrors, ThrowOnError> => (options.client ?? client).get<RoomsGeocodeAddressResponses, RoomsGeocodeAddressErrors, ThrowOnError>({ url: '/api/v1/rooms/geocode', ...options });

/**
 * Get My Room
 *
 * provider 본인 룸 상세를 반환한다 → 200(provider 전용 — 등록/수정 폼 prefill).
 *
 * 인증된 provider의 ``user_id``로 본인 룸(0..1)을 조회한다. 없으면 404 ``ROOM_NOT_FOUND``
 * (등록 안 함 → 폼 생성 모드). 정적 경로라 동적 ``/{room_id}``보다 **먼저** 선언한다(미선언 시
 * "mine"이 room_id로 잡혀 422). booker/admin은 403, 미인증 401(``_require_provider``).
 */
export const roomsGetMyRoom = <ThrowOnError extends boolean = false>(options?: Options<RoomsGetMyRoomData, ThrowOnError>): RequestResult<RoomsGetMyRoomResponses, RoomsGetMyRoomErrors, ThrowOnError> => (options?.client ?? client).get<RoomsGetMyRoomResponses, RoomsGetMyRoomErrors, ThrowOnError>({ url: '/api/v1/rooms/mine', ...options });

/**
 * List Regions
 *
 * 활성 룸이 있는 시/군/구→동/읍/면 콤보 트리를 반환한다 → 200(공개, AC1).
 *
 * 지역 콤보(1차 시군구·2차 동) 데이터 공급. 보유 룸의 지역 b_code를 백엔드 번들 참조에
 * 조인해 라벨·필터 목록을 구성한다(룸이 있는 지역만). **인증 없음**(탐색은 비로그인 —
 * PRD §FR-2, ``/availability``·``GET ""``와 동일 근거). 정적 경로라 동적 ``/{room_id}``보다
 * **먼저 선언**한다(미선언 시 "regions"가 room_id로 잡혀 422). operationId =
 * ``rooms_list_regions``(1.9, 도메인 유일).
 */
export const roomsListRegions = <ThrowOnError extends boolean = false>(options?: Options<RoomsListRegionsData, ThrowOnError>): RequestResult<RoomsListRegionsResponses, unknown, ThrowOnError> => (options?.client ?? client).get<RoomsListRegionsResponses, unknown, ThrowOnError>({ url: '/api/v1/rooms/regions', ...options });

/**
 * Search Rooms
 *
 * 선택 지역/반경(또는 전체)의 활성 룸을 신선 슬롯과 함께 **한 페이지** 반환 → 200(공개·AC1·AC4 + F).
 *
 * 목록 행(이름·가격·룸형태·부대시설 + 예약 가능 배지용 신선 ``remaining_slots``). 두 검색방식:
 *
 * - **지역(3.4):** ``region_code`` 미지정=지역 필터 없음, 지정=시군구/동 레벨 코드로 필터.
 * - **반경(3.5):** ``lat``·``lng``가 **둘 다** 있을 때만 반경 필터 적용(부분 좌표=미적용).
 * ``radius_km`` 미지정 시 기본 3km, 결과는 가까운 순 정렬. ``region_code``와 동시=교집합.
 *
 * **페이징(F):** 거리/지역 계산 정렬이라 **offset 커서**(``CursorPage`` 봉투 — ``items`` +
 * ``next_cursor``). ``limit``(기본 20·최대 100)·``cursor``는 쿼리, 손상/음수 커서는 422.
 * **미매핑/미존재 region_code=빈 페이지 200**(에러 계약 없음 — graceful). 범위 밖 파라미터
 * (``lat∉[-90,90]``·``lng∉[-180,180]``·``radius_km≤0`` 또는 50 초과)=**422**(Query 검증→1.5).
 * **인증 없음**(탐색은 비로그인 — PRD §FR-2). 정적 경로라 ``/{room_id}``보다 **먼저 선언**한다.
 * operationId = ``rooms_search_rooms``(1.9, 도메인 유일).
 */
export const roomsSearchRooms = <ThrowOnError extends boolean = false>(options?: Options<RoomsSearchRoomsData, ThrowOnError>): RequestResult<RoomsSearchRoomsResponses, RoomsSearchRoomsErrors, ThrowOnError> => (options?.client ?? client).get<RoomsSearchRoomsResponses, RoomsSearchRoomsErrors, ThrowOnError>({ url: '/api/v1/rooms/search', ...options });

/**
 * Get Room
 *
 * 단일 룸 신선 요약(바텀시트, Story 3.3) → 200(공개, AC1·AC4).
 *
 * 가격·수용·룸형태·부대시설·영업시간 + ``derive_slots`` 재사용한 **신선 ``remaining_slots``**.
 * **인증 없음**(탐색 첫 화면 핀 탭 — PRD §FR-2, ``/availability``·``GET ""``와 동일 근거).
 * 미존재/비활성=404 ``ROOM_NOT_FOUND``. ``provider_id`` 미노출(공개·과조회 방지). 동적 라우트라
 * 정적 경로 뒤·``PATCH /{room_id}`` 근처 선언. operationId = ``rooms_get_room``(1.9, 도메인 유일).
 * 4.2 상세 화면이 이 엔드포인트를 후기/예약 UI로 확장한다.
 */
export const roomsGetRoom = <ThrowOnError extends boolean = false>(options: Options<RoomsGetRoomData, ThrowOnError>): RequestResult<RoomsGetRoomResponses, RoomsGetRoomErrors, ThrowOnError> => (options.client ?? client).get<RoomsGetRoomResponses, RoomsGetRoomErrors, ThrowOnError>({ url: '/api/v1/rooms/{room_id}', ...options });

/**
 * Update Room
 *
 * 본인 소유 공간을 부분 수정한다 → 200 + RoomPublic(provider 전용, AC1·AC4·AC5).
 *
 * ``model_dump(exclude_unset=True)`` 부분 수정(PATCH 시맨틱) — 미제공 필드는 불변,
 * ``business_hours`` 제공 시 전체 교체. 소유권은 **서비스에서 최종 강제**(타인/미존재
 * room_id는 404 ``ROOM_NOT_FOUND``로 합침). 자정 넘김·범위 밖 값 등 검증 실패는 422.
 * operationId = ``rooms_update_room``(1.9 규약, 도메인 내 유일).
 */
export const roomsUpdateRoom = <ThrowOnError extends boolean = false>(options: Options<RoomsUpdateRoomData, ThrowOnError>): RequestResult<RoomsUpdateRoomResponses, RoomsUpdateRoomErrors, ThrowOnError> => (options.client ?? client).patch<RoomsUpdateRoomResponses, RoomsUpdateRoomErrors, ThrowOnError>({
    url: '/api/v1/rooms/{room_id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create Reservation
 *
 * 선택한 빈 연속 슬롯을 결제 없이 즉시 확정한다 → 201 + ReservationPublic(booker, AC1·AC2).
 *
 * ① 신선 재검증: 요청 슬롯의 ROOM_TZ 날짜에 대해 ``get_room_slots``(4.3 — ``derive_slots`` 재사용)
 * 로 가용 슬롯 집합을 구하고 ``요청 ⊆ 가용``을 확인한다(룸 미존재/비활성은 그 호출의 404 가드).
 * 위반(stale·과거·영업시간외·휴무)은 409 ``SLOT_CONFLICT``. ② 확정: 4.1 ``create_reservation``으로
 * 예약 단위 1건 + 슬롯 점유 행을 단일 트랜잭션 all-or-nothing 생성(이미 점유된 슬롯은 UNIQUE가
 * 최종 차단 → 409). 입력 형식 위반(빈/naive/중복/교차일)은 스키마가 선차단(422).
 */
export const reservationsCreateReservation = <ThrowOnError extends boolean = false>(options: Options<ReservationsCreateReservationData, ThrowOnError>): RequestResult<ReservationsCreateReservationResponses, ReservationsCreateReservationErrors, ThrowOnError> => (options.client ?? client).post<ReservationsCreateReservationResponses, ReservationsCreateReservationErrors, ThrowOnError>({
    url: '/api/v1/rooms/{room_id}/reservations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel Reservation
 *
 * 예약자 본인 확정 예약을 취소한다 → 200 + ReservationPublic(status cancelled, AC1~AC4).
 *
 * 분기 순서(중요):
 *
 * ① **소유권 가드(AC4):** ``session.get(Reservation, id)`` → 미존재이거나 ``booker_id``가
 * 요청자가 아니거나 경로 ``room_id``와 불일치면 **404 ``RESERVATION_NOT_FOUND``**. 타인 예약을
 * 403이 아니라 404로 막아 **타인 예약 존재 여부를 누설하지 않는다**(2.3 ``ROOM_NOT_FOUND`` 패턴
 * 미러 — 미존재와 비-소유를 동일 404로 합침). 미인증=401·provider/admin=403은 ``_require_booker``.
 *
 * ② **게이트 + 취소:** ``service.cancel_reservation_for_booker``가 종료 상태면 즉시 멱등 반환,
 * 아니면 6h 윈도우(``< 6시간`` → 409 ``CANCEL_WINDOW_PASSED``)를 강제한 뒤 4.1
 * ``cancel_reservation``(status flip + 슬롯 DELETE 단일 트랜잭션)을 호출한다. 6h 게이트는
 * **service에 위임**한다.
 *
 * 성공·멱등 모두 **200**(생성만 201). 취소 후 점유 슬롯은 비워졌으므로 ``slot_starts=[]``로
 * 직렬화한다(이미 종료 상태인 멱등 경로도 슬롯이 0건이라 동일). 에러코드·숫자 상태는 화면에
 * 노출하지 않는다(고정 한국어 ``message``만 — UX-DR10, FE 분기는 4.8 ``detail.code``).
 */
export const reservationsCancelReservation = <ThrowOnError extends boolean = false>(options: Options<ReservationsCancelReservationData, ThrowOnError>): RequestResult<ReservationsCancelReservationResponses, ReservationsCancelReservationErrors, ThrowOnError> => (options.client ?? client).post<ReservationsCancelReservationResponses, ReservationsCancelReservationErrors, ThrowOnError>({ url: '/api/v1/rooms/{room_id}/reservations/{reservation_id}/cancel', ...options });

/**
 * List Room Reviews
 *
 * 룸 상세의 후기를 최신순 **한 페이지**로 반환한다 → 200(공개·무인증, AC4 + F 페이징).
 *
 * 룸 상세는 공개 표면(``GET /rooms/{id}`` 동일 근거 — PRD §FR-2)이라 **인증 없음**. 각 후기는
 * **별점·텍스트·작성일만**(작성자 식별 미노출 — KTH 결정 1 익명). ``created_at`` keyset 페이징
 * (``CursorPage`` 봉투). ``limit``(기본 20·최대 100)·``cursor``는 쿼리, 손상 커서는 422. 후기가
 * 없으면 ``items=[]``·``next_cursor=None``(정상 200 — 막다른 화면 금지는 FE 빈 상태 카피).
 * 미존재/비활성 룸도 빈/기존 목록을 낸다(후기는 공개 데이터, 상세 404와 분리).
 */
export const reviewsListRoomReviews = <ThrowOnError extends boolean = false>(options: Options<ReviewsListRoomReviewsData, ThrowOnError>): RequestResult<ReviewsListRoomReviewsResponses, ReviewsListRoomReviewsErrors, ThrowOnError> => (options.client ?? client).get<ReviewsListRoomReviewsResponses, ReviewsListRoomReviewsErrors, ThrowOnError>({ url: '/api/v1/rooms/{room_id}/reviews', ...options });

/**
 * Get Room Slots
 *
 * 활성 룸의 ``date``(ROOM_TZ) 1시간 슬롯을 상태별로 + 다음 빈 날짜를 반환한다 → 200(공개, AC1).
 *
 * 그날 1시간 슬롯을 상태별(``available`` 가용/``past`` 지난 시간/``reserved`` 예약됨)로,
 * 그리고 그날 다음부터 30일 내 첫 빈 날(``next_available_date``)을 함께 싣는다. ``derive_slots``
 * (2.1) 재사용 — ``reserved`` 상태는 **Story 4.9 예약 차감 연결 전까지 미발생**. **인증 없음**
 * (상세·슬롯은 비로그인 — PRD §FR-2, ``GET /{room_id}``와 동일 근거). 미존재/비활성=404
 * ``ROOM_NOT_FOUND``. ``date`` 누락/오형식=422(Query 파싱→1.5 핸들러).
 *
 * **라우팅 순서:** ``/{room_id}/slots``는 2-세그먼트라 정적 ``/geocode``·``/availability``·
 * ``/regions``·``/search``·``GET ""`` 및 1-세그먼트 동적 ``/{room_id}``와 충돌하지 않는다(경로
 * 깊이 다름) — 동적 ``/{room_id}`` 그룹 근처에 둔다. operationId = ``rooms_get_room_slots``(1.9,
 * 도메인 유일) → SDK ``roomsGetRoomSlots``.
 */
export const roomsGetRoomSlots = <ThrowOnError extends boolean = false>(options: Options<RoomsGetRoomSlotsData, ThrowOnError>): RequestResult<RoomsGetRoomSlotsResponses, RoomsGetRoomSlotsErrors, ThrowOnError> => (options.client ?? client).get<RoomsGetRoomSlotsResponses, RoomsGetRoomSlotsErrors, ThrowOnError>({ url: '/api/v1/rooms/{room_id}/slots', ...options });
